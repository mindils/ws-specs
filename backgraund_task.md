## Фоновые задачи

Изучи выгрузку в excel 
app/src/main/java/ru/fgk/ws/app/common/action/AsyncCollectionExcelExportAction.java и все нужное для выполнения задачи.

Интересует как сделана фонавая состовляющая она полностью отвязана от ui vaadin 
и там есть возможность после того как все выгрузится отправить сообщение пользователю и сохранится в таблицу реализованный файл 

мне надо сделать универсальный функционал какого-то job в который можно будет работать фоново запустить отвязанного от ui vaadin 
чтобы не вешать пользовательский интерфейс

в этом job должен быть функционал 
## Функционал 
1. универсальныя таблица куда можно будет сохранить ход выполнения, резальтаты если есть (файл),  и другие нужные поля 
2. проверь как я фоново выгружаю  в exele там есть варианты обхода всех таблицы на основе лоадера мне бы хотелось также иметь возможность
обхода по всех таблице
3. возможность остановить выполнинеи может отправилькакойто сигдал и job выйдес со статувом остановленно 
4. должен быть параметр в котором можно указать многопоточность и он быдет запускать одновременно несколко 
например если есть флаг многопоточности  я получаю из locder первую пачку запускаю потом сразу вторую запускаю и так далее 
5. прогресс 


## как я это вижу но ты можешь сделать по другому

есть универсальный запускальщик фоновых задач

есть какойто интерфейс у которого будет 
старт  стоп получить новые элементы записать прогресс и  так далее

чтобы была возможность запускать как обход всей таблицы и для каждой пачки чтото делать и может еще какието задачи
не связанные с запуском обхода таблицы.


## для тестирования 
сделай на странице dr-download-file-archive новую кнопку 
выгрузить в фоне
при нажатии на эту кнопку будут открываться экран на котором будет выбор 
все текущая страница выбронные элементы и выбор samba или архив.  в коде я еще могу указать по сколько елементов пакинации брать, укажи пока 5
при нажатии на должен выполнится на каждую пачке log и на каждый элемент пачки log
это как раз будет функционал который будет каждый раз разный


если я чтото неточно или не полно написал, спроси
