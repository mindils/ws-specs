# Проверка 001 — T01

Таск: [../task.md](../task.md)
Результат реализации: [../result.md](../result.md)
Итерация: 1
Дата: 2026-09-16 15:00
Состояние кода: commit 1ad658d3f (ветка f/container в worktree cyan-fennel), изменён UiMinimalRole.java, добавлен UiMinimalRoleTest.java, незакоммиченные файлы закрытой работы сохранены
Окружение: worktree ../worktrees/rvk-ws/cyan-fennel/rvk-ws, БД docker-rvk-db-1 (localhost:5432, схема main_rvk_ws), первая проверка по порядку плана (БД и gradle daemon)
Итог: pass — все критерии C1–C3 и негативный сценарий подтверждены

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | UiMinimalRoleTest#grantsFullAccessToOwnFilterConfigs (READ, CREATE, UPDATE, DELETE и MODIFY атрибутов на RvkFilterConfig) |
| C2 | Подтверждён | UiMinimalRoleTest#keepsLoginAndMainViewAccess (ui.loginToUi, MainView, LoginView) |
| C3 | Подтверждён | ./gradlew spotlessCheckAll завершился BUILD SUCCESSFUL |
| Негативный сценарий | Подтверждён | UiMinimalRoleTest#doesNotGrantFilterAdministration (отсутствуют rvkfilter.manageGlobal и manageAllConfigs) |
| Регрессия ролей раздела | Подтверждён | ru.fgk.ws.app.container.security.ContainerRolesTest (8 passing) |
| Правило комментариев | Подтверждён | Проверка изменённых файлов на запрещённые токены дала 0 совпадений |

## Выполненные проверки

- Запуск теста роли в worktree:
  `./gradlew -p ../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests 'ru.fgk.ws.app.security.UiMinimalRoleTest'`
  Код завершения: 0. Результат: 3 passing (22.9s).
  Проверены методы `grantsFullAccessToOwnFilterConfigs`,
  `doesNotGrantFilterAdministration`, `keepsLoginAndMainViewAccess`.
- Регрессионная проверка ролей раздела:
  `./gradlew -p ../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests 'ru.fgk.ws.app.container.security.*'`
  Код завершения: 0. Результат: 8 passing (20.6s).
- Проверка форматирования:
  `./gradlew -p ../worktrees/rvk-ws/cyan-fennel/rvk-ws spotlessCheckAll`
  Код завершения: 0. Результат: BUILD SUCCESSFUL (1s).
- Проверка комментариев по протоколу:
  Поиск регулярным выражением `\b[TFQC]\d{1,}\b|specs/work/|решение пользователя|проверяющ|итераци`
  по файлам `UiMinimalRole.java` и `UiMinimalRoleTest.java` — 0 совпадений.

## Ошибки и непроверенное

Ошибок не обнаружено.
Живая проверка работы личного пресета в браузере под пользователем с ролью
`ui-minimal` будет выполнена в рамках сквозного таска T04 согласно плану.

## Следующий шаг

Таск T01 завершён (`done`). Следующий шаг по порядку плана — проверка или
выполнение таска [T03](../../tasks/T03/task.md).
