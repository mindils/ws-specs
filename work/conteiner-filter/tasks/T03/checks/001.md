# Проверка 001 — T03

Таск: [../task.md](../task.md)
Результат реализации: [../result.md](../result.md)
Итерация: 1
Дата: 2026-09-16 15:45
Состояние кода: commit 1ad658d3f (ветка f/container в worktree cyan-fennel), изменены 13 XML-дескрипторов cnt-*-list-view.xml и messages_ru.properties, незакоммиченные файлы закрытой работы сохранены
Окружение: worktree ../worktrees/rvk-ws/cyan-fennel/rvk-ws, БД docker-rvk-db-1 (localhost:5432, схема main_rvk_ws), bootRun на порту 8082, playwright-cli, проверка после T01 по порядку плана
Итог: pass — критерии C1–C3, регрессии и браузерные сценарии подтверждены

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | 13 XML переведены на flt:rvkFilter, filterPanel удалена; ContainerNsiViewsUiTest#allDictionaryListViews_display (passing) |
| C2 | Подтверждён | Браузер через playwright-cli: поиск, доп. условия, Reset и сохранение пресета проверены на cnt-models, cnt-repair-types, cnt-defects |
| C3 | Подтверждён | 14 ключей msg:// в 13 XML резолвятся, неиспользуемых ключей нет, сырых msg:// в UI нет |
| Регрессия диалогов | Подтверждён | ContainerNsiViewsUiTest#allDictionaryDetailViews_displayForNewEntity (passing) |
| Пакет тестов раздела | Подтверждён | ./gradlew :app:test --tests 'ru.fgk.ws.app.container.*' — 73 passing (48.1s) |
| Форматирование | Подтверждён | ./gradlew spotlessCheckAll завершился BUILD SUCCESSFUL |
| Правило комментариев | Подтверждён | Поиск запрещённых токенов по изменённым файлам дал 0 совпадений |

## Выполненные проверки

- Статический анализ дескрипторов:
  Во всех 13 файлах `cnt-*-list-view.xml` проверено отсутствие `filterPanel`
  и наличие компонента `flt:rvkFilter` с корректным `searchProperty` по полю
  названия и `defaultVisible="true"`.
- Проверка i18n ключей:
  Скриптом проверены все 14 `msg://`-ссылок из 13 XML-дескрипторов против
  `messages_ru.properties`. Все ключи успешно разрешаются. В бандл добавлен
  только один необходимый ключ `cntDefectListView.filter.defectReason`,
  неиспользуемых ключей не оставлено.
- Автоматические UI-тесты:
  `./gradlew -p ../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests 'ru.fgk.ws.app.container.view.ContainerNsiViewsUiTest'`
  Код завершения: 0. Результат: 2 passing (27.7s) — проверены открытие всех
  13 списков и всех 13 detail-форм новой записи.
- Полный пакет тестов контейнерного раздела:
  `./gradlew -p ../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests 'ru.fgk.ws.app.container.*'`
  Код завершения: 0. Результат: 73 passing (48.1s).
- Проверка форматирования:
  `./gradlew -p ../worktrees/rvk-ws/cyan-fennel/rvk-ws spotlessCheckAll`
  Код завершения: 0. Результат: BUILD SUCCESSFUL (1s).
- Проверка комментариев по правилу протокола:
  Поиск regex `\b[TFQC]\d{1,}\b|specs/work/|решение пользователя|проверяющ|итераци`
  по 13 изменённым XML и добавленным строкам в `messages_ru.properties`
  дал 0 совпадений.
- Браузерное тестирование через `playwright-cli` (`bootRun` на порту 8082,
  вход под пользователем с ролью `admin`):
  1. «Модели контейнеров» (`cnt-models`): отображается панель `rvkFilter`
     со строкой поиска. Ввод «1СС» в поиск сузил список с 3 до 2 строк
     (1СС-20, 1СС-40). Добавлено условие по числу дуг «Дуги = 6» через
     «Добавить поле», список сузился до 1 строки (1СС-40). Кнопка «Сбросить»
     вернула исходные 3 строки. Сохранён личный пресет «Мой фильтр 1СС».
     После перезагрузки страницы пресет доступен в меню «Мои фильтры», при
     клике успешно применяется и фильтрует грид.
  2. «Виды ремонтов контейнеров» (`cnt-repair-types`): в поиске введено «Капит»,
     грид сузился с 4 строк до 1 («Капитальный ремонт»). Кнопка «Сбросить»
     вернула все 4 строки.
  3. «Неисправности контейнеров» (`cnt-defects`): через «Добавить поле» выбрано
     свойство по пути `defectReason.defectReasonName` с локализованной
     подписью «Причина неисправности». Фильтрация по значению «технолог»
     сузила список до 2 строк с технологической причиной неисправности.
  4. В консоли браузера и серверном логе приложения ошибок не зафиксировано.
     Сырых `msg://` на проверенных экранах нет.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C3 подтверждены.

## Повторная проверка

Не требуется, проверка первой итерации пройдена с первого раза.

## Следующий шаг

Таск T03 завершён (`done`). Следующий шаг по порядку плана — проверка или
выполнение таска [T02](../../tasks/T02/task.md) (6 основных страниц раздела
«Контейнеры»).
