# T04 — Сквозная проверка раздела с RVK Filter

Статус: in_progress — проход 2 после T05
План: [../../plan.md](../../plan.md)
Зависимости: T01, T02, T03, [T05](../T05/task.md)
Сложность: medium — правки дефектов в уже сделанных областях, новых контрактов нет
Сложность проверки: hard — живое окружение: браузер под ролями, БД, полный контейнерный пакет
Актуальная проверка: [checks/001.md](checks/001.md) — устарела, состояние до T05

## Коротко

После того как роль (T01), справочники (T03), основные страницы (T02) и
перенос справочных ссылок внутрь `rvkFilter` (T05) приняты по отдельности,
проверяется собранное состояние ветки `f/container` целиком: тесты раздела,
форматирование и браузерный проход под ролью просмотра с минимальной ролью.
Найденные дефекты исправляются здесь же. По результату раздел готов к
слиянию пользователем.

Пересмотр 2026-09-17 ([Q01](../../questions/Q01.md)): первый проход
(проверка 001, 2026-09-16) выполнялся, когда справочные фильтры стояли
штатными выпадающими списками над `rvkFilter`; после T05 он повторяется
целиком на entity-полях.

## Результат и контекст

Проверяются критерии всей работы из плана на актуальном состоянии worktree
`../worktrees/rvk-ws/cyan-fennel/rvk-ws`. Пользователи схемы `main_rvk_ws`:
`user_read` (`container-read` + `ui-minimal`), `user_edit`, `user_nsi`
(пароли — как заведены в схеме; если неизвестны, задать через штатный список
пользователей под `admin` и записать в `result.md`). Локальный `user`/`user`
имеет `system-full-access`.

## Область и изоляция

Правки только в областях T01–T03, T05 и только для дефектов, найденных здесь;
изменение контрактов плана не допускается без вопроса. Занимает все общие
ресурсы (БД, gradle daemon, порт 8080, браузер) — выполняется последним, в
одной сессии.

## Реализация

1. Проверить, что T01–T03 и T05 в статусе `done`, свериться с их
   `result.md`; jar в `libs/` — пара 0.0.2.
2. Прогнать команды из «Самопроверки», исправить падения.
3. Пройти браузерный сценарий из «Независимой проверки» самостоятельно как
   smoke, дефекты исправить, повторить тесты затронутой области.

## Критерии приёмки

- C1: `./gradlew :app:test --tests 'ru.fgk.ws.app.container.*' --tests 'ru.fgk.ws.app.nsi.view.vdepo.*' --tests 'ru.fgk.ws.app.security.UiMinimalRoleTest'` зелёный.
- C2: `./gradlew spotlessCheckAll` — BUILD SUCCESSFUL.
- C3: под `user_read` все 19 страниц открываются, поиск и условия
  фильтруют, entity-поля дают подсказки по вводу и выбор через «Выбрать…»,
  личный пресет с entity-условием сохраняется и выбирается после
  переоткрытия с названием, а не id; под `user_nsi` пресет сохраняется на
  справочнике; 0 `ERROR` в логе, сырых `msg://` нет; штатных `propertyFilter`
  на страницах нет.
- C4: `grep -rn 'ListView.filter\.' messages_ru.properties` для контейнерных
  view показывает только ключи, на которые ссылаются XML.

## Самопроверка исполнителя

```bash
./gradlew spotlessApply :app:compileJava :app:compileTestJava
./gradlew :app:test --tests 'ru.fgk.ws.app.container.*' --tests 'ru.fgk.ws.app.nsi.view.vdepo.*' --tests 'ru.fgk.ws.app.security.UiMinimalRoleTest'
./gradlew spotlessCheckAll
```

## Независимая проверка

- Те же команды на состоянии, зафиксированном в `result.md` (commit или
  список файлов).
- Браузер (`bootRun` в фоне, `/actuator/health` UP, `playwright-cli`,
  `/local-login`): под `user_read` — реестр (поиск, условие по дате
  следующего освидетельствования, entity-поле «Модель»: подсказки по вводу,
  «Выбрать…» через `cnt_CntModel.list`, + поиск, Reset, пресет с названием
  модели после переоткрытия), ремонты (поиск по номеру контейнера, «Вид
  ремонта» из подсказок, «Исполнитель» по вводу без кнопки выбора, «Договор
  на ремонт» через lookup), освидетельствования, акты, договоры, гарантии
  («ИД», «Договор»), справочник «Виды неисправностей» (скрытое поле «Причина
  неисправности») и ещё один справочник; под `user_nsi` — справочник с
  пресетом; под `user_edit` — создание ремонта из списка с работающим lookup
  контейнера. Проверить отсутствие 403/500 и сырых `msg://`; погасить только
  свой процесс.
- Отчёт фиксирует изоляцию (схема, порт) и порядок занятия ресурсов.

## Прогресс и продолжение

Проход 1 (2026-09-16, до T05, проверка 001 — `pass`, устарела):

- [x] Тесты и spotless зелёные (79 passing, `spotlessCheckAll` BUILD SUCCESSFUL).
- [x] Сверка ключей `…ListView.filter.*` с `msg://` в XML в обе стороны.
- [x] Браузерный smoke под тремя пользователями: 19 страниц, поиск, условия,
      выпадающие списки, пресеты, lookup контейнера, 0 `ERROR` в логе.
- [x] Передать результат на независимую проверку.

Проход 2 (после `done` у T05, итерация 2):

- [ ] Тесты C1 и spotless зелёные на jar 0.0.2.
- [ ] Сверка ключей `…ListView.filter.*` с `msg://` в XML в обе стороны;
      `<propertyFilter` вне `flt:` в контейнерных view нет.
- [ ] Браузерный smoke под тремя пользователями с entity-полями, пресет с
      названием, lookup контейнера, 0 `ERROR` в логе.
- [ ] Передать результат на независимую проверку.

Проход 1: дефектов в областях T01–T03 не найдено, код не менялся.
Приложение профиля `local` слушает порт 8082, а не 8080; готовность — по
`Started AppApplication` и `200` на `/local-login`, `/actuator/health` в этом
профиле за аутентификацией.

Независимая проверка [checks/001.md](checks/001.md) — `pass`: C1–C4
подтверждены на живом окружении, дефектов в областях T01–T03 нет.

Ближайший шаг: после `done` у [T05](../T05/task.md) — проход 2 по
«Самопроверке» и «Независимой проверке», результат — `result.md` итерация 2.
Препятствия: ожидает T05 (не блокер, порядок выполнения по плану)
