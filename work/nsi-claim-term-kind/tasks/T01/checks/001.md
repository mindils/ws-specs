# Проверка 001 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 09:45
Состояние кода: коммит 5bdd5934757fb3c154ac3dbb932c96e634bf967f плюс незакоммиченные изменения рабочих областей legal и nsi
Окружение: PostgreSQL 16.11 (docker-rvk-db-1, localhost:5432, схема main), Keycloak, MinIO, Temporal; порт 8080 освобождён от устаревшего процесса bootRun (PID 3376036) по выбору пользователя, запущен свежий bootRun в фоне; браузерный проход playwright-cli под пользователем admin с ролью nsi-claim-term-edit; после проверки bootRun погашен
Итог: pass — все критерии C1–C5 и негативный сценарий подтверждены

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | В БД 16 строк (7 «техн», 9 «повр»); повторный сид с ON CONFLICT DO NOTHING идемпотентен, ручная правка term_days сохраняется |
| C2 | Подтверждён | Браузерный render-проход в playwright-cli и UI-тест NsiClaimTermNormViewsUiTest: 8 колонок с подписями по ВагТК, отображение «техн»/«повр» текстом, фильтр отбирает 9 строк «повр», карточка даёт выбор из списка, поле «Дата окончания» сохраняется |
| C3 | Подтверждён | ClaimTermCalculatorTest, TechClaimDeadlineIT, TechClaimViewIT зелёные без изменения ожиданий; строки «повр» не влияют на расчёт сроков |
| C4 | Подтверждён | Сохранение новой нормы через форму UI открыло ревизию 2 со статусом DRAFT («Изменены нормы сроков претензионных этапов»), нажатие «Опубликовать изменения» в UI перевело ревизию в PUBLISHED; NsiClaimTermEditRoleIT зелёный |
| C5 | Подтверждён | В messages_ru.properties проверены все ключи сущности, enum и экрана; сырых msg:// нет; spotlessCheckAll и :app:compileJava успешны; полный набор :app:test — 1384 passing, 0 failing |

## Выполненные проверки

1. **SQL и идемпотентность (C1):**
   - Запрос: `select claim_kind, count(*) from main.nsi_claim_term_norm group by 1 order by 1;`
     Результат: `1 → 7`, `3 → 9` (всего 16 строк).
   - Changelog-и: `01-tbl/331` id 1, 2, 3 и `04-data/331` id 1, 2 в статусе EXECUTED / RERAN.
   - Проверка идемпотентности: изменение `term_days = 99` у строки `claim_kind = 1, operation_code = 16`, повторное выполнение INSERT с `ON CONFLICT DO NOTHING` — вставка 0 строк, значение 99 сохранено, затем восстановлено 2.
   - Тест `WorkCalendarReferenceDataIT.reRunningSeedKeepsUserChanges` пройден.

2. **Статический анализ и компиляция (C5):**
   - `./gradlew spotlessCheckAll :app:compileJava` — BUILD SUCCESSFUL (21 up-to-date).

3. **Целевые интеграционные тесты (C2, C3, C4):**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.NsiClaimTermEditRoleIT" --tests "ru.fgk.ws.app.it.WorkCalendarReferenceDataIT" --tests "ru.fgk.ws.app.legal.techclaim.service.ClaimTermCalculatorTest" --tests "ru.fgk.ws.app.nsi.view.claimtermnorm.NsiClaimTermNormViewsUiTest" --tests "ru.fgk.ws.app.it.TechClaimDeadlineIT" --tests "ru.fgk.ws.app.it.TechClaimViewIT"`:
     Результат: 83 passing (37.3s), 0 failing.

4. **Полный набор тестов проекта (C3, C5):**
   - `./gradlew :app:test`:
     Результат: 1384 passing (1m 51s), 12 pending, 0 failing, BUILD SUCCESSFUL.

5. **Браузерный render-проход в Playwright CLI (C2, C4):**
   - Освобождён порт 8080 (завершён устаревший процесс PID 3376036, сняты блокировки `.lck` HSQLDB).
   - Запущен `./gradlew :app:bootRun` в фоне.
   - Вход под учётной записью `admin`/`admin` через `/local-login` (назначена роль `nsi-claim-term-edit`).
   - Маршрут `/nsi-claim-term-norms`:
     - Подписи колонок грида: «Причина отцепки», «Код операции», «Наименование операции», «Нормативный срок (дней)», «Тип дней», «Порядок этапа», «Дата начала», «Дата окончания».
     - Текстовый вывод вида претензии: «техн» и «повр».
     - Колонка «Дата окончания» содержит значения выгрузки («Дата передачи в ДЭПС», «Дата передачи в департ.»).
   - Фильтрация: выбор «повр» в фильтре «Причина отцепки» оставил в гриде ровно 9 строк, все с видом «повр».
   - Создание нормы: нажатие кнопки «Создать», открылся диалог «Норма срока претензионного этапа».
     - Выпадающий список «Причина отцепки» содержит варианты «техн» и «повр», по умолчанию «техн».
     - Заполнены поля: «повр», код операции 991, наименование «Тестовый этап UI», срок 7, дата начала «Дата начала теста», дата окончания «Дата окончания теста».
     - Нажата кнопка «OK» — диалог закрылся без ошибок (0 errors). В БД проверено появление записи с `finish_note = 'Дата окончания теста'`.
   - Ревизии НСИ (C4): переход на `/nsi-term-revisions`.
     - Отображён баннер о наличии неопубликованных изменений.
     - В таблице появилась ревизия 2 со статусом «Черновик» и примечанием «Изменены нормы сроков претензионных этапов».
     - Нажата кнопка «Опубликовать изменения» — ревизия 2 перешла в статус «Опубликована», баннер скрылся.
   - Очистка тестовых данных: удалена проверочная строка с кодом 991 и ревизия 2; в таблице восстановлены 16 строк.

6. **Негативный сценарий:**
   - В интерфейсе нажата кнопка «Создать», введены существующие значения: вид «техн», код операции 16, наименование «Дубль», срок 5.
   - Нажата кнопка «OK».
   - Наблюдаемое поведение: UI вывел штатное предупреждение `alert: Произошло нарушение ограничения уникальности (UK_NSI_CLAIM_TERM_NORM)`. Ошибки 500, падения контекста или белого экрана нет.

7. **Завершение окружения:**
   - Фоновый процесс `bootRun` остановлен, порт 8080 закрыт.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии проверены и подтверждены.

## Повторная проверка

Первичная проверка итерации 1.

## Следующий шаг

Таск T01 переведён в статус `done`. Так как T01 — единственный таск работы `nsi-claim-term-kind`, вся реализация завершена; рекомендуется выполнить `task-close`.
