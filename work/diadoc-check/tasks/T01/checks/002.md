# Проверка 002 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 2
Дата: 2026-09-21
Состояние кода: commit 65c0c43a8; незакоммиченные изменения:
  `app/src/main/java/ru/fgk/ws/app/diadoc/service/DiadocProcessingSettingsService.java`,
  `app/src/test/java/ru/fgk/ws/app/it/DiadocProcessingSettingsIT.java`.
Окружение: схема `main_f_diadoc_check` в PostgreSQL (`docker-rvk-db-1`, localhost:5432),
  JVM / Gradle daemon, тестовый профиль `local` (JDBC URL к `main_f_diadoc_check`).
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | SQL-запрос `information_schema.columns` в `main_f_diadoc_check`: обе таблицы `diadoc_settings` и `diadoc_sign_settings` содержат четыре поля с `NOT NULL` и `defaultValueBoolean` (`true/false/false/false`), повторный прогон миграций в тестах успешен (`MARK_RAN`) |
| C2 | Подтверждён | Прогон `DiadocProcessingSettingsIT` (6 passing): `resolve(null)` → умолчания, `resolve(probeContractor)` → умолчания, переопределение при правке умолчаний, приоритет строки контрагента, `resolveForPacket` без кода и с кодом контрагента, а также `resolveForPacketReadWithoutContractorReference` (пакет из БД без fetch plan на `contractor`) |
| C3 | Подтверждён | Прогон `DiadocSettingsAdminRoleIT` (3 passing); доказательства браузерного сценария UI (Playwright CLI, порт 8082) актуальны из проверки [001](001.md), т.к. экраны, дескрипторы и роли не изменялись в итерации 2 |
| C4 | Подтверждён | Тесты `DiadocPacketFlowServiceTest` и `DiadocDocumentFlowServiceTest` зелёные (6 passing, 1.4s) |
| C5 | Подтверждён | Доказательства снимков DOM из проверки [001](001.md) актуальны: сырых `msg://` нет, лишний `s` после `<h4 text="Тип пакета">` убран; XML-дескрипторы в итерации 2 не менялись |

## Выполненные проверки

1. **Форматирование, сборка и аудит комментариев:**
   - `./gradlew :app:compileJava`: успешно (BUILD SUCCESSFUL).
   - `./gradlew spotlessCheckAll`: успешно (BUILD SUCCESSFUL).
   - Поиск артефактов работы и запрещённых шаблонов (`AGENTKIT_NOTE:`, `\b[TFQC]\d{1,}\b`, `specs/work/`, «решение пользователя», «проверяющ», «итераци») в git diff и изменённых файлах T01: совпадений 0.

2. **Автоматические тесты:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.DiadocProcessingSettingsIT" --tests "ru.fgk.ws.app.it.DiadocSettingsAdminRoleIT" --tests "ru.fgk.ws.app.diadoc.service.*" --tests "ru.fgk.ws.app.it.DiadocViolationCheckReplayIT"`: 156 passing (33.2s), BUILD SUCCESSFUL.
     - `DiadocProcessingSettingsIT`: 6 passing (включая проверку пакета, загруженного без fetch plan на `contractor`).
     - `DiadocSettingsAdminRoleIT`: 3 passing (проверка прав чтения и записи ролей `diadoc-settings-admin` и `ws-user`).
     - `ru.fgk.ws.app.diadoc.service.*`: 138 passing.
     - `DiadocViolationCheckReplayIT`: 9 passing (регрессионная проверка цепочки обработки пакетов T02 с вызовом `resolveForPacket`).
   - `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.service.DiadocPacketFlowServiceTest" --tests "ru.fgk.ws.app.diadoc.service.DiadocDocumentFlowServiceTest"`: 6 passing (1.4s), BUILD SUCCESSFUL.

3. **SQL-сверка колонок и типов (C1):**
   - Выполнен запрос к `information_schema.columns` в схеме `main_f_diadoc_check`:
     - `diadoc_settings`: `id` (bigint, NOT NULL), `show_related_documents` (boolean, default true, NOT NULL), `check_duplicate_tor_document` (boolean, default false, NOT NULL), `check_cross_packet_reference` (boolean, default false, NOT NULL), `check_missing_document` (boolean, default false, NOT NULL).
     - `diadoc_sign_settings`: те же 4 колонки с теми же типами и значениями по умолчанию.
   - Проверено отсутствие остаточных тестовых данных зонда в таблицах `diadoc_packet`, `diadoc_sign_settings`, `nsi_diadoc_contractor` (count = 0).

4. **Проверка исправления замечания F01.1:**
   - В `DiadocProcessingSettingsService` добавлен метод `resolveByContractorCode(@Nullable String code)` с безопасной обработкой `null`/blank и запросом по `e.contractor.code = :code`.
   - Метод `resolveForPacket(packet)` переведён на `resolveByContractorCode(packet.getContractorCode())`.
   - В `DiadocProcessingSettingsIT` успешно выполняется тест `resolveForPacketReadWithoutContractorReference`, загружающий пакет через `dataManager.load(DiadocPacket.class).id(...).one()` без fetch plan на ссылку `contractor`: возвращаются корректные настройки контрагента (а после удаления строки контрагента — настройки по умолчанию) без выброса `IllegalArgumentException`.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C5 подтверждены.

## Повторная проверка

Замечание [F01.1](fixes/F01.md#f011--resolveforpacket-падает-на-пакете-загруженном-без-contractor) подтверждённо устранено:
- `resolveForPacket` больше не вызывает ленивую загрузку ссылки `contractor`, разыменовываемой по неключевому полю `code`, и работает с любым загрузчиком пакета через `getContractorCode()`.
- Добавлен и протестирован метод `resolveByContractorCode(@Nullable String code)`.
- Специфический тест `resolveForPacketReadWithoutContractorReference` подтверждает исправление.
- Регрессия `DiadocViolationCheckReplayIT` пройдена успешно (9 passing).
- Доказательства для неизменённых в итерации 2 артефактов (UI-сценарии в браузере, отсутствие сырых `msg://`, ликвидация опечатки `s` в XML) актуальны из проверки [001](001.md), так как XML-дескрипторы, messages и роли после проверки 001 не модифицировались.

## Следующий шаг

Таск T01 и исправление F01 завершены (`done`). Следующий шаг — выполнение последующих тасков работы по плану [plan.md](../../plan.md).
