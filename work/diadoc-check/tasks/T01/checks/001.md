# Проверка 001 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-21
Состояние кода: commit 631acb2e0; изменённые файлы:
  `DiadocSignSettings.java`, `DiadocSignSettingsDetailView.java`,
  `diadoc-sign-settings-detail-view.xml`, `diadoc-sign-settings-list-view.xml`,
  `tbl-diadoc_sign_settings.xml`, `menu.xml`, `messages_ru.properties`;
  новые файлы: `DiadocSettings.java`, `DiadocProcessingSettings.java`,
  `DiadocProcessingSettingsService.java`, `DiadocSettingsView.java`,
  `diadoc-settings-view.xml`, `DiadocSettingsAdminRole.java`,
  `DiadocSettingsEditEnabled.java`, `tbl-diadoc_settings.xml`,
  `DiadocProcessingSettingsIT.java`, `DiadocSettingsAdminRoleIT.java`.
Окружение: схема `main_f_diadoc_check` в PostgreSQL (`docker-rvk-db-1`, localhost:5432),
  порт 8082 (`bootRun`), Playwright CLI (`t01`, `t02`).
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | SQL-запрос `information_schema.columns` в `main_f_diadoc_check`: обе таблицы `diadoc_settings` и `diadoc_sign_settings` содержат четыре поля с `NOT NULL` и `defaultValueBoolean` (`true/false/false/false`), повторный прогон миграций в тестах успешен (`MARK_RAN`) |
| C2 | Подтверждён | Прогон `DiadocProcessingSettingsIT`: 5 passing тестов проверяют `resolve(null)` → умолчания, `resolve(probeContractor)` → умолчания, переопределение при правке умолчаний, приоритет строки контрагента и поведение `resolveForPacket` |
| C3 | Подтверждён | Прогон `DiadocSettingsAdminRoleIT` (3 passing тестов) + сквозной браузерный проход Playwright CLI: `admin_diadoc_t01` видит оба пункта меню, заводит и переоткрывает настройки CDI, меняет умолчания; `user_plain_t01` (роль `ws-user`) пунктов меню не видит, прямой переход даёт `Access is denied by Jmix security subsystem` |
| C4 | Подтверждён | Тесты `DiadocPacketFlowServiceTest` и `DiadocDocumentFlowServiceTest` зелёные (6 passing) |
| C5 | Подтверждён | Снимки DOM Playwright CLI подтверждают корректные русские заголовки колонок, экранов и чекбоксов; сырых `msg://` нет; лишний символ `s` после `<h4 text="Тип пакета">` отсутствует |

## Выполненные проверки

1. **Форматирование и компиляция:**
   - `./gradlew spotlessCheckAll`: успешно (BUILD SUCCESSFUL).
   - `./gradlew :app:compileJava`: успешно (BUILD SUCCESSFUL).
   - Поиск артефактов работы и запрещённых шаблонов (`AGENTKIT_NOTE:`, `\b[TFQC]\d{1,}\b`, `specs/work/`, «решение пользователя», «проверяющ», «итераци») в коде и комментариях изменённых и новых файлов: совпадений 0.

2. **Автоматические тесты:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.DiadocProcessingSettingsIT" --tests "ru.fgk.ws.app.it.DiadocSettingsAdminRoleIT" --tests "ru.fgk.ws.app.diadoc.service.*"`: 137 passing (27.8s), BUILD SUCCESSFUL.
   - `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.service.DiadocPacketFlowServiceTest" --tests "ru.fgk.ws.app.diadoc.service.DiadocDocumentFlowServiceTest"`: 6 passing (1.5s), BUILD SUCCESSFUL.

3. **SQL-сверка колонок и типов (C1):**
   - Выполнен запрос к `information_schema.columns` в схеме `main_f_diadoc_check`:
     - `diadoc_settings`: `id` (bigint, NOT NULL, identity), `show_related_documents` (boolean, default true, NOT NULL), `check_duplicate_tor_document` (boolean, default false, NOT NULL), `check_cross_packet_reference` (boolean, default false, NOT NULL), `check_missing_document` (boolean, default false, NOT NULL).
     - `diadoc_sign_settings`: те же 4 колонки с теми же типами и значениями по умолчанию.

4. **Браузерная проверка UI (Playwright CLI, порт 8082):**
   - В схему `main_f_diadoc_check` добавлены тестовые пользователи `admin_diadoc_t01` (роли `diadoc-settings-admin`, `ui-minimal`) и `user_plain_t01` (роли `ws-user`, `ui-minimal`), а также контрагент «ООО «ЦДИ»» (ИНН 7708503727).
   - Запущен `bootRun` на порту 8082, сессия `t01`:
     - Вход под `admin_diadoc_t01`.
     - В меню «Диадок» отображаются два пункта: «Настройки Диадок по контрагентам» и «Настройки Диадок по умолчанию».
     - Переход на «Настройки Диадок по контрагентам» (`diadoc-sign-settings`): таблица содержит все 5 колонок с полными русскими названиями. Нажата кнопка «Создать».
     - В открывшемся диалоге: заголовок «Настройки Диадок по контрагенту», группа «Обработка пакетов», 4 чекбокса с начальными значениями из умолчаний (`true/false/false/false`), заголовок группы «Тип пакета» без опечаток (`s` удалён).
     - Выбран контрагент «ООО «ЦДИ»», включены все 4 флага проверок, сохранено по «OK».
     - Строка появилась в списке (`ООО «ЦДИ» / Да / Да / Да / Да`). По кнопке «Изменить» форма переоткрыта — все 4 чекбокса отмечены.
     - Переход на «Настройки Диадок по умолчанию» (`diadoc/settings`): открыт экран со singleton-настройками (кнопок создания второй строки нет). Изменено значение флага задвоения TorID, нажата кнопка «Сохранить» — запись в `diadoc_settings` обновлена (проверено в БД: 1 строка, `show_related_documents=t`, `check_duplicate_tor_document=t`).
   - Сессия `t02`:
     - Вход под `user_plain_t01` (только роль `ws-user`).
     - В меню «Диадок» пунктов настроек нет (видны только «Пакеты документов Диадок» и «Исходящие пакеты Диадок»).
     - Прямой переход по URL `http://localhost:8082/diadoc/settings` даёт отказ: `Could not navigate to 'diadoc/settings'`, `Reason: Access is denied by Jmix security subsystem`.
     - Прямой переход по URL `http://localhost:8082/diadoc-sign-settings` также даёт отказ: `Reason: Access is denied by Jmix security subsystem`.
   - Приложение `bootRun` остановлено, тестовые пользователи и данные очищены из БД.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии таска полностью подтверждены.

## Повторная проверка

Первая итерация проверки.

## Следующий шаг

Таск T01 завершён (`done`). Следующий шаг — параллельные/последующие таски по плану [plan.md](../../plan.md) (T03, T05, далее T02 и T04).
