# Проверка 002 — T06

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1 — реализация не передавалась заново; таск вернулся на проверку как
зависимый от [T07](../../T07/task.md), переподтверждается критерий C4
Дата: 2026-09-21
Состояние кода: rvk-ws — ветка `f/diadoc-check`, коммит `631acb2e0` плюс
незакоммиченные изменения работы (79 записей `git status --short`: 34 файла под
`app/src/main`, остальные — тесты и тестовые ресурсы; `core` и `sso-plagin`
чисты); rvk-diadoc — ветка `f/schf-supplement`, коммит `b27af3a`, дерево чистое
Окружение: PostgreSQL `docker-rvk-db-1` (`localhost:5432`, `root`/`root`), схема
приложения `main_f_diadoc_check` (`application-test-local.properties`,
`currentSchema=main_f_diadoc_check`), общая внешняя `ws_store` только на чтение;
rvk-diadoc — БД `db-rvk-diadoc` (`localhost:5436`). Изоляция по разделу плана
«Параллельность и тестирование»: T06 идёт последним и один, схема и оба Gradle
daemon заняты только этой проверкой, наборы прогнаны последовательно (сначала
rvk-ws, затем rvk-diadoc), приложения не поднимались, браузер не требовался.
Схема **не** пересоздавалась: прогон шёл поверх состояния, оставшегося от
проверки T07, — это строже пересоздания, потому что показывает, что тесты
убирают за собой в своей схеме
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | подтверждён ранее, доказательства в силе | Шаги 1-2 сценария воспроизведены проверкой [checks/001.md](001.md) (линки A и B с `supplement_document=t`, пакета C нет, 5 × `CROSS_PACKET_REFERENCE` у B, те же 18 строк в зеркале). Продуктовый код с 08:24 не менялся: `find app/src/main -newer checks/001.md` даёт только `frontend/generated/*` (пересоздаются сборкой), T07 менял исключительно `app/src/test`, дерево rvk-diadoc осталось на `b27af3a` |
| C2 | подтверждён ранее, доказательства в силе | То же основание: `process` B с включёнными флагами — `ok=false` с пятью русскими строками нарушений и строкой про подписанные документы, с выключенными — прежний ответ ([checks/001.md](001.md)) |
| C3 | подтверждён ранее, доказательства в силе | Браузерные шаги 5-6 под тремя пользователями со снимками `check-*.png` ([checks/001.md](001.md)); UI-код (`diadoc/view/**`, `pt/view/**`, `dr/view/**`, XML, `messages_ru.properties`) с тех пор не изменялся |
| C4 | **подтверждён** | rvk-ws: `./gradlew :app:test --rerun-tasks` — BUILD SUCCESSFUL, `1536 passing, 12 pending`; разбор `app/build/test-results/test/*.xml` (199 классов, все от 09:09 этого прогона): `tests=1548 failures=0 errors=0 skipped=12`. `./gradlew spotlessCheckAll --rerun-tasks` (13 задач выполнено, не из кеша) — BUILD SUCCESSFUL. rvk-diadoc: `./gradlew test --rerun-tasks` — BUILD SUCCESSFUL, по XML `tests=414 failures=0 errors=0 skipped=3` (73 класса); `./gradlew spotlessApply --rerun-tasks` — BUILD SUCCESSFUL, `git status --short` пуст |

## Выполненные проверки

### Полные наборы тестов и стиль (C4)

```bash
./gradlew :app:test --rerun-tasks           # exit 0, BUILD SUCCESSFUL, 1536 passing / 12 pending
./gradlew spotlessCheckAll --rerun-tasks    # exit 0, BUILD SUCCESSFUL
(cd /home/mindils/data/dev/fgk/rvk-diadoc && ./gradlew test --rerun-tasks)      # exit 0, BUILD SUCCESSFUL
(cd /home/mindils/data/dev/fgk/rvk-diadoc && ./gradlew spotlessApply --rerun-tasks \
   && git status --short)                   # BUILD SUCCESSFUL, вывод git пуст
```

Итоги прогонов сведены из JUnit-XML, а не с экрана Gradle:

| Проект | классов | тестов | падений | ошибок | пропущено |
|---|---|---|---|---|---|
| rvk-ws `app` | 199 | 1548 | 0 | 0 | 12 |
| rvk-diadoc | 73 | 414 | 0 | 0 | 3 |

`0 failing` достигнут буквально, в схеме рабочей копии — то есть в той
формулировке, которую [Q01](../../../questions/Q01.md) оставил без смягчения.
Прежний результат T06 (90 падений) снят не трактовкой критерия, а правкой
тестов в T07.

### Негатив: набор тестов не сократился

Список из 90 тестов, падавших на итерации 1
([app-test-failures-worktree.txt](../app-test-failures-worktree.txt)),
сопоставлен по паре «класс + имя теста» с полным перечнем `testcase` текущего
прогона: **не найдено в прогоне — 0**. Все 90 действительно выполнились и
прошли, а не были отключены, переименованы или помечены пропуском; общее число
выполненных тестов (1548) и число пропущенных (12) совпадают с итерацией 1.

Дополнительно к проверке T07 прогон сделан на схеме, оставшейся от предыдущей
сессии (без `DROP SCHEMA`): зелёный результат на непустой схеме показывает, что
исправленные тесты убирают за собой в своей схеме, а не зависят от чистого
старта.

### Механическая проверка привязки к схеме

`grep -rnE '"[^"]*\bmain\.[a-z_]+' app/src/test/java app/src/test/resources` —
14 строк, все из согласованного списка исключений T07: `DiadocSqlValidatorTest`
(1), `DiadocPacketDocumentsReadTest` (1), `LocalTestDataSource` (4),
`CaseOneUrlsTest` (8). Новых жёстких квалификаторов схемы не появилось.

### Следы работы в изменённых файлах

Шаблоны раздела протокола «Комментарии в коде» прогнаны по всем 79 изменённым и
новым файлам rvk-ws (кроме `frontend/generated`) и по дифу `main...HEAD`
rvk-diadoc (21 файл):

- `AGENTKIT_NOTE:` — совпадений нет ни в одном репозитории;
- `specs/work/` — нет;
- «решение пользователя», «проверяющ» — нет;
- «итераци» — одно совпадение,
  `DrOperRepairTr2ParserServiceIT.java:99` («в следующих итерациях разработки
  канала»), строка существует в коммите `631acb2e0` и к передачам этой работы
  не относится;
- `\b[TFQC]\d{1,2}\b` — в **добавленных** строках диффа обоих проектов и во всех
  новых файлах совпадений нет (`git diff -U0 | grep '^+' | grep -E …` — пусто).
  Срабатывания вне диффа: `LocalDateTime.parse(s + "T00:00:00")`
  (`SyncPacketUpsertRepository.java:352`) и `@DisplayName("C1: …")` в
  `TechClaimSecurityIT`, `WorkCalendarReferenceDataIT`, `NsiClaimTermEditRoleIT`,
  `NsiClaimTermNormViewsUiTest` — это строки базового коммита, унаследованные от
  прежней работы; T07 их не создавал и в этих файлах правил только SQL. Как
  legacy они остаются по правилу проекта, но при закрытии работы их стоит
  снести `task-clean` вместе с остальным — отмечено ниже.

### Общие ресурсы после проверки

Приложения не поднимались, порты 8081 и 8082 свободны. Временные данные
предыдущей проверки в общей `ws_store` действительно убраны: по
`repair_uid='2ad11626-0000-4000-8000-000000000026'` в `ora_v_wag_rem_new` и
`dr_wag_oper_repair_contract` — 0 строк. Схема `main_f_diadoc_check` содержит
только то, что оставили тесты; контрагент CDI, строка `diadoc_sign_settings` и
пользователи `dsadmin`/`wsuser` в ней не сохранились (схема пересоздавалась
проверкой T07) — для повторения сквозного сценария подготовку из
[result.md](../result.md) нужно выполнить заново.

## Ошибки и непроверенное

Подтверждённых ошибок нет; файл исправления не заводится.

Непроверенного в рамках этой передачи нет: C4 переподтверждён прямыми прогонами,
C1-C3 опираются на доказательства [checks/001.md](001.md), актуальные потому, что
продуктовый код и код rvk-diadoc с момента того отчёта не менялись (проверено
`find … -newer` по `app/src/main`, составом `git status` и коммитом `b27af3a` с
чистым деревом). Изменения T07 лежат целиком в `app/src/test` и поведение
приложения не меняют.

Замечания без статуса дефекта:

- `@DisplayName("C1: …")`, `@DisplayName("C4: …")` в четырёх тестовых классах —
  следы **прежней** работы, попавшие в коммит `631acb2e0`. К T06 претензии нет,
  но при `task-close` стоит прогнать `task-clean` по этим именам, иначе шаблон
  протокола будет срабатывать на них и дальше.
- `./gradlew :core:core:test` не запускается (`Failed to load JUnit Platform`) —
  состояние сборки модуля `core`, зафиксировано проверкой
  [T07/checks/001.md](../../T07/checks/001.md); `core` этой работой не менялся,
  в критерии C4 входят наборы `app` и rvk-diadoc.

## Повторная проверка

Замечаний к исполнителю по итогу [checks/001.md](001.md) не было: единственный
открытый пункт — критерий C4, упиравшийся в решение по [Q01](../../../questions/Q01.md).
Решение принято (чинить привязку к схеме `main` внутри работы, критерий не
смягчать), выполнено таском [T07](../../T07/task.md) и здесь переподтверждено
независимым прогоном обоих наборов: rvk-ws — 1548 тестов, 0 падений (было 90);
rvk-diadoc — 414 тестов, 0 падений. Доказательства C1-C3 из
[checks/001.md](001.md) приняты для неизменённых частей на основании неизменности
продуктового кода обоих проектов.

## Следующий шаг

Все таски работы закрыты (`done`), открытых вопросов и исправлений нет.
Следующий шаг — `task-close` с `specs/work/diadoc-check/plan.md`: собрать
`summary.md` и прогнать `task-clean`, в том числе по унаследованным
`@DisplayName("C…")` в тестах. Закрытие работы делает пользователь или
оркестратор, проверяющий её не закрывает.
