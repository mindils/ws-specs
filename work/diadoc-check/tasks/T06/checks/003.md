# Проверка 003 — T06

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1 — новой передачи реализации не было; таск вернулся на проверку как
зависимый от исправлений [T01/F01](../../T01/fixes/F01.md) и
[T04/F01](../../T04/fixes/F01.md), переподтверждался критерий C3
Дата: 2026-09-21 11:10
Состояние кода: rvk-ws — ветка `f/diadoc-check`, коммит `65c0c43a8` («step 1»),
рабочее дерево чистое; это ровно то содержимое, что проверка
[002](002.md) видела незакоммиченным поверх `631acb2e0` (диф коммита — 79 файлов,
из них 34 под `app/src/main`, совпадает с составом `git status --short` в 002).
rvk-diadoc — ветка `f/schf-supplement`, коммит `b27af3a`, дерево чистое
Окружение: проверка статическая по коду и документам работы; общие ресурсы не
занимались — БД `main_f_diadoc_check`, порты 8081/8082, браузер и оба Gradle
daemon свободны. Порядок по разделу плана «Параллельность и тестирование»
соблюдён (T06 идёт один и последним), но живое окружение не поднималось: цель
проверки отсутствует в коде (см. ниже)
Итог: blocked

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | подтверждён ранее, доказательства в силе | Шаги 1-2 воспроизведены проверкой [001](001.md); продуктовый код обоих проектов с тех пор не менялся — содержимое рабочего дерева rvk-ws идентично проверенному в [002](002.md) (то же множество файлов, теперь в коммите `65c0c43a8`), rvk-diadoc остался на `b27af3a` с чистым деревом |
| C2 | подтверждён ранее, доказательства в силе | То же основание: `process` B с включёнными флагами — `ok=false` с русскими текстами, с выключенными — прежний ответ ([001](001.md)) |
| C3 | **не проверен** | Переподтверждать нечего: обе правки, ради которых C3 был переоткрыт, в коде отсутствуют (разбор ниже). Карточка пакета по-прежнему в редакции, проверенной в [001](001.md) |
| C4 | подтверждён ранее, доказательства в силе | Прямые прогоны в [002](002.md): rvk-ws `:app:test` — 1548 тестов, 0 падений, 12 пропущенных; `spotlessCheckAll` — успешно; rvk-diadoc — 414 тестов, 0 падений, `spotlessApply` без диффа. Прогоны сделаны на том же содержимом дерева |

## Выполненные проверки

### Состояние исправлений, от которых зависит C3

Таск ждал `done` у [T01/F01](../../T01/fixes/F01.md) и
[T04/F01](../../T04/fixes/F01.md). Оба исправления в статусе `todo`, оба таска —
`in_progress`; в коде правок нет:

```bash
grep -rn "resolveByContractorCode" app/src            # совпадений нет
grep -rn "showRelatedButton\|showRelatedCheckbox" app/src
```

| Ожидалось после исправления | Фактически в `65c0c43a8` |
|---|---|
| `DiadocProcessingSettingsService.resolveByContractorCode(String)`, `resolveForPacket` по `packet.getContractorCode()` | метода нет; `DiadocProcessingSettingsService.java:68-70` — `resolveForPacket(packet)` → `resolve(packet.getContractor())`, то есть ленивая подгрузка по неключевой колонке, на которой падает F01.1 T01 |
| `refreshEditedEntity()` перезагружает пакет с fetch plan контейнера | `DiadocPacketDetailView.java:1878-1882` — `dataManager.load(DiadocPacket.class).id(...).one()` без fetch plan, как в описании F01.1 T04 |
| Кнопка `showRelatedButton` вместо чекбокса, ключи `showRelatedButton.show/hide` | остались `<checkbox id="showRelatedCheckbox">` (`diadoc-packet-detail-view.xml:79-80`), `@ViewComponent JmixCheckbox showRelatedCheckbox` и `@Subscribe("showRelatedCheckbox")` (`DiadocPacketDetailView.java:246, 602, 621, 1496-1497`), ключ `showRelatedCheckbox.label` (`messages_ru.properties:5659`) |

Поэтому сценарий шага 5 в новой редакции C3 («кнопка переключает состав»,
«Обработать повторно» без «Ошибка обработки») проверять не на чем: проверка
под ролью `diadoc-settings-admin` без полного доступа заведомо воспроизвела бы
уже описанные в F01 дефекты, а не подтвердила критерий. Приложения и браузер не
поднимались намеренно — это не сокращение объёма, а отсутствие объекта проверки.

### Неизменность кода, на которой держатся C1, C2, C4

```bash
git log --oneline -1                     # 65c0c43a8 step 1
git status --short                       # пусто
git show --name-only --format= 65c0c43a8 | wc -l        # 79
git show --name-only --format= 65c0c43a8 | grep -c '^app/src/main'   # 34
(cd /home/mindils/data/dev/fgk/rvk-diadoc && git log --oneline -1 && git status --short)
                                         # b27af3a, вывод пуст
```

Состав коммита совпадает с составом незакоммиченных изменений, на которых
[002](002.md) прогнала оба полных набора: содержимое дерева то же, изменилось
только то, что оно зафиксировано в Git. Перепрогон наборов в этой передаче не
дал бы новых сведений, поэтому общие ресурсы не занимались.

### Следы работы в изменённых файлах

Шаблоны раздела протокола «Комментарии в коде» прогнаны по добавленным строкам
коммита `65c0c43a8` (`git diff -U0 631acb2e0 65c0c43a8 -- app core sso-plagin |
grep '^+' | grep -E 'AGENTKIT_NOTE:|specs/work/|решение пользователя|проверяющ|
итераци|\b[TFQC][0-9]{1,2}\b'`) — совпадений нет. Срабатывания вне диффа
(`"T00:00:00"` в `SyncPacketUpsertRepository`, `@DisplayName("C1: …")` в четырёх
тестовых классах базового коммита) разобраны в [002](002.md) и остаются
legacy-замечанием к `task-clean`.

## Ошибки и непроверенное

Подтверждённых ошибок T06 нет — новый файл исправления не заводится.
Дефекты, из-за которых C3 переоткрыт, уже оформлены и ждут исполнителя:
[T01/F01](../../T01/fixes/F01.md) (F01.1) и [T04/F01](../../T04/fixes/F01.md)
(F01.1, F01.2).

Не проверено:

- C3 целиком в новой редакции — шаг 5 под `ws-user` и под
  `diadoc-settings-admin` без полного доступа (кнопка «Показать/Скрыть связанные
  документы», «Обработать повторно» с карточки B) и шаг 6. Прежние
  доказательства [001](001.md) относятся к редакции с чекбоксом и роли `admin`
  с полным доступом, то есть к состоянию до Q02, и на новую формулировку не
  переносятся.

Условие продолжения: `done` у [T01/F01](../../T01/fixes/F01.md) и
[T04/F01](../../T04/fixes/F01.md) — после этого повторить шаги 5-6 сценария из
раздела «Для независимой проверки» в [result.md](../result.md) под тремя
пользователями (`wsuser` только с `ws-user`, пользователь с `ws-user` +
`diadoc-settings-admin` без `system-full-access`, `admin`), с поднятыми
rvk-diadoc (8081) и rvk-ws (8082). C1, C2 и C4 переподтверждать не нужно, если
исправления не тронут `diadoc/service/**` за пределами
`DiadocProcessingSettingsService` и прогонят свои наборы; правка
`DiadocProcessingSettingsService` и `DiadocPacketDetailView` затрагивает
`DiadocViolationCheckReplayIT` и UI-тесты, поэтому C4 придётся переподтвердить
полным `:app:test`.

## Повторная проверка

По итогу [002](002.md) замечаний к исполнителю T06 не было. Эта передача должна
была закрыть единственный оставшийся пункт — C3 после исправлений по
[Q02](../../../questions/Q02.md), — но исправления ещё не реализованы, поэтому
подтверждать нечего. Доказательства C1, C2 и C4 из [001](001.md) и [002](002.md)
приняты для неизменённых частей на основании совпадения содержимого дерева
rvk-ws с проверенным в 002 и неизменности rvk-diadoc (`b27af3a`, дерево чистое).

## Следующий шаг

Исполнитель: `task-execute` с
[tasks/T01/fixes/F01.md](../../T01/fixes/F01.md), затем `task-execute` с
[tasks/T04/fixes/F01.md](../../T04/fixes/F01.md) (код можно писать параллельно,
тесты и `bootRun` — по очереди, сначала T01). После `done` обоих — `task-verify`
с [tasks/T06/task.md](../task.md) по критерию C3.
