# T06 — Сквозная проверка rvk-diadoc ↔ rvk-ws на данных -26 и полный регресс

Статус: done — проверка 002
План: [plan.md](../../plan.md)
Зависимости: T02, T04, T05, [T07](../T07/task.md)
Сложность: medium — код только для исправления найденного; основная работа —
запуск двух приложений и сценарий
Сложность проверки: hard — живое окружение из двух приложений, двух БД и
браузера, полный набор тестов обоих проектов
Актуальная проверка: [checks/002.md](checks/002.md)

## Коротко

Все части работы собираются вместе: rvk-diadoc с изменённой привязкой СЧФ
и rvk-ws с настройками, новыми нарушениями и экранами. На тестовых данных
-26 проверяется путь «обработка в rvk-diadoc → sync в rvk-ws → process →
карточка и списки». Найденные расхождения исправляются здесь же (в своей
области) или оформляются исправлениями к таскам.

## Результат и контекст

Окружение: rvk-diadoc — `./gradlew bootRun` в
`/home/mindils/data/dev/fgk/rvk-diadoc` (профиль `dev`, локальный
`application-dev.properties` → БД `localhost:5436`, порт 8081); данные -26
попадают в БД 5436 прогоном любого теста rvk-diadoc (`test-026`,
`runOnChange`) либо ручным выполнением SQL в схему `public`. rvk-ws —
`./gradlew :app:bootRun` в worktree (порт 8082, `rvkdiadoc.baseUrl=
http://localhost:8081/` из dev-профиля), `/actuator/health` = UP.
Контрагент CDI (ИНН 7708503727, КПП 770845098) должен быть в
`nsi_DiadocContractor` схемы `main_f_diadoc_check` (проверить, при
отсутствии — завести).

Сценарий:

1. rvk-diadoc: `POST /api/v2/documents/process` `{"messageId": ...}` для
   C (сообщение `b17c632e-9013-4db3-8c3b-868a9826b062`;
   `fe79c311-9419-4cad-8473-bad0e713d818` — это `entity_id` самой СЧФ), A
   (`14bf2bcd-9a45-483d-a0b5-f76f001d9138`), B
   (`be5aa61d-e5a0-4448-b5cb-9417a6d54c05`). Проверить в БД 5436 линки A и
   B (СЧФ `supplement_document=true`), tombstone C, `violations` у B
   (`CROSS_PACKET_REFERENCE` по пяти TorID из A).
2. rvk-ws: `POST /api/diadoc/syncPacket?messageId=<A>` и `<B>`; в
   `diadoc_packet_doc_link` схемы worktree — те же линки с
   `supplement_document`; в `diadoc_packet.violations` у B — массив.
3. Настройки: под `diadoc-settings-admin` создать строку CDI с
   `showRelatedDocuments=true` и тремя проверками `true`.
4. `GET /api/diadoc/processPacket?messageId=<B>` → `ok=false`, в
   `message` русские тексты `CROSS_PACKET_REFERENCE` (5 строк) и, если
   документы A подписаны/отклонены (`signature_status` в зеркале),
   «подписанные/отклонённые в системе»; `GET …?messageId=<A>` — без новых
   нарушений (пакет-владелец). Выключить проверки — process B без этих
   строк.
5. Карточки A и B: СЧФ с иконкой дополнения в обеих; под `ws-user` при
   `showRelatedDocuments=false` в B скрыты документы из A, СЧФ остаётся.
6. Список ВУ-23/Рем.Дет: фильтр «Нарушение» показывает пакет B (тип ТР-2
   — на своём списке, если есть; иначе проверить фильтр на имеющихся
   данных).
7. Полный регресс: rvk-ws `./gradlew :app:test` (`0 failing`),
   `spotlessCheckAll`; rvk-diadoc `./gradlew test`, `spotlessApply` без
   диффа.

## Область и изоляция

Код — только исправления найденных расхождений в областях завершённых
тасков (с пометкой в `result.md`, какие таски затронуты) и, при
необходимости, дополнение replay-фикстур (`app/src/test/resources/diadoc/
replay/packet_doc_link.csv` — линк-дополнение) с соответствующим
ожиданием в `PacketReplayIT`. Ресурсы: обе БД, порты 8081 и 8082,
браузер, оба Gradle daemon — таск выполняется последним, один.

## Реализация

Пошагово по сценарию; каждый шаг фиксировать в `result.md` командами и
наблюдаемым результатом (JSON ответа, SQL-выборки, скриншоты в
`tasks/T06/` не обязательны, но пути к ним — если есть). Приложения
гасить после проверки. Расхождение, требующее правки вне области —
`fixes/Fxx.md` к соответствующему таску, а не тихая правка.

## Критерии приёмки

- C1: Шаги 1-2 дают ожидаемое состояние линков и `violations` в обеих БД.
- C2: Шаг 4: `ok=false` с русскими текстами при включённых проверках,
  прежний ответ при выключенных.
- C3: Шаги 5-6 подтверждены в браузере (или `render not browser-verified`
  с причиной).
- C4: Полные наборы тестов обоих проектов зелёные, spotless чистый.

## Самопроверка исполнителя

Сценарий шаги 1-4 и полный `:app:test`; браузерные шаги можно оставить
проверяющему, указав это в `result.md`.

## Независимая проверка

Повторить сценарий целиком по `result.md`, включая браузер под двумя
ролями; полный `:app:test` и `./gradlew test` rvk-diadoc. Негатив:
process пакета контрагента без строки настроек — три проверки зеркала
(`DUPLICATE_TOR_DOCUMENT`, `CROSS_PACKET_REFERENCE`, `MISSING_DOCUMENT`)
новых нарушений не дают, они выключены умолчаниями; проверка связанных
документов при этом работает, потому что `showRelatedDocuments` в
умолчаниях `true` — это согласованный контракт, а не дефект.

## Прогресс и продолжение

- [x] Окружение поднято, данные -26 обработаны
- [x] Sync и process в rvk-ws
- [x] Экраны
- [x] Полный регресс
- [x] Передать результат на независимую проверку.
- [x] Независимая проверка 1: критерии C1-C3 подтверждены, C4 упирается в
      [Q01](../../questions/Q01.md) — [checks/001.md](checks/001.md).
- [x] [Q01](../../questions/Q01.md) разобран: гейт не смягчается, привязка
      тестов к схеме `main` чинится таском [T07](../T07/task.md).
- [x] [T07](../T07/task.md) выполнен и проверен
      ([T07/checks/001.md](../T07/checks/001.md)): жёсткая схема `main` из
      SQL тестов убрана, полный `./gradlew :app:test` на пересозданной
      схеме — 1548 тестов, 0 падений, 12 пропущенных.
- [x] Повторная независимая проверка по критерию C4: `:app:test` — 1548
      тестов, 0 падений, 12 пропущенных; `spotlessCheckAll` — успешно;
      rvk-diadoc — 414 тестов, 0 падений, `spotlessApply` без диффа. Итог
      `pass` — [checks/002.md](checks/002.md).

Ближайший шаг: таск закрыт, все критерии подтверждены; по работе остаётся
`task-close` с [plan.md](../../plan.md). Препятствия: нет.
