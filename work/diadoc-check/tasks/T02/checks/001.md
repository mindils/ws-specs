# Проверка 001 — T02

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-21 06:30
Состояние кода: commit 631acb2e0145997cff568ad4c040254180dd3f69 + рабочая копия (T01–T03)
Окружение: Docker Compose (PostgreSQL localhost:5432, схема main_f_diadoc_check), порт 8082, Gradle daemon
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | `DiadocViolationCheckReplayIT#process_allChecksEnabled_writesMirrorViolations` подтверждает запись трёх сообщений нарушений в `pack_checked_text`, код `VIOLATION`, `ok=false`; `process_checksDisabled_keepsBaselineResult` подтверждает совпадение с baseline `PacketReplayIT` при выключенных флагах |
| C2 | Подтверждён | `DiadocViolationCheckReplayIT#process_relatedDocuments_reportedByState` проверяет «в процессе подписания», «отклонённые» (`8015`), «подписанные» (`8002`), исключение дополнений и несвязанных; `process_relatedDocumentsDisabled_skipsCheck` подтверждает пропуск проверки при `showRelatedDocuments=false` |
| C3 | Подтверждён | `DiadocViolationCheckReplayIT#process_runTwice_doesNotDuplicateMessages` (ТР-2) и `partsRepair_violationsAreWrittenToErrors` (Рем.Дет) подтверждают отсутствие дублей строк при повторной обработке |
| C4 | Подтверждён | `DiadocViolationCheckReplayIT#tr1_checkResultIsReadAndAppended` подтверждает чтение и запись результата контроля для ТР-1 из `dr_diadoc_oper_repair_tr1` |
| C5 | Подтверждён | `DiadocViolationCheckReplayIT#vu23_longViolationText_isStoredCompletely` подтверждает сохранение текста контроля ВУ-23 длиной > 255 символов в поле `CLOB` |
| C6 | Подтверждён | Набор тестов `DiadocViolationCheckReplayIT`, `PacketReplayIT`, `RemDetPacketReplayIT`, `ru.fgk.ws.app.diadoc.service.*` (включая `PacketParserServiceTest`, `Vu23PacketParserServiceTest`, `DiadocPacketViolationCheckServiceTest`): 157 passing, 0 failing |

## Выполненные проверки

1. **Форматирование и статический анализ:**
   - `./gradlew spotlessCheckAll` — `BUILD SUCCESSFUL` (13 up-to-date, код соответствует Google Java Style).
   - `./gradlew :app:compileJava :app:compileTestJava` — сборка и компиляция тестов успешны.
   - Поиск следов работы по разделу «Комментарии в коде» (`AGENTKIT_NOTE`, `[TFQC]\d+`, `specs/work`, «решение пользователя», «проверяющ», «итераци») в изменённых и новых файлах — совпадений нет.

2. **SQL-сверка типа колонки:**
   - Запрос к `information_schema.columns`:
     ```sql
     select column_name, data_type from information_schema.columns
      where table_schema = 'main_f_diadoc_check'
        and table_name = 'dr_diadoc_oper_repair_packet'
        and column_name = 'pack_checked_text';
     ```
     Результат: `pack_checked_text` имеет тип `text` (соответствует Liquibase changeSet 21 `modifyDataType` к `CLOB`).

3. **Негативные сценарии:**
   - Юнит-тест `DiadocPacketViolationCheckServiceTest#parseViolations_notAnArray_fails`: битый JSON (объект вместо массива) выбрасывает исключение с фиксацией контракта.
   - Интеграционный тест `DiadocViolationCheckReplayIT#process_brokenViolationsJson_marksCheckFailed`: при битом JSON в `violations` процесс не падает, а в отметку контроля записывается `violation.checkFailed` («Проверка нарушений не выполнена: ...»).

4. **Тестовые наборы:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.DiadocViolationCheckReplayIT" --tests "ru.fgk.ws.app.it.PacketReplayIT" --tests "ru.fgk.ws.app.it.RemDetPacketReplayIT" --tests "ru.fgk.ws.app.diadoc.service.*"`: 157 passing, 0 failing.
   - `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.controller.*"`: 11 passing, 0 failing (`PacketParserRestControllerTest` и `SavePacketRestControllerTest`).
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.DiadocProcessingSettingsIT" --tests "ru.fgk.ws.app.it.DiadocSettingsAdminRoleIT"`: 8 passing, 0 failing (регресс T01).

5. **Регресс API через контрольный запуск `bootRun`:**
   - Приложение запущено в фоне (`./gradlew :app:bootRun`), поднято на порту 8082.
   - Выполнен запрос `curl -i -s "http://localhost:8082/api/diadoc/processPacket?typeID=tr2&messageId=89503520-4add-45ea-9d18-0b4e5cd6cbb4"`.
   - Получен HTTP 200 с телом:
     `{"errorType":"REPAIR_NOT_FOUND","message":"Ремонт не найден","messageId":"89503520-4add-45ea-9d18-0b4e5cd6cbb4","ok":false,"packetType":"tr2"}`
   - Фоновый процесс остановлен, порт 8082 освобождён.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C6 подтверждены.
В рамках T02 views не создавались и не изменялись (интерфейсные элементы входят в T04), поэтому браузерный render-проход к данному таску неприменим.

## Повторная проверка

Первая проверка таска T02 (итерация 1). Замечаний нет.

## Следующий шаг

Таск T02 выполнен и переведён в статус `done`. Следующий шаг: реализация UI-таска [T04](../../tasks/T04/task.md) либо сквозная интеграция [T06](../../tasks/T06/task.md) после завершения T04 и T05.
