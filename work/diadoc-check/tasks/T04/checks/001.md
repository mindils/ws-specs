# Проверка 001 — T04

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-21 07:35
Состояние кода: commit 631acb2e0145997cff568ad4c040254180dd3f69 + рабочая копия (T01–T04)
Окружение: Docker Compose (PostgreSQL localhost:5432, схема main_f_diadoc_check), порт 8082, Gradle daemon, Playwright CLI (Chromium)
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Браузерный прогон (`playwright-cli`): под `ws-user` чекбокс «Показать связанные документы» скрыт; при `showRelatedDocuments=false` связанные документы скрыты, дополнения отображаются с иконкой `vaadin:paperclip` и всплывающей подсказкой; при `true` — все связанные документы видны. Под `diadoc-settings-admin` чекбокс виден с предустановленным значением и переключается. Юнит-тест `DiadocPacketDocumentFilterTest` подтверждает логику отбора |
| C2 | Подтверждён | Браузерный прогон и UI-тесты: на обоих экранах (`pt-repair-packets` и `dr-diadoc-oper-repair-packet-vu23s`) фильтр «Результат контроля» корректно ограничивает набор строк по кодам нарушений и значению «Не проверялся» (`is null`). На `pt-repair-packets` фильтр сохраняется в `ViewSettings` между открытиями экрана |
| C3 | Подтверждён | Браузерный прогон на `dr-diadoc-oper-repair-packet-vu23s`: кнопка «Текст ошибки» активна для строки с нарушением и непустым текстом, открывает диалог и отображает содержимое `packCheckedText`. Для строк без текста ошибки/нарушений кнопка заблокирована |
| C4 | Подтверждён | Анализ XML дескрипторов и проверка по `messages_ru.properties` подтвердили отсутствие нерезолвящихся `msg://`. Тестовый набор (`DiadocPacketListViewTest`, `PtRepairPacketListViewFilterTest`, `DrDiadocOperRepairPacketVu23ListViewTest`, `DiadocPacketDocumentFilterTest`) зелёный (16 passing, 0 failing) |

## Выполненные проверки

1. **Форматирование, компиляция и статический анализ:**
   - `./gradlew spotlessCheckAll` — `BUILD SUCCESSFUL` (13 up-to-date, код соответствует правилам проекта).
   - `./gradlew :app:compileJava :app:compileTestJava` — сборка и компиляция тестов успешны.
   - Проверка комментариев на запрещённые артефакты рабочего процесса (`AGENTKIT_NOTE`, `\b[TFQC]\d{1,}\b`, `specs/work/`, «решение пользователя», «проверяющ», «итераци») — 0 совпадений.
   - Проверка `msg://` ключей: все ссылки в `diadoc-packet-detail-view.xml`, `pt-repair-packet-list-view.xml` и `dr-diadoc-oper-repair-packet-vu23-list-view.xml` проверены по словарю `messages_ru.properties` — все ключи присутствуют.

2. **Автоматизированные тесты:**
   - Выполнен запуск тестов UI и слоёв представления:
     `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.view.*" --tests "ru.fgk.ws.app.pt.view.*" --tests "ru.fgk.ws.app.dr.view.*"`
     Результат: 16 passing, 0 failing.
   - Проверены тестовые классы:
     - `ru.fgk.ws.app.diadoc.view.diadocpacket.DiadocPacketDocumentFilterTest` (проверка логики фильтрации own/related/supplement);
     - `ru.fgk.ws.app.diadoc.view.diadocpacket.DiadocPacketListViewTest`;
     - `ru.fgk.ws.app.pt.view.ptrepairpacket.PtRepairPacketListViewFilterTest`;
     - `ru.fgk.ws.app.dr.view.drdiadocoperrepairpacket.DrDiadocOperRepairPacketVu23ListViewTest`.

3. **Браузерная проверка карточки пакета Диадок (C1):**
   - Проверка под `admin_diadoc_t04` (роли `ui-minimal`, `ws-user`, `diadoc-settings-admin`):
     - Открыта карточка пакета `30a02e6d-9065-4176-bc5d-c05fe658bd34`.
     - Чекбокс «Показать связанные документы» отображается в интерфейсе и включен по умолчанию.
     - В таблице документов отображаются собственные документы (9), связанные (2) и дополнение (1).
     - Документ-дополнение `1126585a-8a5b-4f0e-58f4-1d753065b909` отображает иконку скрепки `vaadin:paperclip` и всплывающую подсказку `Дополнение к ФПУ-26: 5fac2c79-06dd-45da-9556-727cf44e6cd6`.
     - При снятии чекбокса скрываются только обычные связанные документы (состав уменьшается до 10 документов: 9 собственных + 1 дополнение).
     - При возврате чекбокса все документы восстанавливаются (12 документов).
   - Проверка под `user_plain_t04` (роли `ui-minimal`, `ws-user` без `diadoc-settings-admin`):
     - При `showRelatedDocuments=false` открыта карточка пакета.
     - Чекбокс «Показать связанные документы» скрыт (`setVisible(false)`).
     - В таблице документов отображаются только собственные документы и дополнение со скрепкой (всего 10 строк); обычные связанные документы скрыты.

4. **Браузерная проверка фильтра «Результат контроля» (C2):**
   - Экран «Пакеты по ремонту деталей» (`/pt-repair-packets`):
     - Выбор «Нарушение» сузил список до 4 строк с кодом нарушения.
     - Выбор «Не проверялся» сузил список до 1 строки с `packCheckedCode is null`.
     - Сброс фильтра вернул весь набор из 6 строк.
     - Сохранение состояния: выбран фильтр «Не проверялся», выполнен переход на `/` и обратно на `/pt-repair-packets` — состояние фильтра восстановилось из `ViewSettings`.
   - Экран «Пакеты ВУ-23» (`/dr-diadoc-oper-repair-packet-vu23s`):
     - Выбор «Нарушение» отобразил только тестовый пакет с кодом `VIOLATION`.
     - Выбор «Не проверялся» отобразил тестовый пакет с `packCheckedCode is null`.
     - Сброс фильтра вернул все записи.

5. **Браузерная проверка кнопки «Текст ошибки» на ВУ-23 (C3):**
   - Для строки без нарушений кнопка «Текст ошибки» неактивна (`disabled`).
   - При выборе строки с кодом «Нарушение» и текстом ошибки кнопка становится активной.
   - При нажатии на кнопку открывается диалоговое окно «Текст ошибки» с текстом «Обнаружено нарушение в пакете ВУ-23».

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C4 подтверждены автоматизированными тестами и браузерным прогоном.
После завершения проверки все временные данные в тестовой базе данных очищены, процессы остановлены.

## Повторная проверка

Не требуется, проверка пройдена с первой итерации.
