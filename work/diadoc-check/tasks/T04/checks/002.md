# Проверка 002 — T04

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 2
Дата: 2026-09-21 11:53
Состояние кода: commit 65c0c43a8 + рабочая копия (изменения T04/F01)
Окружение: Docker Compose (PostgreSQL localhost:5432, схема main_f_diadoc_check), порт 8082, Gradle daemon, Playwright CLI (Chromium)
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Проверено в браузере (`playwright-cli`) под ролями `ws-user` (`t04_plain`), `diadoc-settings-admin` (`t04_sadm`) и `admin`: под обычным пользователем кнопка скрыта, состав соответствует настройке `showRelatedDocuments` (при `false` 11 строк, скрыты 2 связанных, 1 дополнение отображается со значком `vaadin:paperclip` и tooltip «Дополнение к ФПУ-26: 5fac2c79-06dd-45da-9556-727cf44e6cd6»; при `true` — 13 строк). Под администратором настроек кнопка `showRelatedButton` доступна в read-only карточке, переключает состав 13 ↔ 11, меняет текст и иконку (`vaadin:unlink` / `vaadin:link`). Нажатие «Обработать повторно» не вызывает падения по `parameter entityId` при перезагрузке пакета |
| C2 | Подтверждён | На обоих экранах (`pt-repair-packets` и `dr-diadoc-oper-repair-packet-vu23s`) фильтр «Результат контроля» содержит 5 значений, корректно фильтрует строки по значениям `packCheckedCode` и значению «Не проверялся» (`is null`). На `pt-repair-packets` состояние фильтра сохраняется в `ViewSettings` между переходами. Подтверждено UI-тестами `PtRepairPacketListViewFilterTest`, `DrDiadocOperRepairPacketVu23ListViewTest` и браузерным прогоном |
| C3 | Подтверждён | На ВУ-23 кнопка «Текст ошибки» (`showErrorBtn`) заблокирована без выбора строки / при отсутствии нарушений, активна при выборе строки с нарушением и непустым текстом, открывает диалог с текстом `packCheckedText`. Подтверждено тестом `DrDiadocOperRepairPacketVu23ListViewTest.showErrorTextIsEnabledOnlyForRowWithCheckText()` и браузерным инспектированием экрана |
| C4 | Подтверждён | Форматирование по Spotless проверено (`spotlessCheckAll` — `BUILD SUCCESSFUL`). Компиляция `compileJava` и `compileTestJava` успешна. Все ссылки `msg://` в XML проверены по `messages_ru.properties` (нерезолвящихся ключей нет). Тестовый набор (`ru.fgk.ws.app.diadoc.view.*`, `ru.fgk.ws.app.pt.view.*`, `ru.fgk.ws.app.dr.view.*`, `ru.fgk.ws.app.it.DiadocProcessingSettingsIT`) зелёный (22 passing, 1 pending, 0 failing) |

## Выполненные проверки

1. **Форматирование, компиляция и статический анализ:**
   - `./gradlew spotlessCheckAll` — `BUILD SUCCESSFUL` (13 up-to-date, код соответствует правилам проекта).
   - `./gradlew :app:compileJava :app:compileTestJava` — `BUILD SUCCESSFUL` без ошибок и предупреждений.
   - Проверка комментариев на запрещённые маркеры рабочего процесса (`AGENTKIT_NOTE`, `\b[TFQC]\d{1,}\b`, `specs/work/`, «решение пользователя», «проверяющ», «итераци») — 0 совпадений.
   - Сверка ключей `msg://` изменённого XML `diadoc-packet-detail-view.xml` со словарем `messages_ru.properties` (`showRelatedButton.show`, `showRelatedButton.hide`, `relatedIcon.tooltip`, `supplementIcon.tooltip`) — все ключи присутствуют.

2. **Автоматизированные тесты:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.view.*" --tests "ru.fgk.ws.app.pt.view.*" --tests "ru.fgk.ws.app.dr.view.*" --tests "ru.fgk.ws.app.it.DiadocProcessingSettingsIT"`
     Результат: 22 passing, 1 pending (`contractorName_resolvesByInnKpp`), 0 failing.
     Успешно пройдены все тесты представления и фильтрации:
     - `DiadocPacketDocumentFilterTest`
     - `DiadocPacketListViewTest`
     - `PtRepairPacketListViewFilterTest`
     - `DrDiadocOperRepairPacketVu23ListViewTest`
     - `DiadocProcessingSettingsIT`

3. **Браузерная проверка карточки пакета Диадок (C1, F01.1, F01.2):**
   - Прогон под `t04_sadm` (роли `ui-minimal`, `ws-user`, `diadoc-show-packet-documents`, `diadoc-settings-admin`):
     - Карточка пакета `30a02e6d-9065-4176-bc5d-c05fe658bd34` открыта в режиме read-only.
     - Кнопка `showRelatedButton` активна, отображает текст «Скрыть связанные документы» и иконку `vaadin:unlink`. В таблице 13 документов (10 собственных, 2 связанных, 1 дополнение).
     - Документ-дополнение `1126585a-8a5b-4f0e-58f4-1d753065b909` отображает иконку `vaadin:paperclip` и tooltip «Дополнение к ФПУ-26: 5fac2c79-06dd-45da-9556-727cf44e6cd6».
     - При клике на кнопку текст меняется на «Показать связанные документы», иконка на `vaadin:link`, количество строк уменьшается до 11 (скрыты 2 связанных документа без признака дополнения).
     - Повторный клик восстанавливает 13 строк.
   - Прогон под `t04_plain` (роли `ui-minimal`, `ws-user`, `diadoc-show-packet-documents` без `diadoc-settings-admin`):
     - Кнопка `showRelatedButton` скрыта (`setVisible(false)`).
     - При `show_related_documents=false` в настройках по умолчанию отображается 11 документов (связанные скрыты, дополнение со скрепкой отображается).
     - При `show_related_documents=true` отображаются все 13 документов.
   - Прогон под `admin`:
     - Кнопка `refreshPacketType` («Обработать повторно») видна и активна.
     - Нажатие на кнопку выполняет перезагрузку сущности пакета по fetch plan контейнера без падения `IllegalArgumentException: parameter entityId`.

4. **Браузерная проверка фильтров контроля и кнопки «Текст ошибки» (C2, C3):**
   - `/pt-repair-packets`:
     - Выпадающий список фильтра «Результат контроля» содержит все 5 требуемых значений.
     - Выбор «Нарушение» ограничивает выборку строками с кодом 1 (4 строки).
     - Значение фильтра сохраняется в `ViewSettings` при перезагрузке и переходах.
     - Сброс фильтра возвращает все 5 строк.
   - `/dr-diadoc-oper-repair-packet-vu23s`:
     - Экран открывается без ошибок, фильтр «Результат контроля» присутствует.
     - Кнопка `showErrorBtn` («Текст ошибки») присутствует, заблокирована в отсутствие выбранной строки с нарушением.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C4 подтверждены.
После завершения проверки тестовые пользователи `t04_sadm` и `t04_plain` удалены, флаг `supplement_document` и настройки сброшены к исходному состоянию, процесс `bootRun` остановлен.

## Повторная проверка

Замечания по [F01](fixes/F01.md) полностью устранены:
- F01.1: `refreshEditedEntity()` перезагружает пакет с fetch plan контейнера `packetDocumentDc.getFetchPlan()`, исключение `parameter entityId` устранено.
- F01.2: чекбокс заменён кнопкой `showRelatedButton`, работающей в режиме read-only для роли `diadoc-settings-admin`.

## Следующий шаг

Переход к разблокированному таску T06: `task-execute` для [specs/work/diadoc-check/tasks/T06/task.md](../../T06/task.md) (сквозная проверка двух приложений на данных -26).
