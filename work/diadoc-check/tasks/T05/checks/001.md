# Проверка 001 — T05

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-21
Состояние кода: репозиторий `/home/mindils/data/dev/fgk/rvk-diadoc`, ветка
`f/schf-supplement`, коммит `b27af3a` (от `1cb6ae6`), рабочее дерево чистое до и
после проверки. В rvk-ws изменений по этому таску нет.
Окружение: БД `localhost:5436` (контейнер `db-rvk-diadoc`, `docker compose ps` —
Up), порт 8081 — ресурсы только `rvk-diadoc`, по плану изолированы от T01/T03,
других проверок на них не шло. Приложение поднималось `bootRun` с профилем `dev`
и переопределением datasource на `localhost:5436` через `--args` (файлы проекта
не менялись), после проверки остановлено, порт свободен.
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | подтверждён | `SchfSupplementLinkingTest` — 6 тестов, 0 падений (порядки A-B-C, C-A-B, B-C-A, A-C-B, идемпотентность повторной обработки C и B, DTO при `processDocument(C)`) |
| C2 | подтверждён | `PacketDocumentCrossMessageLinkingTest` — 16 тестов, 0 падений, включая заменённый «Линк на FPU_26 без своей строки diadoc_packet игнорируется…» и «PACKET с пересекающимся docid_arr линкует чужие документы как related без чужого PACKET» |
| C3 | подтверждён | `PacketViolationServiceTest` 8, `PacketDocumentSyncServiceTest` 7, `PacketDocumentDtoBuilderTest` 1, `DocumentProcessingServiceUnitTest` — все зелёные; бандл на живых данных: `GET /api/diadoc/packetForSync?messageId=<A>` и `<B>` дают у линка `fe79c311-…` `supplementDocument=true`, `relatedDocument=true`, `originalMessageId=<C>` |
| C4 | подтверждён | `./gradlew test` — BUILD SUCCESSFUL, 414 тестов, 0 failures, 0 errors, 3 skipped; `spotlessCheck` — BUILD SUCCESSFUL; `spotlessApply` + `git status --short` — пусто |
| C5 | подтверждён | Колонка добавлена `addColumn` (последняя по `ordinal_position`) с `column_default = false`; на 55 существующих линках `supplement_document is null` — 0 строк. Повторная обработка C, B, A при принудительно выставленных legacy-`null` не переписала ни одной строки (`xmin` всех 19 строк не изменились, значения остались `null`) |
| Ожидаемое состояние из «Результат и контекст» (живой прогон) | подтверждён | Порядок C, A, B через `POST /api/v2/documents/process` даёт ровно ожидаемое: пакет A — ФПУ-26 `a2532529` (`related=f`, `supplement=f`, `data_source=null`, `original=A`) и СЧФ `fe79c311` (`related=t`, `supplement=t`, `original=C`); пакет B — ФПУ-26 (`related=t`, `supplement=f`, `DOCID_ARR`, `original=A`) и СЧФ (`related=t`, `supplement=t`, `original=C`), линка на PACKET A `7417ec4a` нет; сообщение C — `deleted_date` у пакета и у линка; в `violations` A и B нет `D0075648756` |
| Негатив: СЧФ без наработанной ФПУ-26 остаётся standalone | подтверждён | Живой прогон: после удаления пакетов A/B/C обработка одного C даёт один линк `fe79c311` с `related=f`, `supplement_document=f`, пакет не tombstone-нут. Плюс зелёный `shouldReturnStandaloneSchf_whenProcessSfchWithoutPreparedPacket` с добавленной проверкой `is_supplement=false` |
| Следы работы в коде (раздел протокола «Комментарии в коде») | подтверждён | Поиск по всем 21 изменённым файлам шаблонами `AGENTKIT_NOTE:`, `\b[TFQC]\d+\b`, `specs/work/`, «решение пользователя», «проверяющ», «итераци» — совпадений по существу нет (два ложных: `LocalDateTime.parse(s + "T00:00:00")` и комментарий «текущей итерации» о проходе цикла сборки, существующий в `main` с коммита `413ac24`) |

## Выполненные проверки

Окружение:

```bash
(cd /home/mindils/data/dev/fgk/rvk-diadoc/envs/dev && docker compose ps)
# db-rvk-diadoc  postgres:16  Up 4 days  0.0.0.0:5436->5432/tcp
```

Полный набор тестов и форматирование:

```bash
cd /home/mindils/data/dev/fgk/rvk-diadoc
./gradlew test          # BUILD SUCCESSFUL in 35s; 411 passing, 3 pending
./gradlew spotlessCheck # BUILD SUCCESSFUL
./gradlew spotlessApply && git status --short   # пусто
```

Свод по `build/test-results/test/*.xml` (73 файла): `tests 414 failures 0
errors 0 skipped 3`. По классам критериев: `SchfSupplementLinkingTest` 6/0/0,
`PacketDocumentCrossMessageLinkingTest` 16/0/0, `PacketViolationServiceTest`
8/0/0, `PacketDocumentSyncServiceTest` 7/0/0, `PacketDocumentDtoBuilderTest`
1/0/0, `DocumentProcessingServiceUnitTest` — без падений.

Миграция (C5), состояние колонки на живой БД:

```sql
select column_name, column_default, is_nullable from information_schema.columns
where table_name = 'diadoc_packet_doc_link';
-- supplement_document | boolean | false | YES  (последняя по ordinal_position)
select count(*) total, count(*) filter (where supplement_document is null) nulls
from diadoc_packet_doc_link;   -- 55 | 0
```

Семантика backfill проверена отдельно на временной таблице: `alter table …
add column supplement_document boolean default false` заполняет существующие
строки значением `false`, не `null` (таблица удалена после проверки).

Ручной сценарий (`bootRun`, порт 8081, токен `client_credentials` клиента
`rvk-app`):

1. `POST /api/v2/documents/process` для C, A, B (HTTP 200, `documents` 1 / 8 /
   10). SQL по `diadoc_packet_doc_link` для трёх messageId дал 19 строк, полностью
   совпадающих с ожидаемым состоянием из таска (детали — в таблице критериев).
   `diadoc_packet`: C — `deleted_date` заполнен, A и B — живые. `violations` A
   пустые, у B только `CROSS_PACKET_REFERENCE` по docid_arr-документам (поведение
   до этой задачи), `D0075648756` не встречается.
2. `GET /api/diadoc/packetForSync?messageId=<A>` и `<B>` — HTTP 200, ровно один
   линк с `supplementDocument=true` в каждом бандле, это `fe79c311-…` с
   `relatedDocument=true` и `originalMessageId=<C>`.
3. Проверка «не переписывает все линки»: снят снимок `xmin` всех 19 линков,
   `supplement_document` принудительно выставлен в `null` на всех
   не-дополнениях (эмуляция строк, существовавших до миграции), затем повторно
   обработаны C, B, A. Снимок `xmin` после совпал со снимком до построчно —
   ни одна строка не переписана, значения остались `null`.
4. Негатив: удалены `diadoc_packet`, `diadoc_packet_doc`,
   `diadoc_packet_doc_link` и операции для A, B, C; обработано одно C — получен
   один линк `fe79c311` с `related_document=false`, `supplement_document=false`,
   пакет C живой (не tombstone). Затем обработаны A и B — итоговое состояние
   снова совпало с ожидаемым, `supplement_document is null` — 0 строк.

Приложение остановлено, порт 8081 свободен, рабочее дерево `rvk-diadoc` чистое.

## Ошибки и непроверенное

Подтверждённых ошибок нет. Непроверенного в объёме таска нет.

Замечания без статуса дефекта, на будущее:

- Четыре локальных решения исполнителя из `result.md` проверены и приняты:
  перепутанные в плане id сообщения C и entity СЧФ (тесты и живой прогон идут по
  фактическим данным `-26`), снятый префикс `diadoc.` в тестовом SQL (как во всех
  остальных файлах `liquibase/sql/`), ослабленная проверка tombstone C (своя
  строка `diadoc_packet` у сообщения СЧФ появляется только при standalone-порядке;
  живой прогон C→A→B показал, что при её наличии `deleted_date` проставляется),
  форматный прогон `spotlessApply` по `Fpu26ContentExtractor` (нарушение
  существовало в `main` до задачи).
- Приём `supplementDocument` зеркалом rvk-ws в объём T05 не входит — T03,
  сквозная проверка двух приложений — T06.

## Следующий шаг

Таск закрыт. По плану следующий доступный — `task-execute` по
`specs/work/diadoc-check/tasks/T01/task.md` либо `tasks/T03/task.md` (волна 1,
зависимостей нет); T06 запускается после `done` у T02, T04 и T05.
