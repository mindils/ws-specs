# Проверка 001 — T03

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-21 05:50 EDT
Состояние кода: commit 631acb2e + незакоммиченные изменения T01 и T03 в рабочей копии
Окружение: docker compose (PostgreSQL 16, localhost:5432), схема main_f_diadoc_check, application-test-local.properties
Итог: fail

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | ошибка | Колонка `supplement_document` присутствует в `main_f_diadoc_check.diadoc_packet_doc_link`; changeSet 9 идемпотентен; replay CSV-фикстуры парсятся и вставляются без ошибок, но запуск `./gradlew :app:test --tests "ru.fgk.ws.app.it.PacketReplayIT"` падает (7 тестов fail) из-за зашитой схемы `main` в `PacketReplayFixtures` |
| C2 | подтверждён | `SyncPacketServiceTest.syncStoresAndOverwritesSupplementDocumentFlag` подтверждает запись `true`, `false` при обновлении и сохранение `null` при отсутствии поля; `SyncPacketBundleDtoJsonTest` подтверждает сериализацию/десериализацию Gson; все 22 теста `SyncPacketServiceTest` проходят |
| C3 | подтверждён | `PacketDocumentMapperTest.supplementIsReadFromIsSupplementJsonKey` и `supplementPassesThroughMapper` подтверждают чтение `is_supplement` из JSON и передачу в `DocumentItemDto.supplement` |
| C4 | подтверждён | `DocumentDataSourceTest.docidArrIsRecognized` и `linkWithDocidArrDataSourceResolvesToEnum` подтверждают, что строка `DOCID_ARR` резолвится в `DocumentDataSource.DOCID_ARR` |

## Выполненные проверки

1. **Компиляция и линтинг:**
   - `./gradlew :app:compileJava`: BUILD SUCCESSFUL (код завершения 0).
   - `./gradlew spotlessCheckAll`: BUILD SUCCESSFUL (код завершения 0).
2. **Проверка схемы БД:**
   - `docker exec docker-rvk-db-1 psql -U root -d postgres -c "\d main_f_diadoc_check.diadoc_packet_doc_link"`:
     Колонка `supplement_document | boolean` присутствует.
3. **Целевые тесты T03:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.service.SyncPacketBundleDtoJsonTest" --tests "ru.fgk.ws.app.diadoc.service.SyncPacketServiceTest" --tests "ru.fgk.ws.app.diadoc.entity.DocumentDataSourceTest" --tests "ru.fgk.ws.app.diadoc.mapper.PacketDocumentMapperTest"`:
     33 passing, 0 failing (BUILD SUCCESSFUL).
4. **Регрессионные UI-тесты:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.diadoc.view.diadocpacket.DiadocPacketListViewTest"`:
     1 passing, 0 failing (BUILD SUCCESSFUL).
5. **Тест разбора пакетов (PacketReplayIT):**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.PacketReplayIT"`:
     7 failing, 0 passing. Все 7 тестов падают с ошибкой `NoSuchElementException: No value present` на строке 216 `localPacketService.load(UUID.fromString(messageId)).orElseThrow()`.
6. **Комментарии в коде:**
   - Поиск по шаблонам `AGENTKIT_NOTE:`, `\b[TFQC]\d{1,}\b`, `specs/work/`, `решение пользователя`, `замечание проверяющ`, `итераци` в git diff T03 и новых файлах — совпадений нет.

## Ошибки и непроверенное

- **Ошибка F01.1:** `PacketReplayIT` падает из-за несовместимости `PacketReplayFixtures` со схемой `main_f_diadoc_check`, заданной для worktree в `application-test-local.properties`. В `tables.txt` и методе `loadChecks` жестко зашита схема `main`, из-за чего данные вставляются в `main.diadoc_packet`, а `localPacketService` читает из `main_f_diadoc_check.diadoc_packet` и не находит пакет. Оформлено в [fixes/F01.md](../fixes/F01.md). Временная проверка с резолвом `current_schema()` подтвердила прохождение всех 7 тестов `PacketReplayIT` (доработка репозиторных тестов по правилам роли передаётся исполнителю).

## Следующий шаг

Исполнитель: [fixes/F01.md](../fixes/F01.md) — адаптировать `PacketReplayFixtures` к текущей схеме подключения (`select current_schema()`) и передать итерацию 2 на проверку.
