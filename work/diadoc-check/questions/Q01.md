# Q01 — Полный `:app:test` в worktree: 90 падений из-за жёсткой схемы `main` в тестах

Статус: resolved
Затронутые таски: [T06](../tasks/T06/task.md), [T07](../tasks/T07/task.md)
Блокирует: нет — ответ получен, объём оформлен таском T07; T06 ждёт его
выполнения как зависимости
Этап до блокировки: сценарий T06 пройден полностью и независимо проверен;
критерии C1-C3 подтверждены, открыт только C4

## Вопрос и факты

Гейт работы и критерий C4 таска T06 требуют `./gradlew :app:test` с `0
failing`. В рабочей копии (git worktree, схема `main_f_diadoc_check`) полный
набор даёт **1446 passing, 90 failing**. Проверено, что это не регресс:

- прогон того же набора на базовом коммите `631acb2e0` (до всей работы), в
  отдельном worktree и отдельной схеме `main_t06_base`, дал **1399 passing,
  100 failing**;
- пофамильное сравнение множеств упавших тестов: новых падений — **0**;
  10 тестов, наоборот, починены этой работой (`PacketReplayIT` — 7,
  `RemDetPacketReplayIT` — 3, это F01 к T03 и попутная починка в T02).

Причина падений одна и та же: интеграционные тесты пишут и убирают данные
жёстко квалифицированным SQL `main.<таблица>` (`DrOperRepairTr2ParserServiceIT`,
`TechClaimDeadlineIT`, `WorkCalendarReferenceDataIT`, `MhDetailOriginServiceIT`,
`DiadocPacketCorrectionIT` и ещё около десятка), тогда как подключение в
worktree идёт на `main_f_diadoc_check`. Подготовка данных и уборка уходят в
чужую схему, поэтому тесты не находят своих данных
(`EmptyResultDataAccessException`, `NoSuchElementException`) либо спотыкаются
об остатки прошлого теста (`duplicate key … pk_dr_diadoc_wag_oper_repair_contract`).
Ровно этот дефект уже чинился точечно в `PacketReplayFixtures` (F01 к T03) —
там имена таблиц приведены к `current_schema()`.

Независимая проверка [checks/001.md](../tasks/T06/checks/001.md) повторила
оба прогона на своих схемах и подтвердила цифры: база `631acb2e0` — 1511
тестов, 100 падений; рабочая копия на пересозданной схеме — 1548 тестов, 90
падений; новых падений 0, починено 10. Списки упавших тестов сохранены в
[app-test-failures-base-631acb2e0.txt](../tasks/T06/app-test-failures-base-631acb2e0.txt)
и [app-test-failures-worktree.txt](../tasks/T06/app-test-failures-worktree.txt).
Остальные критерии T06 проверкой подтверждены, поэтому таск ждёт только
этого решения.

Локально в рамках T06 это не решается: правка касается полутора десятков
тестовых классов вне области таска («код — только исправления найденных
расхождений в областях завершённых тасков»), а в основной схеме `main` те же
тесты, скорее всего, зелёные.

## Варианты и рекомендация

1. **Принять как ограничение окружения** (рекомендую). C4 считать выполненным
   по сравнению с базовым коммитом: 0 новых падений, 10 починенных. В
   `summary.md` работы зафиксировать, что полный `0 failing` достижим только
   на схеме `main`. Объём работы не растёт.
2. **Починить схему в тестах внутри этой работы** — привести `main.` к
   `current_schema()` во всех затронутых тестах. Это отдельный таск уровня
   `hard`: около 15 классов, часть из них вне тематики Диадок (претензии,
   производственный календарь), каждый требует повторного прогона. Работа
   расширяется заметно.
3. **Вынести починку в отдельную работу** со ссылкой на этот `summary.md`, а
   T06 закрыть по варианту 1.

До ответа продолжать нечего: остальные критерии T06 подтверждены, результат
передан на независимую проверку.

## Для ответа

В рабочей копии проекта (git worktree) своя схема БД `main_f_diadoc_check`, а
полтора десятка интеграционных тестов написаны с жёстким именем схемы `main`
внутри SQL. Из-за этого полный `./gradlew :app:test` в worktree даёт 90
падений — и давал их (даже 100) ещё до начала этой работы: сравнение с базовым
коммитом показало ноль новых падений и десять починенных. Сама доработка
Диадок проверена сквозным сценарием на живых приложениях и своими тестами.

Вопрос: считать ли критерий «полный набор тестов зелёный» выполненным по
сравнению с базовым коммитом, или чинить жёсткую схему в тестах?

- **Принять сравнение с базой** (рекомендуется) — работа закрывается сейчас,
  в итоговом summary остаётся запись про ограничение worktree.
- **Чинить в этой работе** — плюс один большой таск (~15 тестовых классов,
  часть вне Диадок), закрытие работы сдвигается.
- **Отдельная работа** — закрываем эту сейчас, починку планируем следующей.

До ответа T06 уходит на независимую проверку по остальным критериям.

## Ответ пользователя

Нужно поправить, чтобы не было жёсткой привязки к `main`. Это делаем в этой
работе.

## Ответ и основание

Принят вариант 2: жёсткая привязка к схеме `main` убирается из тестов внутри
этой работы, критерий C4 остаётся буквальным (`0 failing` в любой схеме, в
том числе в worktree). Основание — ответ пользователя 2026-09-21.

Объём уточнён разбором кода при применении ответа и оказался шире, чем
оценивалось в вопросе:

- `app/src/test/java` — 262 вхождения `main.<имя>` в 25 файлах, из них
  SQL-квалификаторы в 21 файле; падают 19 классов (список —
  [app-test-failures-worktree.txt](../tasks/T06/app-test-failures-worktree.txt)).
- Тестовые данные Liquibase тоже жёстко на `main` — 7 файлов
  `app/src/test/resources/ru/fgk/ws/app/liquibase/sql/*.sql` (42 вхождения),
  подключаются `changelog-test.xml` через `<sqlFile>`; `defaultSchemaName`
  нигде не задан, поэтому без их правки `0 failing` недостижим. Именно этим
  объясняется падение `TechClaimDemoDataIT`, где своего `main.` нет.
- Ложные срабатывания, которые трогать нельзя: `LocalTestDataSource`
  (ключи свойств `main.datasource.*`), `CaseOneUrlsTest`
  (`http://case.main.vgk`), `DiadocSqlValidatorTest` (проверяет продуктовое
  правило «разрешена только схема main»), `DiadocPacketDocumentsReadTest`
  (имя файла `main.xml`).
- Продуктовый код SQL со схемой `main` не содержит, поэтому правка
  ограничена тестами и поведение продукта не меняет.

Техническое решение резолвера (детали — в [T07](../tasks/T07/task.md)):
основной приём — снять квалификатор, имя схемы уже приходит из
`currentSchema` в JDBC URL и лежит в `search_path`. `select current_schema()`
(прецедент F01 к T03: `PacketReplayFixtures`, `RemDetPacketReplayIT`)
применяется только там, где имя схемы нужно как значение.

Последствия: закрытие работы сдвигается на один таск; гейт «полный набор
тестов зелёный» из раздела «Критерии всей работы» не смягчается; T06
переоткрывается не из-за дефекта, а как зависимый от T07 — его критерии
C1-C3 остаются подтверждёнными отчётом
[checks/001.md](../tasks/T06/checks/001.md), переподтверждается только C4.

## Применение

Обновлены: `plan.md` (новая строка T07 в таблицах «Таски» и «Параллельность
и тестирование», волна 4 в разделе «Как выполнять»), создан
[T07](../tasks/T07/task.md), [T06](../tasks/T06/task.md) переведён на
зависимость от T07 и очищен от двух расхождений, отмеченных проверкой
(формулировка негативного сценария и путаница идентификаторов сообщения C).

Код ещё предстоит изменить: 21 тестовый класс в `app/src/test/java` и 7
файлов `app/src/test/resources/ru/fgk/ws/app/liquibase/sql/*.sql` — это
объём T07.

Следующий шаг: `task-execute` с путём
`specs/work/diadoc-check/tasks/T07/task.md`; после его `done` — `task-verify`
с `specs/work/diadoc-check/tasks/T06/task.md`.
