# Проверка 001 — T03

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 04:20–04:52 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `3c92665f1`, 34 незакоммиченных изменения (весь код T01–T03 лежит в рабочем дереве, не закоммичен)
Окружение: схема `main_rvk_ws` в общем Docker PostgreSQL, приложение `bootRun` на порту 8082 (поднято и остановлено проверяющим), MinIO из `docker/docker-compose.yml`, бакет `rvk-dev`, браузер `playwright-cli` (Chromium). Порядок по plan.md: T10 заблокирован [Q02](../../../questions/Q02.md) и ресурс не занимал, параллельных проверок и сессий исполнителя не было; порт 8082 и схема `main_rvk_ws` заняты только этой проверкой.
Итог: fail

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | ошибка ([F01.1](../fixes/F01.md)) | Реестр, создание и поиск по номеру работают; поиск по подстроке в `entityComboBox` — нет |
| C2 | подтверждён | смена модели и размера обновляет виртуальные поля, ручные не меняются |
| C3 | ошибка ([F01.2](../fixes/F01.md)) | нормализация номера верна, сообщение о дубле — техническое |
| C4 | подтверждён | диалоги характеристики: предустановленный контейнер, отмена, обновление грида |
| C5 | подтверждён | PDF загружен, отрисован, отдан по «Скачать», заменён без потери записи |
| C6 | ошибка ([F01.3](../fixes/F01.md)) | копия и удаление работают, отказ показан «Непредвиденной ошибкой» |
| C7 | подтверждён | вкладка «История» показала «Создание» и «Изменение» с атрибутами |
| C8 | ошибка ([F01.4](../fixes/F01.md)) | сохранение роли read запрещено и экспорт учитывает фильтр, но часть кнопок доступна |
| C9 | подтверждён | раздел «Уточнено при реализации T03» в `contracts.md` |

## Выполненные проверки

### 1. Gate 1 — статические проверки

IDE-инспекции (`get_file_problems`) в этой сессии не было, выполнен
эквивалент — механические проверки по всем 12 новым и изменённым файлам T03
плюс `compileJava` в составе `:app:test`:

- 33 XML раздела `container` разобраны парсером — нет пустых файлов и ошибок
  разбора;
- `package` каждого `.java` совпадает с каталогом;
- дублей `@ViewController(id=…)` по всему `app/src/main/java` нет;
- каждый `@ViewDescriptor` и `@FragmentDescriptor` резолвится в существующий
  XML;
- все 21 ссылка `msg://` шести дескрипторов T03 и ключи из Java-контроллеров
  (`containerListView.copy.noSelection`, `containerPropertyFragment.*`,
  `containerFileFragment.*`, `contNumRequired`) резолвятся в
  `messages_ru.properties` — нерезолвящихся нет.

Покрытие замены: механические проверки ловят структурные дефекты
дескрипторов, но не семантику property path и data container — их в этой
проверке закрыл фактический браузерный проход по всем экранам T03.

### 2. Gate 2 — полный набор тестов

```bash
cd ../worktrees/rvk-ws/cyan-fennel/rvk-ws
./gradlew :app:test
```

Результат: 1308 тестов, 1247 passing, 12 skipped, 49 failing, BUILD FAILED.

Контейнерный раздел — 31 тест в 11 классах, 0 падений
(`ContainerChildRecordsIT`, `ContainerCrudIT`, `ContainerDeletePolicyIT`,
`ContainerEntityLogIT`, `ContainerNsiReferenceIT`, `ContainerNumberIT`,
`EntityCopySupportIT`, `ContainerRolesTest`, `ContainerNsiViewsUiTest`,
`ContainerRegistryViewsUiTest`, `EntityLogFragmentUiTest`).

Все 49 падений — в легаси-модулях `dr`, `pt`, `mh`, `diadoc`
(`DrOperRepairTr2ParserServiceIT`, `MhDetailOriginServiceIT`,
`DiadocPacketCorrectionIT`, `PacketReplayIT`, `ReceiptCostReplayIT`,
`Mh1mPacketMergeIT`, `MhReferenceCostLookupIT`, `DrPlanRepairParserServiceIT`,
`PtRepairPacketCorrectionIT`, `ContractRepairCountIT`,
`DrOperRepairParserServiceTest`). Причина подтверждена независимо: их фикстуры
пишут SQL с жёстко зашитой схемой (`insert into main.dr_diadoc_wag_oper_repair_contract …`
в `ContractRepairCountIT`, `MhReferenceCostLookupIT`, `PacketReplayFixtures`,
`DiadocPacketCorrectionIT` и ещё шести файлах), а приложение в worktree читает
`main_rvk_ws`. Тот же перечень классов и то же число падений зафиксированы в
[T01/checks/001.md](../../T01/checks/001.md) и
[T09/checks/001.md](../../T09/checks/001.md) до появления кода T03, поэтому
регрессией T03 они не являются.

### 3. Gate 3 — браузерный проход C1–C8

Приложение поднято `./gradlew :app:bootRun` (порт 8082), вход через
`/local-login`. Для C2 и C5 подготовлены данные: по две записи моделей и
размеров, по одной записи типа, обобщённой характеристики, вида
характеристики и вида файла, одна организация в
`ws_store.ora_assb_org_passport_current` (способ разрешён plan.md), роль
`async-export-role` временно назначена `user_edit` и `user_read` — в рабочем
контуре её даёт `ws-user` (`WsUserRole extends … AsyncExportRole`), поэтому
контейнерные роли её не содержат намеренно. Все подготовленные записи и
назначения удалены после проверки; в `cnt_container` снова 0 строк.

**C1.** Пункт «Контейнеры → Реестр контейнеров» открывает `/containers`: 9
фильтров, 21 колонка с русскими подписями, `gridColumnVisibility`,
`simplePagination`, кнопки Создать / Изменить / Копировать / Удалить /
Фоновый экспорт, сырых `msg://` нет. Создан контейнер со всеми полями, НСИ
выбраны через `entity_lookup`, после сохранения запись видна в списке
(`1 строка`), фильтр по номеру `5026` даёт 1 строку, `НЕТ-ТАКОГО` — 0 строк.
Открытие самого списка в режиме lookup из другой формы не проверялось:
потребителя (формы ремонта и освидетельствования) ещё нет — их делают T04 и
T07; в XML объявлены `@LookupComponent("containersDataGrid")` и действия
`lookup_select`/`lookup_discard`.

Ошибка: в `entityComboBox` поиск работает только по точному полному значению.
Подробности — [F01.1](../fixes/F01.md).

**C2.** Выбор модели `V03-MODEL-A` заполнил «Тенты/Дуги/Тросы/Вент.
отверстия» = 1/2/3/4; смена на `V03-MODEL-B` обновила их на 7/8/9/10. Размер
`CS40` заполнил «Длина (фут)» = 40. Поле «Тип» показывает наименование
(`Крупнотоннажный универсальный`). Ручные поля (30 480 / 2 200 / 28 280 /
67 700) при смене lookup не изменились.
Скриншот: `.playwright-cli/t03-verify-c2-form.png`.

**C3.** Номер ` fgku5026002 ` (с пробелами по краям) сохранён как
`FGKU5026002` — подтверждено и в форме, и запросом
`select '['||cont_num||']' from main_rvk_ws.cnt_container`. Повторное создание
с номером `fgku5026002` форму не закрыло (URL остался `/containers/new`), но
показало техническое сообщение — [F01.2](../fixes/F01.md).
Скриншот: `.playwright-cli/t03-verify-c3-duplicate.png`.

**C4.** У несохранённого контейнера вкладок нет, вместо них подсказка
«Сохраните описание — после этого станут доступны вкладки контейнера». После
сохранения появились ровно три вкладки (Характеристики, Файлы, История), мест
под невидимые вкладки нет. Диалог характеристики открылся с предустановленным
read-only контейнером `FGKU5026002`; заполнение и «Отмена» → «Не сохранять»
не создали записи (`select count(*) from cnt_container_property` = 0); после
OK запись в БД появилась и грид вкладки обновился без перезагрузки страницы.

**C5.** PDF загружен, запись сохранена с
`file = s3://cnt_file/2026/09/15/…pdf?name=t03-first.pdf` (папка хранилища
`cnt_file` подтверждена), грид обновился, «Имя файла» осталось ручным
(`ТТН от 01.02.2026`, не равно имени вложения). Просмотр справа отрисовал PDF
(`.playwright-cli/t03-verify-c5-pdf-preview.png`). Кнопка «Скачать» отдала
файл: `GET /VAADIN/dynamic/resource/…` → 200,
`content-type: application/pdf`, `content-disposition: inline;
filename="t03-first.pdf"`, 596 байт = размер исходного файла. Замена файла
сохранила ту же запись (`id = 17`) с новым `FileRef`
(`…?name=t03-second.pdf`), просмотр показал новый документ
(`.playwright-cli/t03-verify-c5-pdf-replaced.png`).

**C6.** «Копировать» открыло карточку со всеми бизнес-полями исходной записи,
пустым номером и без вкладок (детей и файлов нет)
(`.playwright-cli/t03-verify-c6-copy.png`); сохранение копии с новым номером
создало отдельную запись без дочерних строк. Удаление контейнера с
характеристикой и файлом отклонено (запись в БД не удалена), но сообщение
техническое — [F01.3](../fixes/F01.md),
`.playwright-cli/t03-verify-c6-delete-denied.png`. После мягкого удаления
характеристики и файла удаление контейнера прошло: `deleted_date` проставлен,
запись исчезла из списка (`2 строки` → `1 строка`).

**C7.** Вкладка «История» показала событие «Создание», после правки поля
«Изготовитель» и сохранения — второе событие «Изменение»; в нижнем гриде
атрибут выведен русской подписью «Изготовитель» со старым
`Завод-Проверка` и новым `Завод-Проверка-2` значением.

**C8.** Под `user_read`: «Создать» и «Удалить» в реестре заблокированы,
«Изменить» превратилось в «Просмотр», карточка открылась полностью read-only,
прямой URL `/containers/new` тоже отдал read-only форму, попытка сохранить
дала «Доступ запрещён» и ничего не записала
(`.playwright-cli/t03-verify-c8-read-denied.png`); диалог характеристики из
вкладки открылся, но все его поля read-only и запись не создаётся. Часть
кнопок при этом остаётся активной — [F01.4](../fixes/F01.md).
Экспорт под `user_edit` с фильтром «Следующее освидетельствование с
01.01.2029» (в реестре 2 контейнера, под фильтр подходит один) сформировал
xlsx, в котором ровно одна строка данных — `FGKU7777001` — и 21 колонка с
русскими заголовками; файл извлечён из `common_export_task.file_content` и
разобран (`.playwright-cli/t03-verify-c8-export-filter.png`).

**Ошибки времени выполнения.** За весь проход в браузерной консоли один
ERROR — `ERR_NAME_NOT_RESOLVED` на аватар `https://info.main.vgk/...`
(корпоративный адрес, к T03 не относится). В серверном логе два события, оба
объяснены найденными ошибками: `UniqueConstraintViolationException` на
`idx_cnt_container_cont_num` и `DefaultUiExceptionHandler: Unhandled
exception` с `DeletePolicyException`. Vaadin error overlay не появлялся.

### 4. C9 — контракт вкладок

В `contracts.md`, раздел «Карточка контейнера и вкладки», подраздел «Уточнено
при реализации T03» описывает: класс и дескриптор карточки, id `containerDc`,
id `containerTabSheet` и вкладок, места для `repairsTab`/`surveysTab`/
`actsTab`, сигнатуру `setContainerDc(InstanceContainer<Container>)`,
обязательный `refresh()`, загрузку по `Target.HOST_CONTROLLER` и отклонение от
`withParentDataContext` с обоснованием. Имена сверены с фактическим кодом
`ContainerDetailView`, `container-detail-view.xml` и обоими фрагментами —
расхождений нет. Контракта достаточно, чтобы T04 и T07 подключили свои
вкладки.

## Ошибки и непроверенное

Подтверждённые ошибки вынесены в [fixes/F01.md](../fixes/F01.md): F01.1
(поиск в `entityComboBox` не работает по подстроке), F01.2 (техническое
сообщение о дубле номера), F01.3 (техническое сообщение при запрете
удаления), F01.4 (кнопки создания и копирования активны у роли read).

Не проверено:

- открытие реестра контейнеров в режиме lookup из формы-потребителя: такой
  формы ещё нет, её создают T04 и T07. Проверено косвенно — объявление
  `@LookupComponent` и действий `lookup_select`/`lookup_discard`, поиск по
  номеру в фильтре списка;
- `itemsQuery` собственника на реальном объёме организаций: локальная
  `nsi_v_org_passport` пуста, проверка шла на одной сеяной записи, которая и
  выявила F01.1;
- IDE-инспекция Jmix (`get_file_problems`) — инструмент в сессии недоступен,
  выполнен механический эквивалент (см. раздел 1) и полный браузерный проход.

## Следующий шаг

Исполнитель: `task-execute` с файлом
`specs/work/conteiner/tasks/T03/fixes/F01.md`. После исправления — повторная
проверка `task-verify` по `specs/work/conteiner/tasks/T03/task.md`.
