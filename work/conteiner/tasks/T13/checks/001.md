# Проверка 001 — T13

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-16 11:20–11:45 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `1ad658d3f`, 14 незакоммиченных файлов (5 файлов кода T13 с mtime 11:11–11:13 плюс 2 UI-теста T13, а также принятые T11, T12 и `IframeSecurity.java` из T14 и общий `messages_ru.properties`); `git status --short` до и после проверки совпадает, код продукта и репозиторные тесты проверкой не менялись
Окружение: схема `main_rvk_ws` в общем Docker PostgreSQL (`docker-rvk-db-1`), MinIO и Keycloak из `docker/docker-compose.yml`, приложение `bootRun` на порту 8082 — поднято и остановлено проверяющим (`kill 526703 526500`), готовность ловилась кодом `200` на `/local-login`; браузер `playwright-cli` (Chrome, две сессии: `t13` — `user_edit`, `t13r` — `user_read`), разрешение 1366×768. Порядок по plan.md: T14, T11 и T12 `done`, T13 идёт следующей, T15 в статусе `todo` и на ресурсах не работает. Порт 8082, БД и gradle daemon заняты только этой проверкой: `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` отработал до старта `bootRun`, браузерный проход — после него, `spotlessCheckAll` — после остановки приложения. Порт 8083 параллельно занимало приложение соседнего worktree `serene-basin` (чужая работа), контейнерных таблиц оно не трогает
Итог: fail — критерии C1–C4 подтверждены, но в изменённом файле остаётся комментарий со ссылкой на артефакт работы ([fixes/F01.md](../fixes/F01.md))

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | подтверждён | Разделы 2–4: `/container-surveys/new` без акта — `#displayFile` в DOM отсутствует, картинок 0, «Скачать акт» `disabled`, форма слева 640 px, `detailActions` 708–752 при высоте окна 768; после загрузки PDF панель появилась справа (x=819, 531×619, `iframe`), кнопка активна; очистка файла снова прячет панель (`display: none`, 0×0) и гасит кнопку; сохранённое освидетельствование `229` с актом открывается с видимой панелью; из вкладки карточки — та же форма диалогом, «Контейнер» `readonly` со значением `T13CHK00001` (регрессия T04) |
| C2 | подтверждён | Раздел 5: без выбора грид 1046 px на всю вкладку, панель `hidden`; после «Создать» + `refresh()` без прежнего выбора панель осталась скрытой; выбор строки с PDF — грид 516 + панель 514 с `iframe`; снятие выбора повторным кликом — снова 1046 px и скрытая панель; строка без файла (заведена SQL-вставкой) — панель скрыта, битых картинок 0; «Скачать» отдал тот же PDF (`200`, `Content-Type: application/pdf`, 381 байт, `filename="t13-act.pdf"`) — регрессия T03 |
| C3 | подтверждён | Раздел 6: `/container-contracts/212` с заполненными номером и тремя датами и непустым гридом условий при 1366×768 — `contentScroller` 619 px прокручивает содержимое 739 px, «Сохранить / OK / Отмена» на y=712–748, `elementFromPoint` в центре каждой кнопки возвращает саму кнопку; после прокрутки вниз грид виден целиком (292–692), кнопки остались на 708–752; у нового договора — подсказка вместо грида, `warrantiesSection` в DOM нет (регрессия T06) |
| C4 | подтверждён | Разделы 1 и 7: в трёх XML нет `split`, `css=`, `<details>`, `splitterPosition`, `displayNotFoundFile`; `classNames` только `buttons-panel` и `text-secondary`; все 10 `msg://` резолвятся в `messages_ru.properties`; `document.body.innerText.includes('msg://')` = `false` на всех проверенных экранах; `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` — BUILD SUCCESSFUL, 68 passing, 0 failures, включая `ContainerSurveyIT` (2) и `ContainerContractWarrantyIT` (2); `spotlessCheckAll` — BUILD SUCCESSFUL |
| Роль `container-read` | подтверждён | Раздел 8: под `user_read` открываются `/container-surveys/229` (все 7 полей формы `readonly`, панель просмотра видна, «Скачать акт» активна, кнопка загрузки 0×0, кнопки очистки нет), `/container-contracts/212` (та же компоновка, кнопки на 712) и вкладка «Файлы» карточки (без выбора грид 1046 px, с выбором строки с файлом — 516 + 514 с `iframe`); сырых `msg://` нет |
| Комментарии в коде | ошибка | Раздел 1: в `ContainerSurveyDetailView.java` (изменённый файл) остаётся javadoc «по решению пользователя дата следующего освидетельствования…» — формулировка из запрещённого списка; см. [F01.1](../fixes/F01.md) |

## Выполненные проверки

### 1. Статические проверки и комментарии

```bash
cd ../worktrees/rvk-ws/cyan-fennel/rvk-ws
./gradlew :app:test --tests "ru.fgk.ws.app.container.*"   # BUILD SUCCESSFUL in 1m 3s, 68 passing
./gradlew spotlessCheckAll                                # BUILD SUCCESSFUL in 1s
```

Разбор `app/build/test-results/test/*.xml`: 21 класс, 68 тестов, 0 failures,
0 skipped, в том числе `ContainerSurveyIT` (2), `ContainerContractWarrantyIT`
(2), `ContainerSurveyViewsUiTest` (4, вместе с новым «У освидетельствования без
акта панели просмотра нет») и `ContainerContractViewsUiTest` (5, вместе с новым
«У нового договора вместо грида условий показана подсказка»).

Механические проверки трёх XML: все разбираются `xml.dom.minidom`; 0 вхождений
`css=`, `<split`, `splitterPosition`, `<details`, `displayNotFoundFile`;
`classNames` встречается трижды и только со значениями `buttons-panel` и
`text-secondary` (оба разрешены планом); 10 адресов `msg://` из трёх файлов
найдены в `messages_ru.properties`. Удалённые ключи
`containerSurveyDetailView.noFile` и `containerFileFragment.noFile` в `app/src`,
`core` и `sso-plagin` больше не упоминаются; бины `Messages` и `MessageBundle` в
обоих контроллерах остались использованными.

Поиск по правилу «Комментарии в коде» шёл по семи изменённым файлам T13 с
нормализацией переносов строк — построчный `grep` формулировку, разорванную
переносом javadoc, не видит:

```bash
python3 - <<'PY'
import re
files=['app/src/main/java/ru/fgk/ws/app/container/view/containersurvey/ContainerSurveyDetailView.java',
'app/src/main/java/ru/fgk/ws/app/container/view/fragment/containerfile/ContainerFileFragment.java',
'app/src/main/resources/ru/fgk/ws/app/container/view/containersurvey/container-survey-detail-view.xml',
'app/src/main/resources/ru/fgk/ws/app/container/view/fragment/containerfile/container-file-fragment.xml',
'app/src/main/resources/ru/fgk/ws/app/container/view/containercontract/container-contract-detail-view.xml',
'app/src/test/java/ru/fgk/ws/app/container/view/ContainerSurveyViewsUiTest.java',
'app/src/test/java/ru/fgk/ws/app/container/view/ContainerContractViewsUiTest.java']
pats=[r'\b[TFQC]\d{1,}\b', r'specs/work/', r'решени\w*\s+пользовател\w*', r'проверяющ',
      r'итераци', r'по плану', r'AGENTKIT']
for f in files:
    n=re.sub(r'\n\s*\*\s*',' ',open(f,encoding='utf-8').read()); n=re.sub(r'\s+',' ',n)
    for p in pats:
        for m in re.finditer(p,n,re.I):
            print(f,'::',p,'::',n[max(0,m.start()-90):m.end()+60])
PY
```

Единственное совпадение — `ContainerSurveyDetailView.java`, строки 42–44:

```java
 * <p>Никакой логики вокруг {@code nextSurveyDate} контейнера здесь нет и быть не должно: по решению
 * пользователя дата следующего освидетельствования в реестре ведётся вручную и от этих записей не
 * зависит.
```

Абзац сам T13 не писал — он пришёл из более раннего таска, — но файл T13
изменяет, и правило протокола проверяется по изменённым файлам; соседний абзац
того же javadoc исполнитель в этой итерации правил. Подробности — [F01.1](../fixes/F01.md).

### 2. C1 — освидетельствование без акта

`/container-surveys/new` под `user_edit`, 1366×768:

```json
{"surveyBox":{"x":272,"y":73,"w":1078,"h":619},
 "formScroller":{"x":272,"y":73,"w":640,"h":619},
 "displayFile":"absent-in-dom",
 "detailActions":{"x":272,"y":708,"w":347,"h":44},
 "download":{"disabled":true,"text":"Скачать акт"},
 "imgs":[],"rawMsg":false,"innerH":768}
```

Панели просмотра в DOM нет совсем, картинок на форме 0, «Скачать акт»
неактивна, форма занимает левые 640 px, кнопки — на y=708 при высоте окна 768.

### 3. C1 — загрузка и очистка акта

Загружен PDF (381 байт, одна страница, сгенерирован во временном каталоге
сессии). Сразу после загрузки, до сохранения записи:

```json
{"surveyBox":{"w":1078},"formScroller":{"x":272,"w":531},
 "displayFile":{"x":819,"y":73,"w":531,"h":619},"iframe":true,
 "detailActions":{"y":708},"download":{"disabled":false},"imgs":[]}
```

После «Remove file» у поля загрузки:
`{"displayFile":{"hidden":true,"display":"none","w":0,"h":0},
"formScroller":{"w":640},"download":{"disabled":true},"imgs":0}` — панель
исчезает, форма снова занимает 640 px.

### 4. C1 — вторая точка входа и сохранённая запись

Карточка контейнера `T13CHK00001` (`id = 1675`, заведён через UI) → вкладка
«Освидетельствования» → «Создать»: форма открывается диалогом,
`containerField` `readonly` и показывает `T13CHK00001` (регрессия T04),
`#displayFile` в диалоге отсутствует, «Скачать акт» `disabled`, `detailActions`
диалога на y=642–686. После загрузки PDF панель появилась (x=691, 552×486),
кнопка стала активна; «OK» сохранил запись — в БД
`cnt_survey 229 | container 1675 | s3://cnt_file/2026/09/16/5822e639-…pdf`.

Повторное открытие `/container-surveys/229`:

```json
{"displayFile":{"x":819,"y":73,"w":531,"h":619},"iframe":true,
 "download":{"disabled":false},"detailActions":{"y":708},
 "containerReadonly":false,"imgs":0,"rawMsg":false}
```

Панель видна сразу при открытии, из самостоятельного списка поле «Контейнер»
редактируемо — как и было до T13. Форма при видимой панели умещается в scroller
(`scrollHeight` 619 при `clientHeight` 619), нижний край поля загрузки — y=689,
верх панели кнопок — y=708, перекрытия нет.

### 5. C2 — вкладка «Файлы»

Все замеры — на вкладке «Файлы» карточки `/containers/1675`; идентификаторов у
элементов фрагмента в DOM нет, поэтому строка ищется структурно
(`vaadin-grid` и его родитель `vaadin-horizontal-layout`).

- пустая вкладка, выбора нет: `grid 1046×523`, второй ребёнок строки
  `hidden: true, display: none`, картинок 0, кнопки карточки на y=708;
- «Создать» → загрузка PDF → «OK»: строка появилась
  (`cnt_container_file 98`), после `refresh()` выбора нет
  (`selectedItems.length` = 0) и панель по-прежнему скрыта, грид 1046 px;
- выбор строки с PDF: `grid 516×523` + панель `514×523` с `iframe`;
- «Создать» второй записи при выбранной строке: выбор сохраняется
  (`selectedItems.length` = 1), панель остаётся видимой — критерий требует
  скрывать панель при снятии выбора, а выбор здесь не снимался;
- повторный клик по выбранной строке снимает выбор
  (`selectedItems.length` = 0) — панель `hidden`, грид снова 1046 px;
- строка без файла (вставлена напрямую в `cnt_container_file`, чтобы обойти
  обязательность файла в форме): панель скрыта, грид 1046 px, битых картинок 0;
- «Скачать» при выбранной строке с файлом открыл ресурс Vaadin; запрос с той же
  сессией дал `HTTP 200`, `Content-Disposition: inline; filename="t13-act.pdf"`,
  `Content-Type: application/pdf`, 381 байт — файл совпадает с загруженным.

### 6. C3 — карточка договора

`/container-contracts/212` под `user_edit`, 1366×768. В форму внесены номер
`T13-CHK-2026` и даты `01.01.2026 / 31.12.2026 / 15.12.2025`, сохранено;
через «Создать» в секции условий заведено гарантийное условие
(`cnt_warranty 295`, срок 12, осмотр 30 дней), грид стал непустым.

```json
{"contentScroller":{"x":272,"y":73,"w":1078,"h":619},
 "scrollable":{"scrollHeight":739,"clientHeight":619},
 "form":{"y":73,"h":231},"warrantiesSection":{"y":320,"h":492},
 "grid":{"y":412,"h":400},"warrantiesHint":"absent",
 "detailActions":{"x":272,"y":708,"w":363,"h":44},
 "buttons":[{"id":"saveButton","y":712,"hit":"saveButton"},
            {"id":"saveAndCloseButton","y":712,"hit":"saveAndCloseButton"},
            {"id":"closeButton","y":712,"hit":"closeButton"}],
 "rawMsg":false,"innerH":768}
```

Содержимое прокручивается внутри `contentScroller` (739 против 619), кнопки
лежат ниже его границы (692) и hit-тестируются сами собой — полями они не
перекрыты. После прокрутки в самый низ (`scrollTop` = 120) грид виден целиком
(292–692), панель кнопок осталась на 708–752.

Новый договор `/container-contracts/new`: `warrantiesSection` и
`warrantiesDataGrid` в DOM отсутствуют, `warrantiesHint` виден (607×26),
содержимое короче области прокрутки (`scrollHeight` = `clientHeight` = 619),
кнопки на 712 — поведение T06 сохранено.

### 7. Сырые `msg://` и консоль

`document.body.innerText.includes('msg://')` = `false` на `/container-surveys/new`,
`/container-surveys/229`, вкладках «Файлы» и «Освидетельствования» карточки
`/containers/1675`, `/container-contracts/212` и `/container-contracts/new`, в
обеих ролях. Единственная ошибка консоли за обе сессии — недоступный вне
корпоративной сети аватар: `Failed to load resource: net::ERR_NAME_NOT_RESOLVED
@ https://info.main.vgk/api2/v1/phone-book/avatar/user_edit` (и `…/user_read`).

### 8. Роль `container-read`

Отдельная сессия под `user_read` (пароль `read123` из
[T04/result.md](../../T04/result.md)):

- `/container-surveys/229` открывается, все семь полей формы `readonly`, панель
  просмотра видна (531×619, `iframe`), «Скачать акт» активна, кнопка
  «Загрузить» отрисована с размером 0×0, кнопки очистки нет;
- `/container-contracts/212` открывается, компоновка та же, кнопки на 712;
- вкладка «Файлы» карточки: без выбора грид 1046 px и скрытая панель, при
  выборе строки с файлом — 516 + 514 с `iframe`.

## Ошибки и непроверенное

Подтверждённая ошибка одна — [F01.1](../fixes/F01.md): в изменённом файле
`ContainerSurveyDetailView.java` остаётся javadoc с формулировкой «по решению
пользователя», запрещённой разделом протокола «Комментарии в коде». Поведение
кода она не меняет, все критерии C1–C4 подтверждены.

Непроверенных обязательных сценариев нет: подраздел «Независимая проверка»
выполнен целиком — C1–C3 в браузере под `container-edit`, открытие под
`container-read`, `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"`.

Наблюдение вне области T13 (в файл исправления не выносится): та же формулировка
и ссылки на таски есть в `ContainerSurveyIT.java`, `ContainerActListView.java`,
`ContainerActFragment.java`, `ContainerContractListView.java`,
`ContainerWarrantyListView.java` и `ContainerRolesTest.java` — эти файлы T13 не
менял, их уместно вычистить в T15 или отдельным проходом чистки комментариев.

Состояние окружения после проверки: `bootRun` погашен, порт 8082 свободен,
браузерные сессии закрыты, `git status --short` в worktree совпадает с
состоянием до проверки. Заведённые проверкой данные удалены: `cnt_container`,
`cnt_container_file`, `cnt_survey` и `cnt_warranty` снова пусты, договор `212`
возвращён к исходному виду (номер и даты `null`, `version = 1`,
`last_modified_*` пустые), 166 строк `audit_entity_log`, созданных прогоном
тестов и браузерным проходом, удалены. Два PDF остались в MinIO без ссылок из
БД, как и после прошлых проходов.

## Следующий шаг

Исполнитель запускает `task-execute` по [fixes/F01.md](../fixes/F01.md) —
правка одного абзаца javadoc, после чего передаёт итерацию 2 на повторную
проверку. БД, порт 8082, gradle daemon, браузер и MinIO свободны. После `done`
T13 остаётся сквозной [T15](../../T15/task.md), затем `task-close` по всей
работе.
