# Проверка 002 — T13

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 2
Дата: 2026-09-16 11:47–12:15 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `1ad658d3f`, 15 незакоммиченных файлов (7 файлов T13 плюс принятые T11, T12, `IframeSecurity.java` из T14 и общий `messages_ru.properties`). Из семи файлов T13 итерация 2 изменила один — `ContainerSurveyDetailView.java` (mtime 11:47:11, позже проверки 001, шедшей 11:20–11:45); у остальных шести mtime 11:11–11:12, то есть они байт в байт те же, что проверялись в [checks/001.md](001.md). `git status --short` до и после проверки совпадает, код продукта и репозиторные тесты проверкой не менялись
Окружение: схема `main_rvk_ws` в общем Docker PostgreSQL (`docker-rvk-db-1`), MinIO и Keycloak из `docker/docker-compose.yml`, приложение `bootRun` на порту 8082 — поднято и остановлено проверяющим (pid 541325), готовность ловилась кодом `200` на `/local-login`; браузер `playwright-cli` (Chrome, две сессии: `t13v2` — `user_edit`, `t13r2` — `user_read`), разрешение 1366×768. Порядок по plan.md: T11, T12, T14 `done`, T13 идёт следующей, T15 в статусе `todo` и на ресурсах не работает. Порт 8082, схема и gradle daemon заняты только этой проверкой: `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` отработал до старта `bootRun`, браузерный проход — после него, `spotlessCheckAll` — между ними. Порт 8083 параллельно занимало приложение соседнего worktree `serene-basin` (чужая работа); его процесс проверкой не затронут, контейнерных таблиц оно не трогает
Итог: pass — замечание F01.1 исправлено, критерии C1–C4 и открытие под `container-read` подтверждены заново

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| F01.1 (комментарии в коде) | исправлен | Раздел 1: поиск по семи изменённым файлам T13 с нормализацией переносов строк — 0 совпадений по `\b[TFQC]\d{1,}\b`, `specs/work/`, «решени\w* пользовател\w*», «проверяющ», «итераци», «по плану», `AGENTKIT`; новый абзац javadoc `ContainerSurveyDetailView` читается без документов работы |
| C1 | подтверждён | Разделы 3–5: `/container-surveys/new` без акта — `#displayFile` в DOM отсутствует, картинок 0, «Скачать акт» `disabled`, форма слева 640 px, `detailActions` на y=708 при высоте окна 768; после загрузки PDF панель появилась справа (x=819, 531×619, `iframe`), кнопка активна; очистка файла снова прячет панель (`display: none`, 0×0) и гасит кнопку; из вкладки «Освидетельствования» карточки — та же форма диалогом, «Контейнер» `readonly` со значением `T13VER0002` (регрессия T04); сохранённая запись `240` с актом открывается с видимой панелью |
| C2 | подтверждён | Раздел 6: без выбора грид 1046 px на всю вкладку, панель `hidden`; после «Создать» + `refresh()` выбора нет и панель осталась скрытой; выбор строки с PDF — грид 516 + панель 514 с `iframe`; повторный клик снимает выбор — снова 1046 px и скрытая панель; строка без файла (вставлена SQL-ом) — панель скрыта, битых картинок 0; «Скачать» отдал тот же PDF (`200`, `application/pdf`, `filename="t13-check002.pdf"`, 389 байт) — регрессия T03 |
| C3 | подтверждён | Раздел 7: `/container-contracts/212` с заполненным номером и непустым гридом условий при 1366×768 — `contentScroller` 619 px прокручивает содержимое 739 px, «Сохранить / OK / Отмена» на y=712, `elementFromPoint` в центре каждой кнопки возвращает саму кнопку; после прокрутки вниз (`scrollTop` = 120) грид виден целиком (292–692), кнопки остались на 708–752; у нового договора — подсказка вместо грида, `warrantiesSection` и `warrantiesDataGrid` в DOM отсутствуют (регрессия T06) |
| C4 | подтверждён | Разделы 1, 2 и 8: в трёх XML нет `split`, `css=`, `<details>`, `splitterPosition`, `displayNotFoundFile`; `classNames` только `buttons-panel` и `text-secondary`; все 10 `msg://` резолвятся в `messages_ru.properties`; `document.body.innerText.includes('msg://')` = `false` на всех проверенных экранах; `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` — BUILD SUCCESSFUL, 68 passing, 0 failures, включая `ContainerSurveyIT` (2) и `ContainerContractWarrantyIT` (2); `spotlessCheckAll` — BUILD SUCCESSFUL |
| Роль `container-read` | подтверждён | Раздел 9: под `user_read` открываются `/container-surveys/240` (семь полей формы `readonly`, панель просмотра видна с `iframe`, «Скачать акт» активна, кнопка загрузки 0×0), `/container-contracts/212` (та же компоновка, содержимое 739 px в scroller-е 619 px, кнопки на 708) и вкладка «Файлы» карточки (без выбора грид 1046 px и скрытая панель, с выбором строки с файлом — 516 + 514 с `iframe`); сырых `msg://` нет |

## Выполненные проверки

### 1. Комментарии в коде — воспроизведение F01.1

```bash
cd ../worktrees/rvk-ws/cyan-fennel/rvk-ws
python3 - <<'PY'
import re
files=['app/src/main/java/ru/fgk/ws/app/container/view/containersurvey/ContainerSurveyDetailView.java',
'app/src/main/java/ru/fgk/ws/app/container/view/fragment/containerfile/ContainerFileFragment.java',
'app/src/main/resources/ru/fgk/ws/app/container/view/containersurvey/container-survey-detail-view.xml',
'app/src/main/resources/ru/fgk/ws/app/container/view/fragment/containerfile/container-file-fragment.xml',
'app/src/main/resources/ru/fgk/ws/app/container/view/containercontract/container-contract-detail-view.xml',
'app/src/test/java/ru/fgk/ws/app/container/view/ContainerSurveyViewsUiTest.java',
'app/src/test/java/ru/fgk/ws/app/container/view/ContainerContractViewsUiTest.java']
pats=[r'\b[TFQC]\d{1,}\b', r'specs/work/', r'решени\w*\s+пользовател\w*', r'проверяющ',
      r'итераци', r'по плану', r'AGENTKIT']
hits=0
for f in files:
    n=re.sub(r'\n\s*\*\s*',' ',open(f,encoding='utf-8').read()); n=re.sub(r'\s+',' ',n)
    for p in pats:
        for m in re.finditer(p,n,re.I):
            hits+=1; print(f,'::',p,'::',n[max(0,m.start()-90):m.end()+60])
print('TOTAL HITS:',hits)
PY
# TOTAL HITS: 0
```

Абзац, на который указывало [F01.1](../fixes/F01.md), теперь читается так:

```java
 * <p>Никакой логики вокруг {@code nextSurveyDate} контейнера здесь нет и быть не должно: дата
 * следующего освидетельствования в реестре ведётся вручную и от этих записей не зависит.
```

Ссылки на решение пользователя нет, объяснение «почему» сохранено и проверяется
по поведению формы: контроллер `nextSurveyDate` не трогает, а поле «Следующее
освидетельствование» реестра правится руками.

`git diff` изменённого файла показывает ровно две правки относительно `HEAD`:
переписанные абзацы javadoc и метод `updateFilePreview()`, который скрывает и
показывает фрагмент вместо `displayNotFoundFile`, — то есть код итерации 2
совпадает с тем, что проверялось в [checks/001.md](001.md), а новой правкой стал
только текст комментария.

### 2. Статические проверки

```bash
./gradlew :app:test --tests "ru.fgk.ws.app.container.*"   # BUILD SUCCESSFUL in 1m 3s, 68 passing
./gradlew spotlessCheckAll                                # BUILD SUCCESSFUL in 1s
```

Разбор `app/build/test-results/test/*.xml`: 21 класс, 68 тестов, 0 failures,
0 errors, 0 skipped, в том числе `ContainerSurveyIT` (2),
`ContainerContractWarrantyIT` (2), `ContainerSurveyViewsUiTest` (4) и
`ContainerContractViewsUiTest` (5).

Механические проверки трёх XML: все разбираются `xml.dom.minidom`; 0 вхождений
`css=`, `<split`, `splitterPosition`, `<details`, `displayNotFoundFile`;
`classNames` только `buttons-panel` и `text-secondary`; 10 адресов `msg://`
резолвятся в `messages_ru.properties` (ключи там записаны с префиксом пакета);
`containerSurveyDetailView.noFile` и `containerFileFragment.noFile` в
`messages_ru.properties` отсутствуют, ссылок на них в `app/src`, `core` и
`sso-plagin` нет.

### 3. C1 — освидетельствование без акта

`/container-surveys/new` под `user_edit`, 1366×768:

```json
{"surveyBox":{"x":272,"y":73,"w":1078,"h":619},
 "formScroller":{"x":272,"y":73,"w":640,"h":619},
 "displayFile":"absent-in-dom",
 "detailActions":{"x":272,"y":708,"w":347,"h":44},
 "download":{"disabled":true,"text":"Скачать акт"},
 "imgs":[],"rawMsg":false,"innerH":768,"innerW":1366}
```

### 4. C1 — загрузка и очистка акта

Загружен PDF 389 байт, сгенерированный в каталоге сессии. Сразу после загрузки,
до сохранения записи:

```json
{"formScroller":{"x":272,"w":531,"h":619},
 "displayFile":{"x":819,"y":73,"w":531,"h":619},"iframe":true,
 "detailActions":{"y":708},"download":{"disabled":false},"imgs":0}
```

После «Remove file» у поля загрузки:
`{"formScroller":{"w":640},"displayFile":{"hidden":true,"display":"none","w":0,
"h":0},"download":{"disabled":true},"imgs":0}` — панель исчезает, форма снова
занимает 640 px.

### 5. C1 — вторая точка входа и сохранённая запись

Через UI заведён контейнер `T13VER0002` (`id = 1742`); на вкладке
«Освидетельствования» карточки «Создать» открывает форму диалогом:
`surveyBox` 1120×486 на y=140, `#displayFile` отсутствует, «Скачать акт»
`disabled`, `containerField` `readonly` со значением `T13VER0002` (регрессия
T04). После загрузки PDF панель появилась (x=691, 552×486), «OK» сохранил
запись — в БД `cnt_survey 240 | container 1742 | s3://cnt_file/2026/09/16/
83545d20-….pdf`.

Повторное открытие `/container-surveys/240`:

```json
{"displayFile":{"x":819,"y":73,"w":531,"h":619},"iframe":true,
 "formScroller":{"w":531},"scroll":{"sh":619,"ch":619},
 "download":{"disabled":false},"detailActions":{"y":708},
 "containerReadonly":false,"imgs":0,"rawMsg":false}
```

Панель видна сразу при открытии, из самостоятельного списка поле «Контейнер»
редактируемо — как и было до T13.

### 6. C2 — вкладка «Файлы»

Все замеры — на вкладке «Файлы» карточки `/containers/1742`; идентификаторов у
элементов фрагмента в DOM нет, поэтому строка ищется структурно (`vaadin-grid` и
его родитель `vaadin-horizontal-layout`).

- пустая вкладка, выбора нет: `grid 1046×523`, второй ребёнок строки
  `hidden: true, display: none`, картинок 0;
- «Создать» → загрузка PDF → «OK»: строка появилась (`cnt_container_file 104`),
  после `refresh()` `selectedItems.length` = 0 и панель по-прежнему скрыта,
  грид 1046 px;
- выбор строки с PDF: `grid 516×523` + панель `514×523` с `iframe`;
- повторный клик по выбранной строке снимает выбор — панель `hidden`, грид снова
  1046 px;
- строка без файла (вставлена напрямую в `cnt_container_file`, чтобы обойти
  обязательность файла в форме): выбрана (`selectedItems.length` = 1, ключ строки
  с комментарием «без файла — проверка 002»), панель скрыта, грид 1046 px, битых
  картинок 0;
- «Скачать» при выбранной строке с файлом открыл ресурс Vaadin во второй
  вкладке; `fetch` того же адреса из сессии страницы дал `HTTP 200`,
  `Content-Type: application/pdf;charset=UTF-8`,
  `Content-Disposition: inline; filename="t13-check002.pdf"`, 389 байт — файл
  совпадает с загруженным.

### 7. C3 — карточка договора

`/container-contracts/212` под `user_edit`, 1366×768. Договору задан номер
`T13-CHK2-2026` и сохранён; через «Создать» в секции условий заведено
гарантийное условие (`cnt_warranty 308`, срок 12, осмотр 30 дней), грид стал
непустым.

```json
{"contentScroller":{"x":272,"y":73,"w":1078,"h":619},
 "scroll":{"sh":739,"ch":619},
 "form":{"y":73,"h":231},"warrantiesSection":{"y":320,"h":492},
 "grid":{"y":412,"h":400},"hint":"absent-in-dom",
 "detailActions":{"x":272,"y":708,"w":363,"h":44},
 "buttons":[{"id":"saveButton","y":712,"hit":"saveButton"},
            {"id":"saveAndCloseButton","y":712,"hit":"saveAndCloseButton"},
            {"id":"closeButton","y":712,"hit":"closeButton"}],
 "rawMsg":false,"innerH":768}
```

Содержимое прокручивается внутри `contentScroller` (739 против 619), кнопки
лежат ниже его границы (692) и hit-тестируются сами собой — полями они не
перекрыты. После прокрутки в самый низ (`scrollTop` = 120) грид виден целиком
(292–692), панель кнопок осталась на 708–752.

Новый договор `/container-contracts/new`: `warrantiesSection` и
`warrantiesDataGrid` в DOM отсутствуют, `warrantiesHint` виден (607×26) с
текстом «Сохраните договор — после этого к нему можно добавлять гарантийные
условия», содержимое короче области прокрутки (`scrollHeight` = `clientHeight` =
619), кнопки на 708 — поведение T06 сохранено.

### 8. Сырые `msg://` и консоль

`document.body.innerText.includes('msg://')` = `false` на
`/container-surveys/new`, `/container-surveys/240`, вкладках «Файлы» и
«Освидетельствования» карточки `/containers/1742`, `/container-contracts/212` и
`/container-contracts/new`, в обеих ролях. Единственная ошибка консоли за обе
сессии — недоступный вне корпоративной сети аватар:
`Failed to load resource: net::ERR_NAME_NOT_RESOLVED @
https://info.main.vgk/api2/v1/phone-book/avatar/user_edit` (и `…/user_read`).

### 9. Роль `container-read`

Отдельная сессия под `user_read` (пароль `read123` из
[T04/result.md](../../T04/result.md)):

- `/container-surveys/240` открывается, все семь полей формы `readonly`, панель
  просмотра видна (531×619, `iframe`), «Скачать акт» активна, кнопка
  «Upload Files…» отрисована с размером 0×0, кнопки очистки нет;
- `/container-contracts/212` открывается, компоновка та же (`contentScroller`
  619 px, содержимое 739 px, грид на y=412, кнопки на 708);
- вкладка «Файлы» карточки: без выбора грид 1046 px и скрытая панель, при выборе
  строки с файлом — 516 + 514 с `iframe`, картинок 0.

## Ошибки и непроверенное

Ошибок нет. Подраздел «Независимая проверка» выполнен целиком: C1–C3 в браузере
под `container-edit`, открытие под `container-read`,
`./gradlew :app:test --tests "ru.fgk.ws.app.container.*"`. Непроверенных
обязательных сценариев нет.

Наблюдение вне области T13 (в файл исправления не выносится): пока у договора
пуст `contractNum` — а это его `@InstanceName`, — форма гарантийного условия,
открытая кнопкой «Создать», показывает пустое поле «Гарантия к договору» и не
сохраняется с ошибкой «Заполните поле». Как только у договора появился номер,
поле заполнено и сохранение проходит. Разметку T13 это не затрагивает (в
контроллере договора инициализатор `warranty.setContract(contract)` не менялся),
относится к формам T06 и уместно разобрать в T15 или отдельной работе.

Прежнее наблюдение проверки 001 остаётся в силе: формулировки «по решению
пользователя» и ссылки вида `T04`, `T06`, `T07`, `Q04` есть в
`ContainerSurveyIT.java`, `ContainerActListView.java`, `ContainerActFragment.java`,
`ContainerContractListView.java`, `ContainerWarrantyListView.java` и
`ContainerRolesTest.java` — эти файлы T13 не менял.

Состояние окружения после проверки: `bootRun` погашен, порт 8082 свободен,
браузерные сессии закрыты, приложение соседнего worktree на порту 8083 не
затронуто, `git status --short` в worktree совпадает с состоянием до проверки.
Заведённые проверкой данные удалены: `cnt_container`, `cnt_container_file`,
`cnt_survey` и `cnt_warranty` снова пусты, договор `212` возвращён к исходному
виду (номер `null`, `version = 1`, `last_modified_*` пустые), 165 строк
`audit_entity_log`, созданных прогоном тестов и браузерным проходом, удалены. Два
PDF остались в MinIO без ссылок из БД, как и после прошлых проходов.

## Повторная проверка

Замечание [F01.1](../fixes/F01.md) подтверждённо исправлено (раздел 1), других
открытых замечаний нет. Критерии C1–C4 и открытие под `container-read` сняты
заново в полном объёме, а не по прежним доказательствам: единственный изменённый
итерацией 2 файл — контроллер формы освидетельствования, и все его сценарии
пройдены в браузере на итерации 2. Доказательства [checks/001.md](001.md)
использованы только как эталон для сверки чисел; шесть остальных файлов T13
байт в байт те же (mtime 11:11–11:12 против окна проверки 001 11:20–11:45), и
измеренные значения на итерации 2 совпали с ними до пикселя.

## Следующий шаг

T13 переведён в `done`, [fixes/F01.md](../fixes/F01.md) — в `done`. Дальше по
очереди plan.md — сквозной [T15](../../T15/task.md) (`todo`, зависит от T11,
T12, T13, T14; все предшественники теперь `done`): исполнитель запускает
`task-execute` по `specs/work/conteiner/tasks/T15/task.md`. БД, порт 8082,
gradle daemon, браузер и MinIO свободны. После `done` T15 — `task-close` по всей
работе.
