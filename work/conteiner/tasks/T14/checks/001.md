# Проверка 001 — T14

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-16 09:47–09:54 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `1ad658d3f`, незакоммиченные изменения только в `app/src/main/java/ru/fgk/ws/app/security/IframeSecurity.java`
Окружение: Docker PostgreSQL (`localhost:5432`, схема `main_rvk_ws`), MinIO, приложение `bootRun` на порту 8082 (поднято и остановлено проверяющим), Playwright CLI (Chromium). Проверка первая в очереди пересмотра по plan.md, изоляция соблюдена.
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | подтверждён | Анонимные запросы `curl -sI` на `/img/filetype/fileNotFound.png` и `docFileType.png` возвращают `HTTP/1.1 200` и `Content-Type: image/png`; аналогично для `xlsFileType.png`, `htmlFileType.png`, `otherFileType.png` |
| C2 | подтверждён | В браузере Playwright CLI под вошедшим пользователем в форме освидетельствования (`DisplayFile`) загружен файл `.docx`: компонент отобразил `docFileType.png` (`naturalWidth: 512`, `complete: true`), запрос #29 ответил кодом `200`, ошибок в консоли нет; скриншот `.playwright-cli/t14-verify-docfiletype.png` |
| C3 | подтверждён | Анонимный `/local-login` отвечает `200`, защищённый `/containers` возвращает `302` на Keycloak; авторизованный вход через `/local-login` успешен; тесты `ContainerRolesTest` (8 из 8) и весь контейнерный сьют (64 из 64) зелёные |

## Выполненные проверки

### 1. Статические проверки и комментарии в коде

- Проверено форматирование кода:
  ```bash
  ./gradlew -p /home/mindils/data/dev/fgk/worktrees/rvk-ws/cyan-fennel/rvk-ws :app:spotlessCheck
  ```
  Результат: `BUILD SUCCESSFUL`.
- Компиляция Java:
  ```bash
  ./gradlew -p /home/mindils/data/dev/fgk/worktrees/rvk-ws/cyan-fennel/rvk-ws :app:compileJava
  ```
  Результат: `BUILD SUCCESSFUL`.
- Проверены комментарии в изменённом файле `IframeSecurity.java` по правилу
  протокола «Комментарии в коде»: запрещённых маркеров (`\b[TFQC]\d{1,}\b`,
  `specs/work/`, «решение пользователя», «проверяющ», «итераци») нет,
  комментарий поясняет техническую причину разрешения пути `/img/**`.

### 2. Автоматические тесты (Gate 2)

- Запущены тесты безопасности контейнерного пакета:
  ```bash
  ./gradlew -p /home/mindils/data/dev/fgk/worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests "ru.fgk.ws.app.container.security.*"
  ```
  Результат: `8 passing (19.8s), 0 failing`.
- Запущен полный набор контейнерных тестов:
  ```bash
  ./gradlew -p /home/mindils/data/dev/fgk/worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests "ru.fgk.ws.app.container.*"
  ```
  Результат: `64 passing (44.2s), 0 failing`.

### 3. Проверка статики и аутентификации через curl (C1, C3)

Приложение запущено через `bootRun` на порту 8082.
- Анонимные запросы `curl -sI`:
  - `http://localhost:8082/img/filetype/fileNotFound.png` -> `HTTP/1.1 200`, `Content-Type: image/png`
  - `http://localhost:8082/img/filetype/docFileType.png` -> `HTTP/1.1 200`, `Content-Type: image/png`
  - `http://localhost:8082/img/filetype/xlsFileType.png` -> `HTTP/1.1 200`, `Content-Type: image/png`
  - `http://localhost:8082/img/filetype/htmlFileType.png` -> `HTTP/1.1 200`, `Content-Type: image/png`
  - `http://localhost:8082/img/filetype/otherFileType.png` -> `HTTP/1.1 200`, `Content-Type: image/png`
- Проверка защищённости закрытых ресурсов:
  - `http://localhost:8082/containers` -> `HTTP/1.1 302`, `Location: http://localhost:8082/oauth2/authorization/keycloak`
- Проверка доступности формы локального входа:
  - `http://localhost:8082/local-login` -> `HTTP/1.1 200`

### 4. Браузерный проход Playwright CLI (C2)

- В браузере открыта страница `http://localhost:8082/local-login`, выполнен
  успешный вход под учётной записью `admin` / `admin`.
- Выполнен переход на форму нового освидетельствования
  (`http://localhost:8082/container-surveys/new`).
- Через `fileStorageUploadField` загружен тестовый файл `test_document.docx`.
- Компонент `DisplayFile` отобразил иконку `docFileType.png`:
  - В DOM `<img>` имеет атрибуты `src="http://localhost:8082/img/filetype/docFileType.png"`,
    `naturalWidth: 512`, `naturalHeight: 512`, `complete: true`.
  - Сетевой запрос Playwright #29 на `http://localhost:8082/img/filetype/docFileType.png`
    завершился со статусом `200` (`image/png`, размер 12 689 байт).
  - В консоли браузера отсутствуют ошибки загрузки ресурсов (только сетевая
    ошибка корпоративного аватара `https://info.main.vgk/api2/v1/phone-book/avatar/admin`,
    ожидаемая вне контура).
- Сохранён скриншот `.playwright-cli/t14-verify-docfiletype.png`.
- Процесс `bootRun` остановлен, порт 8082 освобождён, временные тестовые записи
  в БД и временные файлы удалены.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C3 подтверждены.

## Повторная проверка

Не требуется, проверка пройдена с первой итерации.

## Следующий шаг

Таск T14 переведён в статус `done`. Согласно plan.md (раздел «Как выполнять»,
волна 5 пересмотра UI), следующей выполняется независимая проверка таска
[T11](../T11/task.md) («Карточка контейнера: Описание первой вкладкой,
неактивные вкладки у нового, кнопки внизу»).
