# Проверка 001 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 02:22
Состояние кода: commit 3c92665f15faaec5bfbb1902dca12c1a2c47a013, ветка f/container (worktree cyan-fennel/rvk-ws)
Окружение: Linux, PostgreSQL 16 (схема main_rvk_ws), Docker compose, JDK 21, Gradle 9.5.1
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | SQL-осмотр схемы: 22 таблицы `cnt_*`, 57 индексов, 0 FK; 0 legacy-полей |
| C2 | Подтверждён | `ContainerCrudIT`: создание, обновление, soft delete для 9 сущностей и НСИ |
| C3 | Подтверждён | `ContainerNumberIT`, `T01IndependentVerificationIT`: trim, uppercase, DENY дубля |
| C4 | Подтверждён | `ContainerDeletePolicyIT`, `T01IndependentVerificationIT`: DENY + soft delete |
| C5 | Подтверждён | `EntityCopySupportIT`: копия без id/версии/аудита/FileRef/номера |
| C6 | Подтверждён | `ContainerNsiReferenceIT`: сохранение и чтение `VStation` и `VOrgPassport` |

## Выполненные проверки

1. **Компиляция Java и модели.** Команда
   `./gradlew -p .../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:compileJava`
   выполнена успешно (код 0).
2. **Сверка схемы БД и миграций.** Запросами к `information_schema` и
   `pg_indexes` проверено:
   - В схеме `main_rvk_ws` созданы ровно 22 таблицы `cnt_*`.
   - 35 вторичных индексов и 22 первичных ключа (всего 57 индексов в БД);
     все `@Index` в коде аннотаций entity совпали с `<createIndex>` в
     changelog-ах.
   - Внешние ключи на уровне СУБД отсутствуют (0 строк по `pg_constraint`).
   - Колонки `data_id`, `trans_id`, `lastrec`, `recdatebegin`, `recdateend`,
     `recdatenew`, `user_name` отсутствуют во всех таблицах.
   - Все changeSet-ы changelog-ов содержат author `cnt`,
     `objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS"`, а все changeSet-ы
     создания таблиц/индексов защищены `preConditions onFail="MARK_RAN"`.
3. **Целевой тестовый набор T01.** Команда
   `./gradlew -p .../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:test --tests "ru.fgk.ws.app.container.*"`
   прошла успешно: 13 тестов из 5 тестовых классов (`ContainerCrudIT`,
   `ContainerDeletePolicyIT`, `ContainerNsiReferenceIT`, `ContainerNumberIT`,
   `EntityCopySupportIT`) выполнены без ошибок за 22.3 с.
4. **Независимый проверочный сценарий C3–C4 и messages.** Создан и запущен
   автономный тестовый класс `T01IndependentVerificationIT`:
   - Сценарий C3 подтвердил нормализацию номера (` fgku... ` -> `FGKU...`),
     валидацию пустого номера и блокировку повтора номера у живого и
     удалённого контейнера.
   - Сценарий C4 подтвердил блокировку удаления контейнера при наличии детей
     (характеристики, ремонта, связи акта), снятие блокировки после мягкого
     удаления детей, а также блокировку удаления модели и договора при
     наличии ссылающихся записей.
   - Проверка русской локализации подтвердила наличие непустых понятных
     подписей для всех 22 entity и каждого их атрибута через `MessageTools`.
   После выполнения временный проверочный файл удалён.
5. **Полный репозиторный прогон `:app:test`.** Запуск
   `./gradlew :app:test` показал 1230 успешных тестов. 48 падений зафиксированы
   в старых интеграционных тестах модулей `dr` и `pt` (`DiadocPacketCorrectionIT`,
   `DrOperRepairTr2ParserServiceIT` и др.), обусловленных жестко зашитым в их
   исходном коде префиксом схемы `main.` в JDBC SQL при запуске в тестовой
   схеме worktree `main_rvk_ws` (известное системное ограничение, описанное в
   `README-worktree.md` и `Q03.md`). Код контейнерного раздела полностью
   изолирован и не внес регрессий.

## Ошибки и непроверенное

Ошибок в коде T01 не обнаружено. Все критерии C1–C6 полностью подтверждены.

## Повторная проверка

Первая проверка реализации, повторная проверка не требовалась.

## Следующий шаг

Таск T01 переведён в статус `done`. Следующий шаг — выполнение таска T02
([tasks/T02/task.md](../T02/task.md)) или параллельного T09
([tasks/T09/task.md](../T09/task.md)) исполнителем `task-execute`.
