# Проверка 002 — T01

Таск: [../task.md](../task.md)
Результат реализации: [../result.md](../result.md)
Итерация: 2
Дата: 2026-09-15 15:28
Состояние кода: commit 3c92665f15faaec5bfbb1902dca12c1a2c47a013, ветка f/container (worktree cyan-fennel/rvk-ws), правка tbl-pt_repair_packet.xml
Окружение: Linux, PostgreSQL 16 (схемы main_rvk_ws, main и временная main_verify_t01), Docker Compose, JDK 21, Gradle 9.5.1
Итог: pass — миграционная часть C1 на чистой схеме и схеме main подтверждена, C2–C6 актуальны по checks/001.md

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Чистая схема `main_verify_t01`: `jmix_Liquibase` успешно выполнился с нуля; колонка `pack_checked_code integer` создана с комментарием в `pt_repair_packet`; changeSet 23 получил MARK_RAN; тестовые данные (6 строк) загружены; повторный прогон с `--rerun-tasks` идемпотентен; на схеме `main` changeSet 1 получил RERAN, changeSet 23 — MARK_RAN; на схеме `main_rvk_ws` changeSet 23 EXECUTED. 22 таблицы `cnt_*` созданы, 0 FK, 0 legacy-полей. |
| C2 | Подтверждён | `ContainerCrudIT` (2 теста) выполнен успешно; актуально по отчёту checks/001.md, код entity не менялся |
| C3 | Подтверждён | `ContainerNumberIT` (3 теста) выполнен успешно на чистой схеме `main_verify_t01`, схеме `main` и схеме `main_rvk_ws` |
| C4 | Подтверждён | `ContainerDeletePolicyIT` (4 теста) выполнен успешно; актуально по отчёту checks/001.md, код entity не менялся |
| C5 | Подтверждён | `EntityCopySupportIT` (2 теста) выполнен успешно; актуально по отчёту checks/001.md, код сервиса не менялся |
| C6 | Подтверждён | `ContainerNsiReferenceIT` (2 теста) выполнен успешно; актуально по отчёту checks/001.md, код entity не менялся |

## Выполненные проверки

1. **Компиляция Java и модели.** Команда
   `./gradlew -p .../worktrees/rvk-ws/cyan-fennel/rvk-ws :app:compileJava`
   выполнена успешно (код 0).
2. **Проверка C1 на чистой схеме БД (`main_verify_t01`).**
   - Создана пустая схема `main_verify_t01` в локальном Docker PostgreSQL.
   - Запущен тест
     `./gradlew -p ... :app:test --tests "ru.fgk.ws.app.container.ContainerNumberIT"`:
     контекст Spring/Jmix успешно поднялся, бин `jmix_Liquibase` отработал без
     ошибок, 3 теста завершились зелёными (код 0).
   - Повторный запуск с `--rerun-tasks` на той же схеме: 3 теста успешно
     пройдены, повторный накат миграций идемпотентен.
   - SQL-осмотр схемы `main_verify_t01`:
     - Колонка `pack_checked_code` (тип `integer`) создана в `pt_repair_packet`,
       колонка `status` отсутствует.
     - Комментарий колонки: «Контроль пакета документов. Код обработки (0 -
       норма, 1 - нарушение, 2 - предупреждение)».
     - В таблице `main_verify_t01.databasechangelog` changeSet 1 имеет статус
       `EXECUTED`, changeSet 3, 16, 17, 22 и 23 — `MARK_RAN`.
     - Таблица `pt_repair_packet` содержит 6 записей тестового changelog-а, у
       всех 6 колонка `pack_checked_code` заполнена.
     - Созданы ровно 22 таблицы `cnt_*`.
   - Временная схема `main_verify_t01` удалена после проверки.
3. **Проверка поведения на схеме `main`.**
   - Запущен тест
     `./gradlew -p ... :app:test --tests "ru.fgk.ws.app.container.ContainerNumberIT"`
     с `currentSchema=main`: контекст поднялся, тесты зелёные.
   - Запрос к `main.databasechangelog` подтвердил: changeSet 1 перезапущен
     (exectype `RERAN`), changeSet 23 получил `MARK_RAN` (колонка уже
     существовала). Схема `main` осталась в целостном состоянии.
4. **Проверка на схеме worktree (`main_rvk_ws`).**
   - Запущен целевой тестовый набор T01 (5 классов, 13 тестов):
     `ContainerCrudIT`, `ContainerDeletePolicyIT`, `ContainerNsiReferenceIT`,
     `ContainerNumberIT`, `EntityCopySupportIT`. Все 13 тестов пройдены за 39 с.
   - Запущен полный набор контейнерных тестов (15 классов, 64 теста): все 64
     теста пройдены за 1 мин 7 с.
   - SQL-осмотр схемы `main_rvk_ws`: 22 таблицы `cnt_*`, 57 индексов (35
     вторичных + 22 PK), 0 FK, 0 устаревших технических колонок.
   - Скриптом проверена полнота русских подписей в `messages_ru.properties` для
     всех 22 entity пакета `ru.fgk.ws.app.container.entity`: 0 отсутствующих
     ключей.
5. **Полный репозиторный прогон `:app:test`.** Запуск
   `./gradlew -p ... :app:test --continue` выполнил 1343 теста: 1282 успешных, 12
   пропущенных, 49 падений. Все 49 падений относятся к 11 старым классам
   модулей `dr` и `pt` (`DiadocPacketCorrectionIT`,
   `DrOperRepairTr2ParserServiceIT` и др.), обусловленным жестко зашитым в них
   префиксом схемы `main.` при запуске в тестовой схеме worktree `main_rvk_ws`.
   Ни одного падения в коде контейнерного раздела нет.

## Ошибки и непроверенное

Ошибок не обнаружено. Все условия по итерации 2 и ответу Q03 подтверждены.

## Повторная проверка

- Замечание Q03 (падение `jmix_Liquibase` на чистой схеме из-за отсутствия
  колонки `pack_checked_code` в `pt_repair_packet`) полностью устранено: на
  чистой схеме колонка заводится через `createTable`, а на существующих схемах
  — через changeSet 23 с `addColumn` и preCondition `onFail="MARK_RAN"`.
- Критерии C2–C6 подтверждены отчётом checks/001.md и верифицированы повторным
  прогоном тестового набора T01.

## Следующий шаг

Таск T01 переведён в статус `done`. Все таски работы завершены.
Следующий шаг — выполнение `task-close` для закрытия всей работы
«Реестр, ремонты и освидетельствования контейнеров» и подготовки summary.md.
