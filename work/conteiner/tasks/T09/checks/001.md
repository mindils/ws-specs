# Проверка 001 — T09

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 04:05
Состояние кода: commit 3c92665f1 (ветка f/container, worktree cyan-fennel/rvk-ws)
Окружение: Linux, PostgreSQL 16 (схема main_rvk_ws), Docker compose (docker-rvk-db-1), JDK 21, Gradle 9.5.1, bootRun порт 8082, Playwright CLI
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | `ContainerEntityLogIT`: в `audit_entity_log` зафиксированы CREATE, MODIFY (старое и новое значения), DELETE |
| C2 | Подтверждён | `ContainerEntityLogIT`: справочник `CntModel` и ремонт `ContainerRepair` журналируются, ссылка сохранена именем |
| C3 | Подтверждён | `EntityLogFragmentUiTest`: фрагмент фильтрует по экземпляру, сортирует новее сверху; выбор строки дает атрибуты |
| C4 | Подтверждён | `ContainerEntityLogIT`: переименование `CntRepairType` после события не затрагивает сохранённые строки журнала |
| C5 | Подтверждён | SQL-прогон и `ContainerEntityLogIT`: ровно 22 entity, 116 атрибутов, 0 дублей; повторный INSERT дает `INSERT 0 0` |

## Выполненные проверки

1. **Компиляция Java и тестов, проверка форматирования.**
   В worktree `cyan-fennel/rvk-ws` выполнены команды
   `./gradlew :app:compileJava :app:compileTestJava` и `./gradlew spotlessCheck`.
   Компиляция и статический анализ завершены с кодом 0.

2. **Сверка схемы и конфигурации Liquibase в СУБД.**
   Через psql в схеме `main_rvk_ws` выполнены запросы к `audit_logged_entity` и
   `audit_logged_attr`:
   - Настроены все 22 контейнерные сущности с `auto_ = true` и `manual_ = true`.
   - Настроено 116 бизнес-атрибутов (системные атрибуты и вычисляемые
     `@JmixProperty` исключены согласно контракту).
   - Дублей в таблицах конфигурации аудита нет (`count(*) HAVING count(*) > 1`
     равен 0).
   - Повторное применение SQL-скрипта миграции в транзакции подтвердило
     идемпотентность: вернуло `INSERT 0 0`.

3. **Автоматизированные тесты.**
   - Целевой набор T09:
     `./gradlew :app:test --tests "ru.fgk.ws.app.container.ContainerEntityLogIT" --tests "ru.fgk.ws.app.container.view.fragment.EntityLogFragmentUiTest"`
     выполнил 7 тестов (4 интеграционных, 3 UI-интеграционных), все успешно
     пройдены.
   - Полный набор контейнерного раздела:
     `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"`
     выполнил 26 тестов (T01, T02, T09) без единой ошибки за 42.4 с.
   - Полный репозиторный прогон `:app:test` выполнил 1303 теста (1242 pass,
     12 skip, 49 fail). Все 49 падений относятся к легаси-модулям `dr` и `pt`
     с жестко зашитой схемой `main.` в JDBC URL (известное ограничение схемы
     worktree `main_rvk_ws`, аналогично отчётам T01 и T02). Код контейнерного
     раздела не вызвал регрессий.

4. **Интерактивная браузерная проверка в Playwright CLI (порт 8082).**
   Приложение поднято через `./gradlew :app:bootRun` в worktree
   `cyan-fennel/rvk-ws` на порту 8082:
   - Вход в систему под администратором `admin` / `admin`.
   - В навигационном меню раскрыты разделы «Служебное меню» → «Журнал
     изменений». Пункт «Журнал изменений сущностей» (`audit/entity-log`)
     присутствует и открывается корректно.
   - На вкладке «Просмотр» отображаются реальные события создания и удаления
     контейнеров, моделей, ремонтов и актов. При клике на строку контейнера
     `cnt_Container` в нижнем гриде отображаются локализованные русские
     наименования всех изменённых полей («Номер контейнера», «Модель»,
     «Вместимость (л)», «Дата следующего освидетельствования» и др.), их новые
     и старые значения, а также идентификаторы связанных сущностей.
   - На вкладке «Настройка» отображается список из 22 контейнерных сущностей.
     При выборе строки `cnt_Container` подтверждено включение флагов «Авто»
     и «Вручную», а также отметка всех 16 бизнес-атрибутов.
   - Сняты скриншоты экрана:
     `.playwright-cli/page-2026-09-15T08-00-53-784Z.png` (вкладка «Настройка»)
     и `.playwright-cli/page-2026-09-15T08-01-12-255Z.png` (вкладка «Просмотр»).
   - Сессия браузера закрыта, фоновый процесс `bootRun` корректно остановлен,
     порт 8082 освобождён.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C5 подтверждены.

Ограничение рендеринга: для `EntityLogFragment` зафиксировано
`render not browser-verified` внутри реальной карточки контейнера, так как
хост-экран (таск T03) ещё не реализован. Поведение фрагмента (загрузка по
параметрам, сортировка, реакция на выбор строки, очистка без параметров)
полностью покрыто тестом `EntityLogFragmentUiTest`. Отрисовка фрагмента в
браузере будет окончательно проверена в рамках приемки T03.

## Повторная проверка

Первая проверка реализации, замечаний нет, повторная проверка не требовалась.

## Следующий шаг

Таск T09 переведён в статус `done`. Следующий шаг по плану — выполнение
зависимого таска T03 ([tasks/T03/task.md](../T03/task.md)) или параллельного T10
([tasks/T10/task.md](../T10/task.md)) исполнителем `task-execute`.
