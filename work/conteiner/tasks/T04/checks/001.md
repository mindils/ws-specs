# Проверка 001 — T04

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 08:40–09:28 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `3c92665f1`, 36 незакоммиченных изменений (весь код T01–T04 в рабочем дереве)
Окружение: схема `main_rvk_ws` в общем Docker PostgreSQL, приложение `bootRun` на порту 8082 (поднято и остановлено проверяющим), MinIO из `docker/docker-compose.yml`, бакет `rvk-dev`, браузер `playwright-cli` (Chromium, две сессии: `t04_edit` под `user_edit`, `t04_read` под `user_read`). Порядок по plan.md: проверка после T06, параллельных проверок и сессий исполнителя не было; порт 8082, схема `main_rvk_ws` и gradle daemon заняты только этой проверкой — `:app:test` отработал до старта `bootRun`, браузерный проход шёл после него. Скриншот — `.playwright-cli/t04-verify-c4-form-uploaded.png`.
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | подтверждён | Запись #92, созданная из списка `/container-surveys`, отобразилась во вкладке «Освидетельствования» контейнера 751 (`/containers/751`), а во вкладке контейнера 752 — `0 строк`; создание из вкладки контейнера 752 открывает форму с предустановленным контейнером и атрибутом `readonly`; дубли отсутствуют |
| C2 | подтверждён | Lookup реестра контейнеров по кнопке «Открыть экран выбора связанной сущности» и автодополнение combobox находят контейнер по подстроке `26100` / `26200`; даты начала (`01.02.2026`), окончания (`03.02.2026`) и следующего освидетельствования (`01.02.2031`) в БД и UI строго календарные, без времени |
| C3 | подтверждён | Создание освидетельствования, изменение дат, перенос записи на контейнер 752 и удаление проверены прямым SQL в БД `select cont_num, next_survey_date from main_rvk_ws.cnt_container`: `FGKU5026100` = `2027-03-15`, `FGKU5026200` = `2028-11-01` на всех этапах не изменились (также подтверждено тестом `ContainerSurveyIT`) |
| C4 | подтверждён | PDF загружен в MinIO через `fileStorageUploadField`, отображён в `iframe` компонента `DisplayFile`, заменён на второй файл без ошибок; скачивание доступно при наличии файла; «Отмена» не сохраняет данные в БД; «Копировать» открывает форму без файла и без истории; удаление записи переводит её в soft delete (`deleted_date` и `deleted_by = user_edit` проставлены в БД, грид показывает `0 строк`) |
| C5 | подтверждён | Под ролью `user_read` кнопки «Создать», «Копировать», «Удалить» заблокированы (`disabled`), «Изменить» переименована в «Просмотр» (`disabled`), прямой переход на `/container-surveys/new` открывает форму только для чтения, сохранение блокируется; роль `user_edit` обладает полным доступом; диалог фонового экспорта в Excel открывается |

## Выполненные проверки

### 1. Gate 1 — статические проверки

- 42 XML раздела `container` успешно разобраны парсером без синтаксических ошибок;
- 87 Java-файлов раздела `container` проверены: `package` строго совпадает со структурой каталогов, пустых файлов нет;
- Все 15 message-ключей `msg://` экранов `container-survey-list-view.xml`, `container-survey-detail-view.xml` и `container-survey-fragment.xml` присутствуют в `messages_ru.properties` и корректно резолвятся;
- Все `<itemsQuery>` дескрипторов содержат `escapeValueForLike="true"` и `searchStringFormat="(?i)%${inputString}%"`;
- Проверка форматирования `./gradlew spotlessCheck` выполнена: `BUILD SUCCESSFUL`.

### 2. Gate 2 — тесты

Запущен целевой набор тестов:
```bash
./gradlew :app:test --tests "ru.fgk.ws.app.container.ContainerSurveyIT" \
  --tests "ru.fgk.ws.app.container.view.ContainerSurveyViewsUiTest" \
  --tests "ru.fgk.ws.app.container.security.ContainerRolesTest"
```
Результат: `11 passing (32.7s), 0 failing`.

Запущен весь контейнерный набор тестов:
```bash
./gradlew :app:test --tests "ru.fgk.ws.app.container.*" --tests "ru.fgk.ws.app.common.exception.*"
```
Результат: `51 passing (46.6s), 0 failing`.

Запущен полный прогон тестов `:app:test`:
1328 тестов выполнено. Все 49 падений относятся к 11 известным легаси-сьютам (`dr`, `pt`, `mh`, `diadoc`), в фикстурах которых жёстко прописана схема `main.` вместо `main_rvk_ws`. В модуле `container` и `common.exception` 0 падений.

### 3. Gate 3 — браузерный проход (playwright-cli)

В БД подготовлены тестовые контейнеры `FGKU5026100` (`next_survey_date = 2027-03-15`), `FGKU5026200` (`next_survey_date = 2028-11-01`), вид освидетельствования «Периодическое освидетельствование РМРС» и тестовая организация `T04-ORG-SMOKE` в `ws_store.ora_assb_org_passport_current`.

1. **Создание из списка (C1, C2, C4):**
   - Переход под `user_edit` на `http://localhost:8082/container-surveys`. Нажата кнопка «Создать».
   - В поле «Контейнер» нажата кнопка «Открыть экран выбора связанной сущности»: открылся реестр контейнеров, выбран `FGKU5026100`.
   - Выбран вид «Периодическое освидетельствование РМРС», введены календарные даты `01.02.2026`, `03.02.2026`, `01.02.2031`, исполнитель `T04-ORG-SMOKE`.
   - Загружен тестовый акт `test_survey_act.pdf`: в форме появился просмотр в `iframe`, кнопка «Скачать акт» стала активной. Скриншот сохранён в `.playwright-cli/t04-verify-c4-form-uploaded.png`.
   - Файл заменён на `test_survey_act_2.pdf`: имя обновилось, просмотр переключился.
   - Нажата «OK»: запись успешно сохранилась (id 92), в списке отобразилась `1 строка`.

2. **Проверка вкладки карточки контейнера (C1):**
   - Переход на `/containers/751`. На вкладке «Освидетельствования» отображается созданная запись #92.
   - Переход на `/containers/752`. На вкладке «Освидетельствования» отображается `0 строк`.
   - На вкладке контейнера 752 нажата кнопка «Создать»: открылся модальный диалог, поле «Контейнер» заполнено `FGKU5026200` и имеет `hasReadonlyAttr: true`.
   - Нажата «Отмена»: диалог закрылся, в таблице `cnt_survey` осталась ровно 1 строка.

3. **Перенос на другой контейнер и инвариант даты реестра (C3):**
   - В списке `/container-surveys` открыта запись 92 на редактирование.
   - Поле «Контейнер» изменено на `FGKU5026200` по поиску `26200`. Нажата кнопка «OK».
   - В БД `cnt_survey.container_id` стал 752.
   - Проверены даты в БД: `FGKU5026100` = `2027-03-15`, `FGKU5026200` = `2028-11-01` — даты не изменились.

4. **Копирование записи (C4):**
   - В списке выбрана запись 92 и нажата кнопка «Копировать».
   - Открылся диалог создания: поля дат, вида и исполнителя предзаполнены, поле «Идентификатор» пустое, а файл акта не перенесён («Файл не выбран», «Акт освидетельствования не загружен», «Скачать акт» неактивна).
   - Диалог закрыт кнопкой «Отмена».

5. **Экспорт в Excel (C5):**
   - Нажата кнопка «Фоновый экспорт»: открылся диалог «Фоновый экспорт в Excel» с режимами «Выгрузить всё», «Текущая страница», «Только выделенные». Диалог закрыт.

6. **Проверка прав роли на чтение (C5):**
   - В сессии `user_read` открыта страница `/container-surveys`.
   - Кнопки «Создать», «Копировать», «Удалить» имеют состояние `disabled`. Кнопка «Изменить» переименована в «Просмотр» (`disabled`). Кнопка «Фоновый экспорт» активна.
   - Прямой переход на `/container-surveys/new` открывает форму с полями только для чтения и заблокированной загрузкой файлов. Нажатие «OK» блокируется, запись в БД не создаётся.

7. **Мягкое удаление (C4):**
   - Под `user_edit` выбрана запись 92 и нажата кнопка «Удалить». В диалоге подтверждения нажато «Да».
   - В списке стало `0 строк`.
   - Прямой SQL-запрос подтвердил soft delete: `deleted_date = 2026-09-15 13:27:03.853+00`, `deleted_by = user_edit`.
   - Даты `next_survey_date` обоих контейнеров в БД остались прежними (`2027-03-15` и `2028-11-01`).

8. **Очистка данных:**
   - Браузерные сессии закрыты, приложение остановлено (порт 8082 освобождён).
   - Тестовые данные физически удалены из `main_rvk_ws` и `ws_store`.

## Ошибки и непроверенное

Ошибок нет. Все критерии C1–C5 подтверждены в полном объёме.

## Повторная проверка

Не требуется, проверка пройдена с первой итерации.

## Следующий шаг

Таск T04 завершён (`done`). Согласно плану `plan.md`, следующим шагом является выполнение таска [T07](../../tasks/T07/task.md) («Ремонты: страница и вкладка; вкладка Акты»).
