# Проверка 002 — T12

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 2
Дата: 2026-09-16 14:55–15:12 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `1ad658d3f`, те же 8 незакоммиченных файлов, что и в [checks/001.md](001.md) (4 файла T12 + 3 файла принятого T11 + `IframeSecurity.java` из принятого T14); после итерации 1 изменён единственный файл `container-repair-detail-view.xml` (mtime 10:54), остальные три файла T12 имеют mtime 10:31–10:34, то есть не менялись; `git status --short` до и после проверки совпадает, код продукта и репозиторные тесты проверкой не менялись
Окружение: схема `main_rvk_ws` в общем Docker PostgreSQL (`docker-rvk-db-1`), MinIO и Keycloak из `docker/docker-compose.yml`, приложение `bootRun` на порту 8082 — поднято и остановлено проверяющим, готовность ловилась кодом `200` на `/local-login`; браузер `playwright-cli` (Chrome, две сессии: `t12v2` — `user_edit`, `t12v2r` — `user_read`), разрешения 1366×768 и 1920×1080. Порядок по plan.md: T14 и T11 `done`, T12 идёт следующей, T13 и T15 в статусе `todo` и на ресурсах не работают. Порт 8082 и gradle daemon заняты только этой проверкой: `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` и `spotlessCheckAll` отработали до старта `bootRun`, браузерный проход — после него. Порт 8083 параллельно занимало приложение соседнего worktree (чужая работа), контейнерных таблиц оно не трогает
Итог: pass — замечание F01.1 исправлено, критерии C1–C6 подтверждены заново в браузере и тестами

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| F01.1 | исправлен | Раздел 1: поиск по изменённым файлам T12 с нормализацией переносов строк даёт 0 совпадений; комментарий перед `formLayout id="documentsForm"` объясняет ручной ввод стоимостей отсутствием автоматических расчётов в разделе ремонтов контейнеров, без ссылок на артефакты работы |
| C1 | подтверждён с оговоркой | Разделы 2 и 3: у нового ремонта и у сохранённого (`/container-repairs/425`) ровно пять вкладок `repairTab`, `contractTab`, `documentsTab`, `manufacturerTab`, `fileTab` с нужными подписями, выбрана первая; по вкладкам разложены 32 поля точно по таблице контракта (14 + 5 + 8 + 4 + 1), привязка данных видна по значениям `425` / `TSTV2000001`. Оговорка прежняя: в C1 указано 33 поля, у формы их 32 — завышено число в постановке, а не пропущено поле |
| C2 | подтверждён | Раздел 4: на `/container-repairs/new` с открытой вкладкой «Документы и стоимость» «OK» вернул выбор на `repairTab`, `containerField` и `rejectDateField` получили `invalid`, URL остался `/container-repairs/new`, `cnt_repair` = 0; с заполненными контейнером и «Датой браковки» ремонт сохранился (`id = 425`) |
| C3 | подтверждён | Раздел 5: без файла `#displayFile` в DOM отсутствует, картинок на вкладке нет, «Скачать документ» `disabled`; после загрузки PDF панель 1046×474 с `iframe` и активная кнопка; после «Remove file» панель `hidden`, `display: none`, 0×0, кнопка снова `disabled`; сохранённый ремонт с документом открывается сразу с панелью 1046×474 |
| C4 | подтверждён | Раздел 6: карточка `/containers/1607` → вкладка «Ремонты» → «Создать» открывает ту же форму диалогом (пять вкладок, выбрана `repairTab`), `containerField` `readonly` со значением `TSTV2000001`; после «OK» диалог закрылся, грид вкладки показал строку `425 | 16.09.2026 | 10.09.2026`, в БД `date_begin = 2026-09-10` |
| C5 | подтверждён | Раздел 7: `/container-repairs/new` при 1366×768 — `detailActions` 708–752, «OK» 712–748, `elementFromPoint` в центре даёт сам `saveAndCloseButton`; при 1920×1080 — 1020–1064; в диалоге из карточки при 1366×768 — 679–715, «OK» и «Отмена» hit-тестируются сами собой, в точке неактивной «Скачать документ» лежит сама панель `detailActions` (кнопка `disabled` пропускает указатель), полями кнопки не перекрыты |
| C6 | подтверждён | Разделы 1 и 8: `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` — BUILD SUCCESSFUL, 66 passing; `spotlessCheckAll` — BUILD SUCCESSFUL; в XML нет `details`, `split`, `css=`, `classNames=`; все 9 `msg://` резолвятся; `document.body.innerText.includes('msg://')` = `false` на всех проверенных экранах |
| Роль `container-read` | подтверждён | Раздел 9: под `user_read` форма `/container-repairs/425` открывается, пять вкладок на месте, все 14 полей вкладки «Ремонт» `readonly`, панель просмотра видна (474 px), «Скачать документ» активна, кнопка загрузки не отрисована (0×0), кнопки очистки нет, сырых `msg://` нет |
| Комментарии в коде | подтверждён | Раздел 1: 0 совпадений по `\b[TFQC]\d+\b`, `specs/work/`, «решение пользователя», «проверяющ», «итераци», «по плану», `AGENTKIT` в трёх изменённых T12 файлах кода и тестов |

## Выполненные проверки

### 1. Исправление F01.1, комментарии, статика, тесты

Поиск по изменённым файлам T12 (`container-repair-detail-view.xml`,
`ContainerRepairDetailView.java`, `ContainerRepairViewsUiTest.java`) с
нормализацией переносов строк — построчный `grep` формулировку, разорванную
переносом, не видит:

```bash
python3 - <<'PY'
import re
files=['app/src/main/resources/ru/fgk/ws/app/container/view/containerrepair/container-repair-detail-view.xml',
'app/src/main/java/ru/fgk/ws/app/container/view/containerrepair/ContainerRepairDetailView.java',
'app/src/test/java/ru/fgk/ws/app/container/view/ContainerRepairViewsUiTest.java']
pats=[r'\b[TFQC]\d{1,}\b', r'specs/work/', r'решени\w* пользовател\w*', r'проверяющ',
      r'итераци', r'по плану', r'AGENTKIT']
for f in files:
    n=re.sub(r'\s+',' ',open(f,encoding='utf-8').read())
    for p in pats:
        for m in re.finditer(p,n,re.I):
            print(f,'::',p,'::',n[max(0,m.start()-100):m.end()+60])
PY
```

Совпадений нет. Комментарий на месте замечания сейчас такой:

```xml
<!-- Стоимости вводятся вручную из счёта-фактуры: в разделе ремонтов контейнеров нет
     автоматических расчётов, поэтому НДС и итог не выводятся из стоимости ремонта. -->
```

Причина названа в терминах самого раздела и понятна читателю кода, не видевшему
документов работы; первое предложение про счёт-фактуру сохранено. Проверка
`messages_ru.properties` отдельно: собственные ключи ремонта переехали без
следов артефактов, чужие совпадения раздела `dr` (`ReportOperRepairDocFlowDto.c1`,
`grid.c1.title`) — не комментарии и областью T12 не затронуты.

```bash
cd ../worktrees/rvk-ws/cyan-fennel/rvk-ws
./gradlew :app:test --tests "ru.fgk.ws.app.container.*"   # BUILD SUCCESSFUL in 59s, 66 passing
./gradlew spotlessCheckAll                                # BUILD SUCCESSFUL in 1s
```

Прошли, в частности, оба теста вкладок: «Форма ремонта открывается на первой из
пяти вкладок, без файла просмотра нет» и «Ремонт без даты браковки не
сохраняется и возвращает на вкладку с этим полем».

Механические проверки XML: файл разбирается `xml.dom.minidom`; `css=`,
`<details`, `<split`, `classNames=`, `displayNotFoundFile` — 0 вхождений; 32
уникальных `id="…Field"`; все девять `msg://` найдены в
`messages_ru.properties`; удалённые ключи `section.repair|dates|contract|
manufacturer|file` и `noFile` в `app/src` не упоминаются.

### 2. C1 — состав и порядок вкладок, распределение полей

`/container-repairs/new` под `user_edit`, 1366×768:

```json
{"count":5,"tabs":[{"id":"repairTab","label":"Ремонт","selected":true},
{"id":"contractTab","label":"Исполнитель и договор","selected":false},
{"id":"documentsTab","label":"Документы и стоимость","selected":false},
{"id":"manufacturerTab","label":"Уведомление завода","selected":false},
{"id":"fileTab","label":"Документ","selected":false}],"rawMsg":false}
```

Содержимое невыбранных вкладок в DOM не отрисовано, поэтому поля собирались
переключением по вкладкам (`menuFilterField` — поиск по меню приложения, к форме
не относится): `repairTab` — `idField`, `containerField`, `repairTypeField`,
`defectReasonField`, `defectField`, `commentField`, `roofUpgradeField`,
`rejectDateField`, `rejectStField`, `departDateField`, `dateBeginField`,
`dateEndField`, `returnDateField`, `dateNextField` (14); `contractTab` —
`orgField`, `repContractField`, `repStField`, `repByWarrField`,
`warrOnRepField` (5); `documentsTab` — `docsSubmissionDateField`,
`docsVerificationDateField`, `docsVerificationResultField`,
`rejectionReasonField`, `repCostField`, `vatField`, `vatRubField`,
`costWithVatField` (8); `manufacturerTab` — `manufNotifDateField`,
`repStInstrField`, `manufAnswDateField`, `manufReNotifDateField` (4);
`fileTab` — `documentField` (1). Итого 32, совпадает с таблицей таска,
`contracts.md` и разбором XML.

### 3. Сохранённый ремонт из самостоятельной страницы

`/container-repairs/425` даёт те же пять вкладок в том же порядке с выбранной
`repairTab`; `idField` = `425`, `containerField` = `TSTV2000001` и не
read-only — точка входа список.

### 4. C2 — валидация с другой вкладки

Тестовые данные заводились через UI: контейнер `TSTV2000001` (`id = 1607`).
На `/container-repairs/new` открыта вкладка «Документы и стоимость»
(`selected: documentsTab`), нажат «OK»:

```json
{"url":"/container-repairs/new","selectedTab":"repairTab",
 "containerInvalid":true,"rejectInvalid":true}
```

`select count(*) from main_rvk_ws.cnt_repair` = 0 — запись не сохранена. После
заполнения контейнера и «Даты браковки» `16.09.2026` сохранение прошло:
в БД появилась строка `425 | 1607 | 2026-09-16 | 2026-09-10`.

### 5. C3 — вкладка «Документ»

Новый ремонт, вкладка «Документ» до загрузки:
`{"displayFilePresent":false,"downloadDisabled":true,"imgsOnTab":[]}` — панели
нет в DOM совсем, картинок нет, кнопка неактивна.

Загрузка PDF (сгенерирован во временном каталоге сессии, 586 байт, 1 страница):
`{"present":true,"hidden":false,"w":1046,"h":474,"iframe":true,
"downloadDisabled":false}` — просмотр появился сразу, до сохранения записи.

Очистка кнопкой «Remove file»:
`{"present":true,"hidden":true,"display":"none","w":0,"h":0,
"downloadDisabled":true}`.

Повторная загрузка и «OK» → `document =
s3://cnt_file/2026/09/16/adf5e565-…-341c375fa29c.pdf?name=t12-check.pdf`.
Повторное открытие `/container-repairs/425`, вкладка «Документ»:
`{"present":true,"hidden":false,"w":1046,"h":474,"iframe":true,"imgs":[],
"downloadDisabled":false,"rawMsg":false}` — панель видна при открытии, битой
картинки нет.

Единственная ошибка в консоли за оба прохода — недоступный вне корпоративной
сети аватар: `Failed to load resource: net::ERR_NAME_NOT_RESOLVED @
https://info.main.vgk/api2/v1/phone-book/avatar/user_edit` (и `…/user_read`).

### 6. C4 — вторая точка входа и обновление грида

`/containers/1607` открывается с семью вкладками карточки, «Описание» выбрано.
Вкладка «Ремонты» → «Создать» открывает форму ремонта диалогом: в DOM
появляются `repairTab`, `contractTab`, `documentsTab`, `manufacturerTab`,
`fileTab`, выбрана `repairTab`, `containerField` имеет `readonly` и показывает
`TSTV2000001`. Заполнены «Дата браковки» `16.09.2026` и «Дата начала ремонта»
`10.09.2026`, нажат «OK» диалога: диалог закрылся, грид вкладки перечитался и
показал `425 | 16.09.2026 | 10.09.2026`, в БД `date_begin = 2026-09-10`.

### 7. C5 — геометрия кнопок

Страница `/container-repairs/new`:

- 1366×768: `#detailActions` 708–752, «OK» 712–748,
  `document.elementFromPoint` в центре кнопки возвращает сам
  `saveAndCloseButton`; видимый `vaadin-scroller` вкладки прокручивается внутри
  себя (`scrollHeight` 592 при `clientHeight` 567), поля под панель не уходят;
- 1920×1080: `#detailActions` 1020–1064, попадание в «OK» такое же.

Диалог из карточки при 1366×768: панель диалога 679–715, «OK» и «Отмена»
679–715 и hit-тестируются сами собой; в центре «Скачать документ» лежит сама
панель `detailActions`, потому что у нового ремонта без файла кнопка `disabled`
и не принимает указатель — перекрытия полями формы нет.

### 8. C6 — сырые `msg://` и запрещённые элементы

`document.body.innerText.includes('msg://')` = `false` на
`/container-repairs/new` (все пять вкладок), `/container-repairs/425`,
`/containers/1607` с диалогом ремонта и под ролью чтения. Подписи вкладок и
подзаголовки отрисованы по-русски. Статические проверки XML — раздел 1.

### 9. Роль `container-read`

Отдельная браузерная сессия под `user_read`. `/container-repairs/425`
открывается, пять вкладок в том же порядке, `repairTab` выбрана, все 14 полей
вкладки «Ремонт» `readonly` (14 из 14). На вкладке «Документ» панель просмотра
видна (474 px, `iframe`), «Скачать документ» активна, кнопка загрузки в DOM есть,
но не отрисована (0×0), кнопки очистки нет — загрузить или снять файл под ролью
чтения нельзя. Сырых `msg://` нет.

## Ошибки и непроверенное

Ошибок нет, непроверенных обязательных сценариев нет: подраздел «Независимая
проверка» выполнен целиком — C1–C5 из обеих точек входа под `container-edit`,
открытие под `container-read`, `./gradlew :app:test --tests
"ru.fgk.ws.app.container.*"`.

Расхождение постановки сохраняется и дефектом не считается: C1 требует «все 33
поля матрицы `cnt_repair`», фактически полей формы 32 (31 бизнес-атрибут плюс
`idField`). Число в критерии завышено на единицу, состав полей полный — сверено
с таблицей таска, `contracts.md` и разбором XML.

Состояние окружения после проверки: `bootRun` погашен, порт 8082 свободен,
браузерные сессии закрыты, `git status --short` в worktree совпадает с
состоянием до проверки. Заведённые проверкой строки удалены (`cnt_repair` = 0,
`cnt_container` = 0), вместе с ними — 75 строк `audit_entity_log` по
`cnt_Container` и `cnt_ContainerRepair`. Загруженный PDF остался в MinIO без
ссылки из БД, как и после прошлых проходов.

## Повторная проверка

Замечание [F01.1](../fixes/F01.md) подтверждённо исправлено: раздел 1. Прочих
замечаний в [checks/001.md](001.md) не было.

Итерация 2 изменила один комментарий в `container-repair-detail-view.xml` —
поведение это не меняет, что подтверждено фактически, а не по заявлению
исполнителя: сценарии C1–C6 и открытие под ролью чтения пройдены заново в этой
же проверке и дали те же результаты, что в [checks/001.md](001.md) (те же
пять вкладок и 32 поля, те же 1046×474 у просмотра, те же координаты панели
кнопок 708–752 и 1020–1064, те же 66 зелёных тестов). Доказательства проверки
001 используются только для сверки, самостоятельной силы в этом отчёте они не
несут.

## Следующий шаг

Следующий таск очереди — [T13](../../T13/task.md) в статусе `todo`: исполнитель
запускает `task-execute` по нему; БД, порт 8082, gradle daemon, браузер и MinIO
свободны. После `done` T13 остаётся сквозной [T15](../../T15/task.md), затем
`task-close` по всей работе.
