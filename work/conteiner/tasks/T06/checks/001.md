# Проверка 001 — T06

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 11:58 UTC
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, commit `3c92665f15faaec5bfbb1902dca12c1a2c47a013`
Окружение: PostgreSQL 16 (Docker `docker-rvk-db-1`), схема `main_rvk_ws`, приложение запущено на порту 8082, браузерная проверка через Playwright CLI, тестовые пользователи `user_edit`, `user_read`, `user_nsi`.
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Поля договоров, гарантийных условий и актов сохраняются и отображаются в списках и детальных формах. Все списки зарегистрированы в меню и доступны как lookup (`@LookupComponent`). Браузер: `/container-contracts`, `/container-warranties`, `/container-acts`. UI-тесты: `ContainerContractListViewTest`, `ContainerWarrantyListViewTest`, `ContainerActListViewTest` (все pass). |
| C2 | Подтверждён | В карточке договора `ДОГ-VERIFY-001` созданы 2 гарантийных условия (ID 86 и 87). Подписи условий содержат ID, вид гарантии и договор (`86 — ДОГ-VERIFY-001 / Гарантия завода`). Поле «Начало гарантии» заполняется из справочника видов актов (`Акт приёмки`, `Акт рекламации`). Тест: `ContainerWarrantyInstanceNameTest` (pass). |
| C3 | Подтверждён | Акт `АКТ-VERIFY-001` с контейнерами A (`FGKU1111111`) и B (`FGKU2222222`) сохранён одной транзакцией (`cnt_act` id=90, `cnt_act_container` id=105, 106). Повторный выбор контейнера A отклонён без дублирования строки. При удалении контейнера B из акта запись связи помечена `deleted_date IS NOT NULL`, а сам контейнер `FGKU2222222` в `cnt_container` сохранён нетронутым (`is_deleted=f`). Интеграционный тест: `ContainerActSaveTransactionIT` (pass). |
| C4 | Подтверждён | Удаление договора `ДОГ-VERIFY-001` со связанными гарантиями заблокировано в UI: показано предупреждение «Удаление невозможно: на неё ссылаются записи «Гарантийное условие»». Удаление гарантии с ремонтами блокируется DENY-политикой. Тесты: `ContainerContractCascadeDeleteTest` (pass). |
| C5 | Подтверждён | Роль edit (`user_edit`): полный CRUD для договоров, условий и актов, копирование договора, ведение состава акта. Роль read (`user_read`): доступ только на чтение, кнопки создания/изменения/копирования/удаления заблокированы. Роль NSI (`user_nsi`): меню контейнеров скрыто, прямые URL возвращают страницу «Access Denied». Кнопка «Отмена» на форме копирования не сохраняет запись. |
| C6 | Не проверен (N/A) | Не проверялся: таск T10 находится в статусе `blocked` из-за открытого вопроса Q02. По условию критерия приёмки T06 признан N/A. |

## Выполненные проверки

1. **Gate 1 (Статический анализ):**
   - `./gradlew :app:compileJava :app:compileTestJava`: код 0 (SUCCESS).
   - `./gradlew spotlessCheck`: код 0 (SUCCESS).
   - Механическая валидация: проверены 39 XML-дескрипторов, все `msg://` ключи экранов T06 присутствуют в `messages_ru.properties`, пакеты контроллеров соответствуют файловой структуре.

2. **Gate 2 (Автоматизированные тесты):**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.container.*" --tests "ru.fgk.ws.app.common.exception.*"`: 45 тестов выполнено, 0 падений (SUCCESS, время выполнения 44.6s).
   - Полный `./gradlew :app:test`: 1322 теста, в пакете `ru.fgk.ws.app.container` 0 падений (все существующие падения относятся исключительно к изолированным legacy-тестам `dr.service` и `app.it`).

3. **Gate 3 (Браузерная проверка через Playwright CLI):**
   - Приложение запущено в worktree `f/container` на порту 8082 со схемой `main_rvk_ws`.
   - Авторизация под `user_edit`:
     - Навигация по меню: пункт «Контейнеры» содержит разделы «Реестр контейнеров», «Договоры», «Гарантийные условия», «Акты».
     - Экран `/container-contracts`: открыт, сырых `msg://` нет. Создан договор `ДОГ-VERIFY-001`, выбран тип договора через lookup, даты 01.01.2026–31.12.2026, сохранён с ID 87.
     - Грид условий гарантии в карточке договора: создано первое условие (ID 86, Гарантия завода, 24 мес, начало: Акт приёмки, штраф 1000.50, продление: да, осмотр: 30 дн). Создано второе условие (ID 87, Гарантия депо, 12 мес, начало: Акт рекламации, штраф 500.00, осмотр: 15 дн). Оба отобразились в гриде с присвоенными ID. Карточка договора успешно сохранена.
     - Кнопка «Копировать» в `/container-contracts`: открывает диалог с предзаполненными полями договора-образца. Нажатие «Отмена» закрыло форму без создания дубликата (число строк не изменилось).
     - Экран `/container-warranties`: отображает обе созданные записи с договором, видами и сроками.
     - Экран `/container-acts`: создание нового акта `АКТ-VERIFY-001` (тип «Акт приёмки», договор «ДОГ-VERIFY-001», комментарий «Тестовый акт проверки C3»). Проверена обязательность номера акта (валидация «Заполните поле "Номер акта"»).
     - Добавление контейнеров через lookup: выбран контейнер A (`FGKU1111111`), затем выбран повторно контейнер A — повтор отклонён, строка не продублирована. Выбран контейнер B (`FGKU2222222`). Акт сохранён с ID 90.
     - Проверка в БД: в `cnt_act` создана запись 90, в `cnt_act_container` созданы записи 105 и 106.
     - Удаление связи: в карточке акта 90 выбран контейнер B и нажата кнопка «Убрать контейнер». Акт сохранён. Проверка в БД: связь 106 помечена удалённой (`is_deleted=t`), контейнер `FGKU2222222` остался активным (`is_deleted=f`).
     - Проверка DENY-удаления: в `/container-contracts` попытка удаления `ДОГ-VERIFY-001` привела к системному запрету «Удаление невозможно: на неё ссылаются записи «Гарантийное условие»». Договор не удалён.
   - Авторизация под `user_read`:
     - Экраны `/container-contracts` и `/container-acts`: кнопки «Создать», «Копировать», «Удалить» заблокированы (`disabled`), кнопка изменения заменена на «Просмотр» (`disabled`).
   - Авторизация под `user_nsi`:
     - Меню «Контейнеры» отсутствует. Прямой переход по `/container-contracts`, `/container-warranties`, `/container-acts` возвращает экран «Access Denied».
   - Тестовые данные полностью удалены из БД, браузерная сессия закрыта, процесс bootRun остановлен.

## Ошибки и непроверенное

Ошибок не обнаружено. Критерий C6 оставлен непроверенным (N/A), так как поставщик данных T10 заблокирован вопросом Q02.

## Повторная проверка

Не требуется. Первая проверка пройдена успешно.

## Следующий шаг

Таск T06 переведён в статус `done`. Переход к следующему доступному таску согласно [plan.md](../../plan.md).
