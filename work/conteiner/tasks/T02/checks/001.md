# Проверка 001 — T02

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 03:30
Состояние кода: commit 3c92665f1, ветка f/container (worktree cyan-fennel/rvk-ws)
Окружение: Linux, PostgreSQL 16 (схема main_rvk_ws), Docker compose (docker-rvk-db-1), JDK 21, Gradle 9.5.1, bootRun порт 8082, Playwright CLI
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Статический анализ 13 XML/Java и браузер: все 13 справочников в меню «Справочники → Контейнеры», propertyFilter, simplePagination, async_excel_export, все атрибуты матрицы contracts.md (включая widthF «Длина (фут)»), 0 сырых msg:// |
| C2 | Подтверждён | Браузер под `user_nsi` (`container-nsi-edit`): создание, изменение, soft delete (`deleted_by`, `deleted_date` в БД); удаление причины при наличии ссылки из неисправности блокируется `DeletePolicyException: Unable to delete cnt_CntDefectReason because there are references from cnt_CntDefect` |
| C3 | Подтверждён | Браузер под `user_edit` (`container-edit`): кнопки создания/удаления недоступны, прямой переход на `/cnt-defect-reasons/new` рендерит поля `readonly`; под `user_read` (`container-read`) доступны только чтение и диалог фонового экспорта в Excel |
| C4 | Подтверждён | Браузер под `user_nsi`: в `CntDefectDetailView` выбор причины неисправности через lookup открывает модальный диалог `CntDefectReasonListView` с `@LookupComponent` и действием `lookup_selectAction`, выбор строки возвращает значение в поле формы |
| C5 | Подтверждён | Автотест `ContainerRolesTest`: подтверждено наличие всех политик entity/attribute/view/menu для ролей `container-nsi-edit`, `container-edit`, `container-read` и отсутствие CRUD основных сущностей у роли НСИ; `ContainerNsiViewsUiTest` успешно открывает все 13 экранов |

## Выполненные проверки

1. **Компиляция Java и тестов.**
   Команда `./gradlew :app:compileJava :app:compileTestJava` в worktree `cyan-fennel/rvk-ws` завершилась с кодом 0 без ошибок и предупреждений.

2. **Статический анализ XML-дескрипторов и контроллеров (13 справочников).**
   - Все 13 `*-list-view.xml` содержат `<lookupComponent>...`, действия `lookup_select` и `lookup_discard`, `propertyFilter` с `operation="CONTAINS"`, `simplePagination` и действие `fgk_async_excel_export`. Компонент `genericFilter` отсутствует во всех дескрипторах.
   - Все 13 Java-контроллеров list view содержат аннотацию `@LookupComponent`.
   - Все 13 `*-detail-view.xml` содержат полный состав полей согласно матрице `contracts.md`. В частности:
     - `CntDefectDetailView`: `defectReasonField` выполнен как `entityComboBox`.
     - `CntContainerSizeDetailView`: поле `widthF` имеет локализованную подпись «Длина (фут)».
   - В `menu.xml` созданы каркас `<menu id="container">` и подгруппа `<menu id="nsi_container">` внутри `<menu id="nsi">` со всеми 13 пунктами.
   - В `messages_ru.properties` добавлены все 26 заголовков экранов и пункты меню без пропусков.

3. **Набор автоматизированных тестов контейнерного раздела.**
   Команда `./gradlew :app:test --tests "ru.fgk.ws.app.container.*"` выполнила 19 тестов из 7 классов (`ContainerRolesTest`, `ContainerNsiViewsUiTest`, `ContainerCrudIT`, `ContainerDeletePolicyIT`, `ContainerNsiReferenceIT`, `ContainerNumberIT`, `EntityCopySupportIT`) — все 19 тестов успешно пройдены (код 0).
   - `ContainerRolesTest` строго валидирует матрицы разрешений всех трёх ролей.
   - `ContainerNsiViewsUiTest` через Jmix Flow UI test assist инициализирует контроллеры всех 13 list view.

4. **Браузерная проверка в Playwright CLI (порт 8082).**
   Приложение запущено через `./gradlew :app:bootRun` с локальным профилем на порту 8082, база данных `main_rvk_ws` в PostgreSQL 16.
   - **Сценарий C1, C4, C2 под `user_nsi` (`container-nsi-edit`):**
     - Вход через `/local-login` под `user_nsi`.
     - Открытие меню: «Справочники → Контейнеры» отображает все 13 пунктов с корректными русскими названиями без сырых `msg://`.
     - Переход в `/cnt-defect-reasons`: создание причины «Износ в эксплуатации TEST-VERIFY».
     - Проверка `propertyFilter`: фильтрация по подстроке работает, сброс фильтра восстанавливает запись.
     - Редактирование причины: переименование в «Износ в эксплуатации ИЗМЕНЁН», сохранение.
     - Переход в `/cnt-defects`, создание новой неисправности: нажатие лукапа у поля «Причина неисправности» открыло диалоговое окно `CntDefectReasonListView`, нажатие «Выбрать» вернуло выбранную запись в combobox (C4 подтверждён). Неисправность сохранена с именем «Трещина фитинга TEST-VERIFY».
     - Возврат в `/cnt-defect-reasons`: попытка удаления причины неисправности, на которую ссылается созданная неисправность. Получена ошибка `DeletePolicyException: Unable to delete cnt_CntDefectReason because there are references from cnt_CntDefect` (C2 DENY подтверждён).
     - Переход в `/cnt-defects`: удаление связанной неисправности.
     - Возврат в `/cnt-defect-reasons`: удаление причины неисправности прошло успешно, в таблице 0 строк.
     - В СУБД запросом SQL проверено: записи получили `deleted_by='user_nsi'` и timestamp `deleted_date` (soft delete подтверждён).
   - **Сценарий C3 под `user_edit` (`container-edit`):**
     - Вход под `user_edit` (`edit123`).
     - Переход в `/cnt-defect-reasons`: кнопка «Создать» заблокирована (`disabled`), кнопка «Удалить» заблокирована (`disabled`), кнопка действия переведена в режим «Просмотр».
     - Прямой переход по URL `/cnt-defect-reasons/new`: Flow UI отображает поля ввода с HTML-атрибутом `readonly` (`<input readonly>`), изменение или создание записи пользователем невозможно.
   - **Сценарий C3 под `user_read` (`container-read`):**
     - Вход под `user_read` (`read123`).
     - Меню и все 13 справочников доступны на чтение. Кнопки «Создать», «Просмотр», «Удалить» заблокированы.
     - Кнопка «Фоновый экспорт» активна, нажатие открывает модальный диалог «Фоновый экспорт в Excel» с выбором режимов выгрузки.

5. **Очистка данных и окружения.**
   - Проверено отсутствие неудалённых тестовых записей в БД (`SELECT count(*) WHERE deleted_date IS NULL` = 0).
   - Браузерная сессия Playwright закрыта (`playwright-cli -s=cnt close`).
   - Фоновый процесс `./gradlew :app:bootRun` корректно остановлен.

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C5 полностью подтверждены автоматическими тестами и интерактивным браузерным тестированием.

## Повторная проверка

Первая проверка реализации, замечаний нет, повторная проверка не требовалась.

## Следующий шаг

Таск T02 переведён в статус `done`. Следующие шаги по плану:
- Выполнение тасков ветки T03/T04/T05/T06/T07 исполнителем `task-execute`.
