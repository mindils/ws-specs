# Проверка 001 — T08

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-15 11:15–12:45 (-04:00)
Состояние кода: worktree `../worktrees/rvk-ws/cyan-fennel/rvk-ws`, ветка `f/container`, коммит `3c92665f1`, 36 незакоммиченных изменений — тот же набор, что зафиксирован в отчётах T03/T07; до и после проверки `git status --short` совпадает, код продукта проверкой не менялся
Окружение: схема `main_rvk_ws` в общем Docker PostgreSQL (`docker-rvk-db-1`), MinIO из `docker/docker-compose.yml` (бакет `rvk-dev`), приложение `bootRun` на порту 8082 — поднято и остановлено проверяющим (`kill 3956025`), браузер `playwright-cli` (Chromium, четыре сессии: `t08nsi` — `user_nsi`, `t08edit` — `user_edit`, `t08read` — `user_read`, `t08admin` — `admin`). Порядок по plan.md: T08 выполняется последним, параллельных тасков нет; БД, gradle daemon и порт 8082 заняты только этой проверкой — `./gradlew :app:test` отработал до старта `bootRun`, браузерный проход шёл после него. Пароль `user_nsi` — `nsi123` (в result.md не был указан).
Итог: fail

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | ошибка в части прав ролей; остальное подтверждено | Механическая сверка матрицы своим скриптом: 22 entity содержат ровно колонки матрицы (расхождений 0), 21 detail-форма покрывает все несистемные атрибуты своей entity, `required` совпадает с таблицей «Обязательные поля (UI)», 40 view-id раздела покрыты `ContainerReadRole` и `ContainerEditRole` (лишних нет), 26 — `ContainerNsiEditRole`. В браузере: 13 справочников в меню, шесть вкладок карточки, 31 поле формы ремонта. **Строка матрицы «`ContainerReadRole` … экспорт» не выполняется** — см. [F01](../fixes/F01.md) и [Q04](../../../questions/Q04.md) |
| C2 | подтверждён | Сквозной сценарий раздела пройден в браузере под тремя ролями (разделы 3.1–3.9 ниже): НСИ заводит справочники, edit создаёт контейнеры, характеристику, файл, освидетельствование, ремонт по сценарию 6 инструкции, акт на два контейнера; read только читает; НСИ к основным записям не допущен |
| C3 | подтверждён | Ни ремонт, ни освидетельствование не меняют `next_survey_date` реестра: у контейнера A `date_next` освидетельствования = 2031-08-01 при `next_survey_date` = 2027-03-01, значение не изменилось. Стоимости сценария 6 записаны буквально (31479.65 / 22 / 6925.52 / 38405.17), пересчёта нет. В коде раздела нет `setNextSurveyDate` вне entity, нет entity операций/накладных, единственный listener только нормализует номер |
| C4 | подтверждён | Soft delete контейнера A (строка осталась с `deleted_by=user_edit`, из списка исчезла), четыре отказа удаления с русскими сообщениями, дубль номера отклонён при живой и при удалённой записи, копия без номера и без детей, отмена формы не создаёт записей, EntityLog показывает CREATE/MODIFY/DELETE с парой старое/новое |
| C5 | ошибка не подтверждена; полная зелёность недостижима в worktree | `./gradlew :app:test` — `1342 tests completed, 49 failed, 12 skipped`. Все 49 падений — в 11 легаси-классах `dr`/`pt`/`mh`/`diadoc`, в контейнерном пакете 0 падений из 49 тестов. Причина подтверждена независимо и не связана с T08 — см. раздел 2 |
| C6 | подтверждён | Error overlay не появлялся ни разу; в серверном логе за весь проход 0 записей уровня `ERROR`; в 520 снимках страниц нет ни одного сырого `msg://`; в консоли только известные внешние ошибки (раздел 3.10) |

## Выполненные проверки

### 1. Готовность к приёмке и статические проверки

- Зависимости и предшественники: T01, T02, T03, T04, T06, T07, T09 — `Статус: done`,
  открытых исправлений нет (`T03/fixes/F01.md` — `done`).
- Состав раздела: 22 entity, 21 detail-view + 6 фрагментов, 3 роли, 22 таблицы
  `cnt_*` в схеме.
- Сверка матрицы `contracts.md` выполнена независимым скриптом, не сверкой с
  текстом result.md: сравнивались имена колонок в `@Column/@JoinColumn` каждой
  entity с таблицами «Основные таблицы» и «Справочники»; пропусков и лишних
  колонок нет. Отдельным проходом сверены `property=` каждого detail-view с
  бизнес-атрибутами своей entity — непокрытых атрибутов нет.
- `msg://`: 47 дескрипторов раздела и `menu.xml` разобраны скриптом,
  нерезолвящихся ссылок в контейнерном разделе нет. Шесть нерезолвящихся
  ссылок в `menu.xml` относятся к чужим модулям (`ru.fgk.ws.core`,
  `ru.fgk.sso`, `ru.fgk.component.rvkfilter`) и к легаси-пунктам вагонов —
  вне раздела и вне области T08.
- `menu.xml` изменён только добавлением: 70 добавленных строк, удалённых нет.
- IDE-инспекции (`get_file_problems`) в сессии нет; её роль в этой проверке
  закрыта фактическим браузерным проходом по всем экранам раздела.

### 2. Полный набор тестов

```bash
cd ../worktrees/rvk-ws/cyan-fennel/rvk-ws
./gradlew :app:test
```

Результат: `1342 tests completed, 49 failed, 12 skipped`, BUILD FAILED.

Разбор по XML-отчётам `app/build/test-results/test/`: контейнерный пакет — 21
сьют, 49 тестов, 0 падений, включая новый `ContainerScenarioIT` (1 тест,
passed). Все 49 падений — в тех же 11 классах, что зафиксированы проверками
T01, T03, T04 и T07 до появления кода T08: `DrOperRepairTr2ParserServiceIT` 10,
`MhDetailOriginServiceIT` 7, `DiadocPacketCorrectionIT` 7, `PacketReplayIT` 7,
`ReceiptCostReplayIT` 6, `Mh1mPacketMergeIT` 4, `MhReferenceCostLookupIT` 3,
`DrPlanRepairParserServiceIT` 2, `PtRepairPacketCorrectionIT` 1,
`ContractRepairCountIT` 1, `DrOperRepairParserServiceTest` 1. Причина
перепроверена независимо: 16 тестовых файлов пишут SQL с жёстко зашитой схемой
`main.` (например `ContractRepairCountIT.java:94` — `INSERT INTO
main.dr_diadoc_wag_oper_repair_contract`), а приложение в worktree читает
`main_rvk_ws`, поэтому вставленные строки не видны запросам. Регрессией T08 эти
падения не являются; ошибкой T08 их не считаю, но буквальное «`:app:test`
зелёный» из C5 в этом worktree недостижимо без правки чужих легаси-тестов —
это вне области работы.

Разбор нового теста: `ContainerScenarioIT.runsWholeContainerSection` (538 строк)
действительно проверяет заявленное — 13 справочников, два контейнера с разными
датами, виртуальные поля из модели и размера, подпись условия гарантии с ИД,
независимость `next_survey_date`, акт на два контейнера, пять отказов удаления,
переименование с сохранением связей, копию без детей, дубль номера по индексу,
снятие связи, мягкое удаление с проверкой строки в БД, отказ занять номер
удалённого и записи EntityLog с парами старое/новое. Тест не обёрнут
транзакцией, данные и записи `audit_entity_log` убирает в `@AfterEach`.

Исправление дефекта подписи проверено по коду: `getRepairName()`,
`getSurveyName()` и `getWarrantyName()` печатают ИД только при `id != null`,
формат сохранённой записи прежний. В браузере подтверждено дважды: подпись
сохранённого освидетельствования в журнале — `124 — 2026-08-01`, подпись
условия гарантии в lookup ремонта — `159 — T08-79-9 / T08 Гарантия завода`.

### 3. Браузерный проход (`playwright-cli`, четыре сессии)

Данные заведены с нуля, как требует сценарий таска: начальных данных из старой
ЕРК в работе нет ([Q02](../../../questions/Q02.md)).

**3.1. Под `user_nsi` — справочники.** В меню только «Справочники» и
«Дополнительно»; раздела «Контейнеры» нет. Группа «Справочники → Контейнеры»
показывает все 13 пунктов с русскими названиями, без сырых `msg://`. Заведены
значения, нужные сценарию: модель `T08 Модель-А` (тенты 2, дуги 4, тросы 1,
вент. 6), тип `T08 Крупнотоннажный`, размер `T08-1AAA` (длина 40 фут), обобщённая
характеристика, вид характеристики, вид файла, вид освидетельствования, вид
ремонта, причина неисправности, неисправность (причина выбрана lookup-диалогом
`CntDefectReasonListView`), тип акта, тип договора, вид гарантии. Станции
выбирались из существующего НСИ.

**3.2. Под `user_edit` — контейнеры A и B.** Реестр → «Создать». Форма содержит
все поля матрицы, включая виртуальные. Номер введён как ` fgku8001000 ` — в БД
сохранён `FGKU8001000` (нормализация на пути сохранения). После выбора модели
поля «Тенты/Дуги/Тросы/Вент. отверстия» показали 2/4/1/6, после выбора размера
«Длина (фут)» — 40, колонка «Тип» — наименование типа. Контейнер B —
`FGKU8002000` с другой датой следующего освидетельствования (2028-09-15 против
2027-03-01 у A).

**3.3. Характеристика, файл, освидетельствование.** Во вкладке «Характеристики»
карточки A диалог открылся с предзаполненным и `readOnly` полем «Контейнер»;
запись сохранена (`prop_value='Деревянный (T08)'`). Во вкладке «Файлы» загружен
PDF — в БД `file=s3://cnt_file/2026/09/15/…pdf?name=t08-doc.pdf`, файл лежит в
MinIO. Освидетельствование заведено **с отдельной страницы** `/container-surveys`
с загрузкой акта; во вкладке «Освидетельствования» карточки A оно видно как та
же запись (`id=124`) и открывается той же формой — контейнер там `readOnly`.
Акт заменён вторым файлом: `survey_act` сменился на `…?name=t08-doc2.pdf`.
`next_survey_date` контейнера A при этом не изменился (2027-03-01 при
`date_next` освидетельствования 2031-08-01).

**3.4. Ремонт по сценарию 6 инструкции.** Заведены договор `T08-79-9` и условие
гарантии (ИД 159). В форме ремонта обязательными помечены ровно «Контейнер» и
«Дата браковки». Условие гарантии найдено **поиском по ИД**: ввод `159` дал
единственный вариант `159 — T08-79-9 / T08 Гарантия завода`. Станции выбраны
типом в поле: `[075308] БРОНКА` (браковки и ремонта) и `[596506] ВОРОНЕЖ 1` (по
инструкции). В БД запись 250: `container_id=972`, `rep_contract_id=159`,
`warr_on_rep_id=159`, `rep_by_warr_id` пуст (две ссылки независимы),
`rep_st_id=1445`, `reject_st_id=1445`, `rep_st_instr_id=12604` (три станции
независимы), 9 дат, `rep_cost=31479.65`, `vat=22`, `vat_rub=6925.52`,
`cost_with_vat=38405.17`, `docs_verification_result='принято'` — всё буквально,
без пересчёта. Второй ремонт создан **из вкладки «Ремонты»** карточки: поле
«Контейнер» предзаполнено и `readOnly`, запись 251 появилась и в списке, и во
вкладке — одна entity и одна форма из двух точек входа.

**3.5. Акт на два контейнера и вкладки B.** Из вкладки «Акты» карточки A —
«Создать»: форма акта открылась с A уже в составе, добавлен B, сохранено. В БД
акт 172 и две связи (220 — A, 221 — B) одной транзакцией. У контейнера B
пройдены все шесть вкладок: «Характеристики» 0 строк, «Ремонты» 0,
«Освидетельствования» 0, «Акты» 1 строка (`T08-АКТ-AB`), «Файлы» 0, «История» —
запись «Создание» пользователя `user_edit`. Ошибок и сырых `msg://` ни на одной
вкладке нет.

**3.6. Переименование, копия, дубль.** Контейнер A переименован вводом
`fgku8001999` — в БД `FGKU8001999`, связи целы (2 ремонта, 1
освидетельствование, 1 связь акта, 1 файл). «Копировать» дало форму с
бизнес-полями и пустым номером, вкладки заменены подсказкой «Сохраните
описание — после этого станут доступны вкладки контейнера»; после сохранения под
номером `FGKU8003000` копия 976 имеет модель/тип/размер/дату оригинала и 0
дочерних записей. Попытка сохранить второй контейнер с номером `FGKU8001999`
отклонена: сообщение «Контейнер с таким номером уже существует», форма осталась
открытой с введённым значением, в БД записи не прибавилось; в логе —
`UniqueConstraintViolationHandler … constraintName=IDX_CNT_CONTAINER_CONT_NUM`.

**3.7. Документ ремонта, история, отмена.** Документ ремонта 250 загружен,
показан в `iframe` фрагмента просмотра, затем заменён вторым файлом
(`document` сменился на `…?name=t08-doc2.pdf`), кнопка «Скачать документ»
активна. Во вкладке «История» карточки A — «Создание» и «Изменение»; у
«Изменения» в нижнем гриде атрибут «Номер контейнера» со старым `FGKU8001000` и
новым `FGKU8001999`. Справочник вида ремонта переименован под `user_nsi` в
«T08 Текущий ремонт ИЗМЕНЁН» — старая запись журнала ремонта по-прежнему
содержит `repairType=T08 Текущий ремонт`, то есть подпись на момент события.
Форма ремонта открыта, заполнена датой и закрыта через «Отмена» → «Не
сохранять»: `cnt_repair` остался 2 строки, число записей `audit_entity_log` по
`cnt_%` не изменилось (2468 до и после).

**3.8. Снятие связи, возврат, запреты удаления.** «Убрать из контейнера» в
карточке A: связь 220 помечена удалённой, связь 221 у контейнера B жива, акт на
месте. «Добавить существующий» вернуло **ту же строку** `id=220` (`version=3`),
второй строки не появилось. Отказы удаления (все с русским текстом «Удаление
невозможно. Нельзя удалить запись «…»: на неё ссылаются записи «…». Сначала
удалите связанные записи.»): контейнер A → «Ремонт контейнера»; договор
`T08-79-9` → «Ремонт контейнера»; условие гарантии 159 → «Ремонт контейнера»;
справочник вида ремонта под `user_nsi` → «Ремонт контейнера».

**3.9. Мягкое удаление и журнал.** Удалены оба ремонта, освидетельствование,
характеристика, файл, снята связь акта — после этого контейнер A удалён: в БД
строка осталась с `deleted_by=user_edit` и `deleted_date`, из списка реестра
исчез (осталось «2 строки»). Попытка завести контейнер с номером удалённого
`FGKU8001999` отклонена тем же сообщением — номер уникален глобально.
В `entityLog.view` (`/audit/entity-log`, «Служебное меню → Журнал изменений»,
под `admin`) при сортировке по убыванию видны записи «Удаление»: `FGKU8001999` /
«Контейнер» / id 972, `T08-АКТ-AB / FGKU8001999` / «Контейнер в акте» / 220,
файл 61, характеристика 115, освидетельствование `124 — 2026-08-01`.

**3.10. Роли и окружение.** Под `user_read` в реестре «Создать», «Копировать»,
«Удалить» — `disabled`, «Изменить» переименована в «Просмотр» и включается
только при выделенной строке; карточка открывается только для чтения (12 из 13
текстовых полей `readonly`, тринадцатое — поиск в боковом меню), кнопки всех
вкладок `disabled`; прямой URL `/containers/new` даёт read-only форму, «OK» не
создаёт записи (`cnt_container` — 3 строки, `max(id)=976` до и после). Работают
поиск и фильтры реестра (фильтр по `8003` сузил список), настройка колонок
(меню «Показать все / Скрыть все» со всеми колонками) и скачивание файла
(`DownloaderImpl: added Anchor` в логе). **Экспорт под `user_read` отказывает —
см. «Ошибки и непроверенное».** Под `user_nsi` прямые URL `/containers`,
`/container-repairs`, `/container-acts` дают страницу «Access Denied».

Ошибки времени выполнения: error overlay не появлялся; в серверном логе 0
записей уровня `ERROR` за весь проход (два `WARN` — автоконфигурация Jmix при
старте, вне раздела); в консоли браузера только известные внешние ошибки —
аватар `info.main.vgk`, `403` на `/img/filetype/fileNotFound.png` (общий дефект
`DisplayFile`, зафиксирован в T04 и T07), `404 favicon.ico`, `400` на Keycloak
(корпоративный SSO вне контура) и `beforeunload`-предупреждения Chromium,
порождённые автоматизацией. В 520 сохранённых снимках страниц сырых `msg://`
нет.

**3.11. Уборка.** Тестовые данные удалены физически: `cnt_container`,
`cnt_repair`, `cnt_survey`, `cnt_container_property`, `cnt_container_file`,
`cnt_act`, `cnt_act_container`, `cnt_contract`, `cnt_warranty` — 0 строк; из
справочников удалены только строки, заведённые этой проверкой (`cnt_defect` —
прежние 3 строки, `cnt_act_type`, `cnt_contract_type`, `cnt_warranty_type` —
прежние 3 строки, остальные справочники вернулись к 0). Записи
`audit_entity_log` по использованным id удалены (по `cnt_%` после старта
приложения — 0 строк), `common_export_task` — 0 строк. Браузерные сессии
закрыты, `bootRun` остановлен, порт 8082 свободен. `git status --short` в
worktree — те же 36 записей, что до проверки.

## Ошибки и непроверенное

**Подтверждённая ошибка — [F01.1](../fixes/F01.md): экспорт недоступен под
контейнерными ролями.** Под `user_read` (роли `container-read` и `ui-minimal`)
«Фоновый экспорт» → «Выгрузить всё» на реестре отвечает «Доступ запрещён»; в
логе `AccessLogger: Denied access to [entity 'common_ExportTask' create, read,
update, delete] for user [user_read] by CrudEntityConstraint`. Та же операция
под `user_nsi`, у которого дополнительно назначена `async-export-role`,
завершается успешно (`common_export_task` в статусе `DONE`, файл «Тип акта -
15.09.2026.xlsx»), то есть сам механизм экспорта исправен, не хватает прав в
ролях раздела. Действие `fgk_async_excel_export` при этом стоит во всех списках
раздела, а `contracts.md` перечисляет «экспорт» среди прав `ContainerReadRole`.
Проверка T07 ([T07/checks/001.md](../../T07/checks/001.md), раздел «Ошибки и
непроверенное») зафиксировала эту двусмысленность и прямо адресовала её T08;
`result.md` T08 её не разбирает. Направление исправления — решение за пределами
постановки (дублировать политики `ExportTask` в трёх ролях против правила
проекта «политики `ExportTask` несёт единственная `AsyncExportRole`» либо
закрепить совместное назначение ролей), поэтому оформлено вопросом
[Q04](../../../questions/Q04.md).

**Не ошибка, но C5 буквально не выполнен:** `./gradlew :app:test` в этом
worktree не может быть зелёным — 49 падений в 11 легаси-классах из-за жёстко
зашитой схемы `main.` в их фикстурах (раздел 2). Тот же состав и то же число
падений зафиксированы проверками T01, T03, T04 и T07 до кода T08; в контейнерном
пакете падений нет. Считаю ограничением окружения, а не дефектом T08; если
пользователь хочет буквально зелёный прогон, правка чужих легаси-тестов —
отдельная работа.

**Не проверялось отдельно:**

- Выбор организаций («Собственник», «Исполнитель») — локальная
  `main.nsi_v_org_passport` пуста (0 строк), как и записано в plan.md; поля
  оставлены незаполненными, пустой список за проверенный выбор не выдаю. Эти же
  поля закрыты тестами T03/T07 на сеяных данных.
- Сценарии 2–5 инструкции целиком: они описывают правку тех же полей ремонта и
  файлов, что пройдены в 3.4 и 3.7, и были воспроизведены проверкой T07.
- Выпадающий список справочного поля в форме неисправности не раскрылся при
  вводе подстроки в сессии `t08nsi`; тот же механизм в сессии `t08edit` работал
  штатно (станции, условие гарантии), запись заведена через `entity_lookup`.
  Отношу к особенности headless-сессии, а не к дефекту: подтверждение —
  успешный typeahead в других сессиях того же прогона.

## Следующий шаг

Пользователь или архитектор: `task-resolve` по
[questions/Q04.md](../../../questions/Q04.md) — выбрать, как раздел получает
право на Excel-экспорт. После ответа исполнитель: `task-execute` по
[fixes/F01.md](../fixes/F01.md), затем повторная независимая проверка T08
(достаточно перепроверить экспорт под тремя ролями и прогон
`--tests "ru.fgk.ws.app.container.*"`; остальные критерии подтверждены этим
отчётом на неизменном коде).
