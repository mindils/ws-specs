# Проверка 001 — T08

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-12 04:38
Состояние кода: Ветка `feature/caseone-table` (базовый коммит `f2b96adc2 «step 1»`), изменения T08 в рабочем дереве (не закоммичены).
Окружение: Linux, Docker Compose (`rvk-db` PostgreSQL 16.11 up на порту 5432, `application-test-local.properties`, Java 21, Gradle composite build).
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Все 7 норм (`IdOper` 16, 9, 10, 11, 12, 13, 14), 5 веток назначения передачи в претензионную работу (`Srok_PassPP`), переопределения по возврату претензии (`Pret_Vozvrat_Dt > Send_PD`), семантика `coalesce(Send_PD, DatPret)` и NULL-сценарии покрыты 40 тестами в [`ClaimTermCalculatorTest.java`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/test/java/ru/fgk/ws/app/legal/warrantyrepair/service/ClaimTermCalculatorTest.java). Предварительный расчёт [`WarrantyRepairDeadlineService.preview(...)`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/legal/warrantyrepair/service/WarrantyRepairDeadlineService.java) и сохранённый расчёт идентичны, что подтверждено тестом `recalculatesWhenDepsOrDuChanges`. |
| C2 | Подтверждён | Примеры из [calendar-behavior-differences.md](../../input/calendar-behavior-differences.md) проверены тестами: старт в выходной день не сдвигает базу (`weekendStartDoesNotShiftStartToWorkingDay`), подряд идущие новогодние праздники пропускаются полностью (`consecutiveHolidaysAreFullySkipped`), рабочая суббота сокращает срок (`workingSaturdayShortensTermComparedToFiveDayWeek`), обычная неделя совпадает со старой логикой пятидневки (`calendarNormAddedWithoutWeekends`), переход через границу года рассчитывается по календарю (`yearBoundaryCalculatedByCalendar`), отсутствие календаря вызывает явное исключение [`WorkCalendarGapException`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/nsi/service/WorkCalendarGapException.java) вместо тихой пятидневки (`missingCalendarRangeIsDataErrorNotSilentFiveDayWeek`), отсутствующая норма бросает [`ClaimTermNormNotFoundException`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/nsi/service/ClaimTermNormNotFoundException.java) (`unknownNormIsAnError`). |
| C3 | Подтверждён | Автоматический пересчёт при сохранении изменений ДЭПС, ДЮ и отцепки реализован через [`WarrantyRepairDeadlineRecalculationListener.java`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/legal/warrantyrepair/service/WarrantyRepairDeadlineRecalculationListener.java) в транзакции. При неприменимости этапа сохранённые даты очищаются (`null`), строка расчёта остаётся консистентной (`inapplicableStageClearsStoredDates`). Ошибка расчёта откатывает сохранение исходных данных (`calculationErrorRollsBackSourceChange`). Рекурсивные вызовы исключены (слушатель не слушает изменения самой `WarrantyRepairDeadline`). |
| C4 | Подтверждён | Массовый пересчёт [`WarrantyRepairDeadlineService.recalculateAll()`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/legal/warrantyrepair/service/WarrantyRepairDeadlineService.java) идемпотентен (`bulkRecalculationIsIdempotent`). Прямой импорт в обход Jmix восстанавливается последующим массовым пересчётом (`directImportIsRecoveredByBulkRecalculation`). Публикация ревизии через [`WarrantyRepairDeadlinePublicationHandler.java`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/legal/warrantyrepair/service/WarrantyRepairDeadlinePublicationHandler.java) атомарно пересчитывает все претензии и проставляет новый номер ревизии (`publishingRevisionRecalculatesAndStampsNewNumber`), а при сбое расчёта публикация отменяется и сохраняются прежние согласованные данные (`failedPublicationKeepsPreviousCalculations`). |
| C5 | Подтверждён | В роли [`LegalWarrantyRepairRole.java`](file:///home/mindils/data/dev/fgk/rvk-ws/app/src/main/java/ru/fgk/ws/app/legal/warrantyrepair/security/LegalWarrantyRepairRole.java) на сущность `WarrantyRepairDeadline` предоставлены только политики чтения (`EntityPolicyAction.READ` и `@ViewPolicy`), модификация пользователю недоступна. Изменение технической таблицы выполняется сервисом под системной сессией через `systemAuthenticator.withSystem(...)` с использованием `UnconstrainedDataManager`. Календарный контекст загружается пакетно за один запрос на расчётную выборку (в `loadCalendarContext` диапазоном дат или полным снимком), SQL-запросы на каждый день или ячейку отсутствуют. |
| C6 | Подтверждён | Документ [calendar-behavior-differences.md](../../input/calendar-behavior-differences.md) дополнен разделом «Расчёт сроков (T08, 2026-09-12)», содержащим сопоставление полей и условий с процедурой `pThp_PretTehSelect2.sql`, правила вычисления дат факта и уровней просрочки для T02, контракт использования таблицы `main.dr_warranty_repair_deadlines` и регламент обязательного вызова пересчёта после прямого SQL/NiFi импорта. Результаты старой MSSQL выверены по исходному тексту процедуры с явной фиксацией отличий. |

## Выполненные проверки

1. **Гейт 1 (API, статика и форматирование):**
   - `./gradlew spotlessCheckAll` — BUILD SUCCESSFUL (все 21 таск up-to-date).
   - `./gradlew :app:compileJava` — BUILD SUCCESSFUL (код компилируется без замечаний).
   - Механическая проверка исходных файлов T08:
     - Все Java-файлы содержат корректный `package ru.fgk.ws.app.legal.warrantyrepair...`
     - 0-байтовые файлы отсутствуют.
     - Lombok ограничен аннотациями `@Getter` и `@Setter` в соответствии с конвенциями проекта.
     - Entity `WarrantyRepairDeadline` аннотирована `@JmixEntity`, `@Table(name = "dr_warranty_repair_deadlines", schema = "main")`, первичный ключ `Long id` с `@GeneratedValue(strategy = GenerationType.IDENTITY)`.
     - Запрещённый field `@Autowired` в сервисах отсутствует (используется constructor injection).
     - Циклические зависимости между `TermReferenceRevisionService` и `WarrantyRepairDeadlineService` разорваны через `ObjectProvider<WarrantyRepairDeadlineService>` в `WarrantyRepairDeadlinePublicationHandler`.

2. **Проверка схемы БД (`main.dr_warranty_repair_deadlines` в `docker-rvk-db-1`):**
   - Таблица создана changelog-ом `324-dr_warranty_repair_deadlines.xml`:
     - 37 колонок: `id`, `version`, `deps_id` (not null), `revision_number` (not null), `calculated_date` (not null), 7 четвёрок колонок дат (`*_due_date`, `*_level1_date`, `*_level2_date`, `*_level3_date`), 4 поля аудита.
     - Первичный ключ `pk_dr_warranty_repair_deadlines` на `id`.
     - Уникальное ограничение `uq_dr_warranty_repair_deadlines_deps` на `deps_id`.
     - Индекс `idx_dr_warranty_repair_deadlines_revision` на `revision_number`.
     - Идемпотентность обеспечена `<preConditions onFail="MARK_RAN">`.

3. **Гейт 2 (модульные и интеграционные тесты):**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.legal.warrantyrepair.service.ClaimTermCalculatorTest" --tests "ru.fgk.ws.app.it.WarrantyRepairDeadlineIT"`:
     - 54 passing (21.5s), 0 failures, 0 errors.
     - Модульный срез `ClaimTermCalculatorTest` (40 тестов):
       - Назначение сроков и стартовые даты (14 тестов)
       - Пять веток передачи документов в претензионную работу (7 тестов)
       - Передача в судебную работу и возврат претензии (6 тестов)
       - Календарь и нормы (6 тестов)
       - Пороги просрочки и сравнение с первоисточником (7 тестов)
     - Интеграционный срез `WarrantyRepairDeadlineIT` (14 тестов):
       - Пересчёт при изменении ДЭПС, ДЮ, отцепки
       - Очистка дат при неприменимости этапа
       - Откат транзакции при ошибке расчёта
       - Предварительный расчёт без сохранения
       - Идемпотентность массового пересчёта
       - Восстановление после прямого импорта
       - Публикация ревизий с пересчётом и сохранение при сбое
       - Безопасность: доступ только на чтение для роли `legal-warranty-repair`
       - Пакетная загрузка календаря без запросов на каждый день

## Ошибки и непроверенное

- Ошибок в коде и тестах T08 не обнаружено.
- Единственный красный тест в общем прогоне приложения — `SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail`, изолирован в задаче T09 (вопрос Q03) и не относится к расчёту гарантийных сроков.
- Гейт 3 (Flow UI render) не применим к T08, так как задача реализует доменный слой расчёта, сервисы и БД без создания новых пользовательских экранов (отображение в гридах выполняется в рамках таска T02).

## Повторная проверка

Первичная проверка итерации 1. Замечаний нет.

## Следующий шаг

Таск T08 переведён в статус `done`.
Следующий шаг: реализация задачи [T02](../T02/task.md) по обновлению представлений и гридов со сроками (`task-execute @specs/work/dr-warranty-repair-rename-ui/tasks/T02`).
