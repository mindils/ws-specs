# Проверка 001 — T07

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-12 04:05
Состояние кода: Ветка `feature/caseone-table` (базовый коммит `f2b96adc2 «step 1»`), изменения T07 в рабочем дереве (не закоммичены).
Окружение: Linux, Docker Compose (`rvk-db` PostgreSQL 16.11 up на порту 5432, `application-test-local.properties`, Java 21, Gradle composite build).
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Выполнены проверочные SQL-запросы по схемам `main` и `ws_store` в контейнере `docker-rvk-db-1`. Календаря и аналогов `nsHoliday` в обеих схемах нет; отсутствие календаря в корпоративной `ws_store` подтверждено пользователем (2026-09-12). Обоснован и выбран ровно один путь: собственная НСИ `main.nsi_work_calendar_day`. Внешняя схема не затрагивалась, фиктивная проекция не создавалась. |
| C2 | Подтверждён | Таблица `main.nsi_work_calendar_day` содержит сплошной диапазон с 2024-01-01 по 2026-12-31 (ровно 1096 строк), 0 строк с `NULL` в `working` или `day_kind`. Число рабочих дней совпадает с официальным производственным календарём РФ: 2024 — 248, 2025 — 247, 2026 — 247. Рабочие субботы (27.04.2024, 02.11.2024, 28.12.2024, 01.11.2025) зафиксированы как `WORKING_WEEKEND` с источником `xmlcalendar.ru`. Отсутствующая дата бросает `WorkCalendarGapException` с указанием даты и диапазона, не заменяясь пятидневкой. Тесты `WorkCalendarSnapshotTest` (10 проверок) и `WorkCalendarReferenceDataIT` (тесты C2) пройдены. |
| C3 | Подтверждён | Таблица `main.nsi_claim_term_norm` содержит 7 норм для гарантийных претензий `KodPret=1` (операции 16→2, 9→12, 10→10, 11→19, 12→14, 13→4, 14→14, все в рабочих днях `WORKING`). Уникальный индекс `uk_nsi_claim_term_norm` по `(claim_kind, operation_code)`. Идемпотентность доказана тестом `reRunningSeedKeepsUserChanges`, выполняющим оригинальный SQL changelog-а через JDBC: пользовательские правки сохраняются, дубли не возникают. Неизвестная норма бросает `ClaimTermNormNotFoundException` (`unknownNormIsAnError`). |
| C4 | Подтверждён | Административная роль `NsiClaimTermEditRole` (`nsi-claim-term-edit`) даёт полные права на ведение `NsiWorkCalendarDay` и `NsiClaimTermNorm`, права чтения/создания/обновления `NsiTermRevision`, а также `@ViewPolicy` на все пять новых views (`nsi_NsiWorkCalendarDay.list`, `nsi_NsiWorkCalendarDay.detail`, `nsi_NsiClaimTermNorm.list`, `nsi_NsiClaimTermNorm.detail`, `nsi_NsiTermRevision.list`) и `@MenuPolicy` на три пункта меню. Тест `NsiClaimTermEditRoleIT` подтвердил: администратор ведёт календарь и нормы; неавторизованный пользователь получает пустой результат при чтении и `AccessDeniedException` при сохранении. 77 ключей в `messages_ru.properties` закрывают все UI-элементы и сущности; скрипт сверки подтвердил отсутствие пропусков. Render-проход Playwright CLI подтвердил открытие списков и диалогов. |
| C5 | Подтверждён | Документ [calendar-behavior-differences.md](../../input/calendar-behavior-differences.md) содержит раздел «Реализация календаря (T07, 2026-09-12)», где зафиксированы источник (`xmlcalendar.ru`), диапазон покрытия (2024–2026, 1096 дней), обоснование отсутствия 2027 года (постановление о переносах на 2027 год не утверждено), а также протокол транзакционной публикации ревизий («всё или ничего») через `TermReferenceRevisionService`, слушатель `TermReferenceDataChangeListener` и контракт `TermReferencePublicationHandler` для T08. |

## Выполненные проверки

1. **Гейт 1 (API, статика и форматирование):**
   - `./gradlew spotlessCheckAll` — BUILD SUCCESSFUL (все 13 тасков up-to-date).
   - `./gradlew :app:compileJava` — BUILD SUCCESSFUL (код компилируется без ошибок).
   - Механические статические проверки по skill `jmix-ide-static-analysis`:
     - Строка `package` присутствует во всех новых Java-файлах `ru.fgk.ws.app.nsi`.
     - 0-байтовые файлы: среди созданных в T07 файлов 0-байтовых нет.
     - Корректность XML-пролога/BOM: все `*-view.xml` начинаются с XML-тега, BOM отсутствует.
     - Запрещённый `genericFilter`: в XML новых экранов отсутствует (используются `propertyFilter`).
     - Запрещённый прямой `com.vaadin.flow.component.dialog.Dialog`: отсутствует.
     - Непривязанные ручные параметры лоадеров `= :`: отсутствуют.
     - `createAction` и `editAction` в списках содержат явный `viewId` диалога (`nsi_NsiWorkCalendarDay.detail`, `nsi_NsiClaimTermNorm.detail`) и `openMode="DIALOG"`.
     - Все 26 ссылок на `msg://` из 5 дескрипторов проверены скриптом на Python и успешно резолвятся в `messages_ru.properties`.

2. **Проверка реальной БД PostgreSQL (`docker-rvk-db-1`):**
   - Календарь:
     ```sql
     select count(*), min(calendar_date), max(calendar_date) from main.nsi_work_calendar_day;
     -- count: 1096, min: 2024-01-01, max: 2026-12-31
     ```
   - Распределение рабочих дней:
     ```sql
     select extract(year from calendar_date) yr, count(*) total,
            count(*) filter (where working) work_days,
            count(*) filter (where not working) non_work
     from main.nsi_work_calendar_day group by 1 order by 1;
     -- 2024: total 366, work_days 248, non_work 118
     -- 2025: total 365, work_days 247, non_work 118
     -- 2026: total 365, work_days 247, non_work 118
     ```
   - Рабочие субботы (2024-04-27, 2024-11-02, 2024-12-28, 2025-11-01):
     `working = true`, `day_kind = 'WORKING_WEEKEND'`, `note` и `source_ref` заполнены.
   - Нормы сроков:
     ```sql
     select claim_kind, operation_code, name, term_days, day_type, stage_order
     from main.nsi_claim_term_norm order by stage_order;
     -- Ровно 7 строк (IdOper 16, 9, 10, 11, 12, 13, 14; term_days 2, 12, 10, 19, 14, 4, 14; day_type WORKING)
     ```
   - Ревизии:
     ```sql
     select * from main.nsi_term_revision;
     -- revision_number: 1, status: 'PUBLISHED', change_note заполнено
     ```

3. **Гейт 2 (модульные и интеграционные тесты):**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.nsi.service.WorkCalendarSnapshotTest"`:
     10 passing (724ms). Проверены старт в выходной день (день №1 — понедельник без смещения старта), последовательные праздники (пропускаются целиком), рабочая суббота, обычная неделя, N=0, отрицательное N, переход через границу года, выход за пределы диапазона (`WorkCalendarGapException`), границы покрытия.
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.WorkCalendarReferenceDataIT" --tests "ru.fgk.ws.app.it.NsiClaimTermEditRoleIT"`:
     15 passing (20.3s). Проверены сплошное покрытие без NULL, совпадение рабочих дней с официальным календарём, рабочие субботы и переносы из данных, ошибка при отсутствующей дате, уникальность даты и норм, повторный запуск changelog-ов с сохранением правок, ошибка неизвестной нормы, протокол ревизий, чтение и модификация под `NsiClaimTermEditRole`, отказ в доступе без роли.
   - `./gradlew :app:test` (полный тестовый набор):
     1293 passing, 12 pending, 1 failing. Единственный отказ — `SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail` (`OptimisticLockException` в маппере OIDC, фоновый дефект ветки, вынесенный в таск T09 по решению Q03). Для T07 дефект не является блокирующим и отказом не считается.

4. **Гейт 3 (рендеринг Flow UI):**
   - Проверены артефакты Playwright CLI в `.playwright-cli/`:
     - Экран `/nsi-work-calendar-days`: заголовок «Производственный календарь», плашка «Календарь наполнен с 2024-01-01 по 2026-12-31, дней: 1096. Даты вне этого диапазона дают ошибку расчёта, а не выходной», грид с данными, фильтры «Дата с» / «Дата по», диалог создания/редактирования дня.
     - Экран `/nsi-claim-term-norms`: заголовок «Нормы сроков претензионных этапов», 7 строк в гриде, диалог редактирования нормы.
     - Экран `/nsi-term-revisions`: заголовок «Ревизии календаря и норм», строка «Неопубликованных изменений нет», кнопка «Опубликовать изменения».
     - Сырых `msg://`, error overlay и серверных исключений нет.

## Ошибки и непроверенное

- Ошибок в реализации не обнаружено.
- Ограничение по 2027 году: 2027 год намеренно не заполнен в соответствии с постановкой (постановление Правительства РФ о переносах на 2027 год ещё не принято). Поведение подтверждено: при выходе расчёта за 2026-12-31 выбрасывается `WorkCalendarGapException` с указанием недостающей даты и диапазона.
- Статическая IDE-инспекция (MCP `get_file_problems`): в среде отсутствует Jmix-плагин IDE, проверка выполнена по процедуре отката (полная Java-компиляция + набор механических проверок дескрипторов + валидация `msg://` + render-проход).

## Повторная проверка

Первичная проверка итерации 1. Дефект `NoSuchViewException` на кнопках создания/редактирования, обнаруженный исполнителем в ходе render-прохода, был устранён до передачи на приёмку и подтверждён чистым в данной проверке.

## Следующий шаг

Таск T07 переведён в статус `done`.
Зависимости для следующего таска T08 (`T01, T07`) полностью удовлетворены (T01 — `done`, T07 — `done`).
Следующий шаг: реализация таска [T08](../T08/task.md) исполнителем (`task-execute @specs/work/dr-warranty-repair-rename-ui/tasks/T08`).
Параллельно может выполняться независимый таск [T09](../T09/task.md) по устранению гонки в маппере OIDC.
