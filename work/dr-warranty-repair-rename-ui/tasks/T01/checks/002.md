# Проверка 002 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 2
Дата: 2026-09-11 16:55
Состояние кода: Ветка `feature/caseone-table` (базовый коммит слияния `f2b96adc2 «step 1»`), изменения T01 в рабочем дереве (не закоммичены).
Окружение: Linux, Docker Compose (`rvk-db` PostgreSQL 16.11 up на порту 5432, `application-test-local.properties`).
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | `grep -rn 'legal_warranty_repair' app/src` и `grep -rni 'legal_warranty_repair' app/src` — вывод пуст (exit code 1). `grep -rn 'LEGAL_WARRANTY_REPAIR' app/src` — вывод пуст. |
| C2 | Подтверждён | Скрипт `checks/verify.py`: старых имён колонок и полей в области таска (4 changelog-а, пакет `ru.fgk.ws.app.legal.warrantyrepair`, блок ключей `…warrantyrepair…` в `messages_ru.properties`) нет. Каждое новое имя присутствует в changelog, entity и i18n. |
| C3 | Подтверждён | Выполнена полная очистка таблиц задачи в PostgreSQL (`drop table ... cascade` и `delete from main.databasechangelog`). Liquibase применён с нуля при прогоне теста `UserTest`: созданы 4 таблицы `dr_warranty_repair` (49 колонок), `dr_warranty_repair_deps` (76 колонок), `dr_warranty_repair_du` (34 колонки), `dr_warranty_repair_case_one` (16 колонок). Суммарно: 175 колонок. Таблиц `legal_warranty_repair*` в схеме `main` нет. |
| C4 | Подтверждён | `checks/verify.py` подтвердил совпадение 175 имён колонок между changelog и entity. Проверка типов по дампу БД `information_schema.columns` подтвердила полное совпадение типов, длин строк и precision/scale (175 из 175). |
| C5 | Подтверждён | По `pg_indexes`: индекс `idx_dr_warranty_repair_wag_defect` создан уникальным (`CREATE UNIQUE INDEX idx_dr_warranty_repair_wag_defect ON main.dr_warranty_repair USING btree (wagnum, defect_date)`); частичный уникальный индекс сохранён под новым именем `uq_dr_warranty_repair_deps_claim` (`... (repair_uid, claim_index) WHERE (deleted_date IS NULL)`). |
| C6 | Подтверждён | `checks/verify.py`: в `messages_ru.properties` осиротевших ключей `…warrantyrepair…` нет, полей entity без ключа нет; ровно 207 ключей. |

## Выполненные проверки

1. **Гейт 1 (API и статика):**
   - `./gradlew :app:compileJava` — BUILD SUCCESSFUL.
   - `./gradlew spotlessCheckAll` — BUILD SUCCESSFUL.
2. **Критерий C1:**
   - `grep -rni 'legal_warranty_repair' app/src` — совпадений нет.
   - `grep -rn 'LEGAL_WARRANTY_REPAIR' app/src` — совпадений нет.
3. **Критерии C2, C4 (имена), C6:**
   - `python3 specs/work/dr-warranty-repair-rename-ui/tasks/T01/checks/verify.py` — вывод:
     `C1 legal_warranty_repair в app/src: НЕТ ВХОЖДЕНИЙ`
     `C2: OK`
     `C4 сверка имён entity<->changelog: OK (Всего колонок: 175)`
     `C6: OK`
     `ИТОГ: все статические критерии OK`.
4. **Критерий C3 (Liquibase с нуля) и C5 (индексы):**
   - Выполнен сброс в PostgreSQL:
     `drop table if exists main.dr_warranty_repair_case_one, main.dr_warranty_repair_du, main.dr_warranty_repair_deps, main.dr_warranty_repair, main.legal_warranty_repair_case_one, main.legal_warranty_repair_du, main.legal_warranty_repair_deps, main.legal_warranty_repair cascade;`
     `delete from main.databasechangelog where filename like '%warranty_repair%';`
   - Запущен `./gradlew :app:test --tests "ru.fgk.ws.app.user.UserTest"` — контекст Spring/Jmix и миграции Liquibase отработали успешно (BUILD SUCCESSFUL).
   - Запрос к `information_schema.columns` подтвердил наличие 4 таблиц:
     `dr_warranty_repair` (49), `dr_warranty_repair_case_one` (16), `dr_warranty_repair_deps` (76), `dr_warranty_repair_du` (34), всего 175 колонок.
   - Запрос к `pg_indexes` подтвердил создание уникального индекса `idx_dr_warranty_repair_wag_defect` и частичного уникального индекса `uq_dr_warranty_repair_deps_claim`.
5. **Критерий C4 (типы данных entity ↔ PostgreSQL):**
   - Дамп структуры `information_schema.columns` в `/tmp/dbcols_check.txt`.
   - Прогон скрипта проверки типов: `Сверено колонок: 175 из 175; C4 типы: OK`.
6. **Гейт 2 (загрузка контекста и тесты):**
   - `./gradlew :app:test` — выполнено 1278 тестов: 1265 passing, 12 pending, 1 failing.
   - Контекст Spring/Jmix и все тесты доменных модулей приложения (`dr`, `da`, `pt`, `rp`, `wagons`, `user` и др.) поднимаются и проходят успешно.
   - Единственный упавший тест: `ru.fgk.ws.app.sso.SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail` (`OptimisticLockException` в маппере OIDC). Относится к фоновому дефекту ветки по SSO, вынесен в [Q03](../../questions/Q03.md), воспроизводится на базовой линии ветки без изменений T01 и к гарантийным ремонтам отношения не имеет.
7. **Гейт 3 (рендеринг Flow UI):**
   - Не применим (в таске T01 views не создавались).

## Ошибки и непроверенное

Ошибок в реализации таска T01 не выявлено.
Все 6 критериев C1–C6 подтверждены фактическими доказательствами.
Замечание по формулировке C3 в постановке: фактически в 4 таблицах создано 175 колонок (число 197 колонок в плане и исходной постановке было расчетной оценкой до выгрузки структуры). Состав и типы всех 175 колонок соответствуют целевой модели.

## Повторная проверка

В итерации 1 проверка была заблокирована (`blocked`, отчёт [checks/001.md](001.md)), так как слияние с `main` не было закоммичено, а наработки находились в стеше.
В итерации 2:
- Коммит слияния с `main` выполнен (`f2b96adc2`).
- Наработки T01 перенесены в рабочее дерево ветки `feature/caseone-table`.
- Все предварительные препятствия устранены, критерии C1–C6 полностью подтверждены на чистой БД.

## Следующий шаг

Таск T01 переведён в статус `done`.
Следующие доступные шаги:
1. Закоммитить наработки T01 в ветку `feature/caseone-table`.
2. Перейти к реализации независимого таска [T07](../tasks/T07/task.md) («Источник полного календаря, его наполнение и НСИ норм») либо после T07 — к [T08](../tasks/T08/task.md) / [T02](../tasks/T02/task.md).
3. При необходимости рассмотреть открытый вопрос [Q03](../../questions/Q03.md) по формулировке гейта 2 через `task-resolve`.
