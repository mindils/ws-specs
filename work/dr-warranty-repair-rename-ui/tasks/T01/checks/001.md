# Проверка 001 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-11 16:30
Состояние кода: Ветка `feature/caseone-table` в состоянии незавершённого слияния (MERGE_HEAD = `a9b082e2f` в `dd71c094f`, `git commit` не выполнен). Наработки T01 не накатаны на рабочее дерево и сохранены в `stash@{0}`.
Окружение: Linux, Docker Compose (`rvk-db` PostgreSQL 16.11 up на порту 5432)
Итог: blocked

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | не проверен | Реализация не завершена в рабочем дереве: изменения T01 находятся в `stash@{0}`. В рабочем каталоге остаются файлы `320-legal_warranty_repair.xml` и класс `LegalWarrantyRepair.java` со старыми именами. |
| C2 | не проверен | Изменения T01 не накатаны на ветку из `stash@{0}`. |
| C3 | не проверен | В рабочем дереве changelog-и не содержат новых имен; в локальной БД присутствуют как `dr_*`, так и `legal_*` таблицы от предварительных ручных прогонов. |
| C4 | не проверен | Entity и changelog в рабочем дереве не обновлены. |
| C5 | не проверен | Изменения индексов не накатаны на рабочее дерево. |
| C6 | не проверен | Обновления `messages_ru.properties` находятся в `stash@{0}`. |

## Выполненные проверки

1. Проверка состояния git: `git status` — зафиксировано состояние незавершённого слияния:
   `Все конфликты исправлены, но вы все еще в процессе слияния. (используйте «git commit», чтобы завершить слияние)`.
   MERGE_HEAD: `a9b082e2f7b0f3d33ca49c23aa1e9fd8fe54de6b`.
2. Проверка наличия изменений T01 в рабочем дереве:
   - `ls app/src/main/resources/ru/fgk/ws/app/liquibase/changelog/01-tbl/*warranty*` — файлы `320-legal_warranty_repair.xml` .. `323-legal_warranty_repair_case_one.xml` присутствуют со старыми именами.
   - `LegalWarrantyRepair.java` содержит `@Table(name = "legal_warranty_repair")`, поле `repairAcceptDate`, `inventoryDate` вместо `repairArrivalDate`, `insertVagtkDate`.
3. Проверка `git stash`: в `stash@{0}` обнаружены изменения T01 (файлы `320-dr_warranty_repair.xml` .. `323-dr_warranty_repair_case_one.xml`, сущности `LegalWarrantyRepair*.java`, `messages_ru.properties`).
4. Проверка записей задачи: в `task.md` (строки 268–270) и `Q02.md` (строки 50–56) прямо указано:
   «Остаётся накатить наработки T01 (переименования таблиц/полей, сохранённые в stash) на обновлённую ветку, выполнить проверки и передать на task-verify».
5. Проверка состояния БД: `docker exec docker-rvk-db-1 psql ...` — в схеме `main` одновременно присутствуют таблицы `legal_warranty_repair*` и `dr_warranty_repair*`.

## Ошибки и непроверенное

Реализация таска T01 не завершена в коде ветки `feature/caseone-table`.
В соответствии с п. 1 регламента `task-verify`:
«Если переданного результата нет или реализация ещё не завершена, запиши `blocked` с причиной и следующим шагом исполнителю; не выдавай предварительные проверки за приёмку».

Препятствия:
1. Незавершённый коммит слияния `main` в ветку `feature/caseone-table`.
2. Наработки T01 сохранены в `stash@{0}` и не применены к кодовой базе.
3. Отсутствует актуализированный `result.md` для ветки `feature/caseone-table` (текущий `result.md` описывает итерацию 1 на устаревшей базе коммита `a72fc2ba3` с заблокированным Gate 2).

## Повторная проверка

Не применима: это первая проверка (001).

## Следующий шаг

Исполнитель (`task-execute`):
1. Завершить слияние в git: выполнить коммит слияния (`git commit`).
2. Применить наработки T01 из stash на ветку `feature/caseone-table` (`git stash pop` / `git stash apply stash@{0}`).
3. Очистить таблицы гарантийных ремонтов в локальной БД перед прогоном:
   ```sql
   drop table if exists main.dr_warranty_repair_case_one, main.dr_warranty_repair_du,
     main.dr_warranty_repair_deps, main.dr_warranty_repair,
     main.legal_warranty_repair_case_one, main.legal_warranty_repair_du,
     main.legal_warranty_repair_deps, main.legal_warranty_repair cascade;
   delete from main.databasechangelog where filename like '%warranty_repair%';
   ```
4. Запустить проверки C1–C6, `./gradlew :app:compileJava`, `./gradlew spotlessCheckAll`, `./gradlew :app:test`.
5. Обновить [result.md](../result.md) (итерация 2) и передать таск на повторную проверку `task-verify`.
