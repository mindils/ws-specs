# Проверка 001 — T09

Таск: [../task.md](../task.md)
Результат реализации: [../result.md](../result.md)
Итерация: 1
Дата: 2026-09-12
Состояние кода: ветка `feature/caseone-table`, HEAD `f2b96adc2 «step 1»`. Изменения T09
не закоммичены, проверялись по рабочему дереву. Проверенные файлы T09:
`sso-plagin/sso-plagin/src/main/java/ru/fgk/sso/impl/SsoSynchronizingOidcUserMapper.java`
(md5 `ee57e0a7e1a9cba0685d1027ebe46da8`),
`sso-plagin/sso-plagin/src/test/java/ru/fgk/sso/impl/SsoSynchronizingOidcUserMapperTest.java` (новый),
`sso-plagin/sso-plagin/src/test/java/ru/fgk/sso/{SsoTest.java,SsoTestConfiguration.java}`,
`sso-plagin/sso-plagin/src/test/resources/ru/fgk/sso/{test-app.properties,liquibase/changelog-test.xml}`,
`sso-plagin/sso-plagin/sso-plagin.gradle`.
В дереве также лежат незакоммиченные наработки T01/T02/T07/T08 (`legal.warrantyrepair`, `nsi`),
их T09 не затрагивает; полный `:app:test` прогнан на этом же дереве.
Окружение: `docker info` — OK; `docker-rvk-db-1` (postgres 16.11) на `0.0.0.0:5432` (Up 13 hours),
`application-test-local.properties` → `jdbc:postgresql://localhost:5432/postgres?currentSchema=main`.
Gradle 9.5.1, JDK 21. Keycloak и браузер не требовались.
Итог: pass

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 — три зелёных прогона конкурентного IT подряд | подтверждён | `./gradlew :app:test --tests "…SsoOidcUserMapperConcurrencyIT" --rerun-tasks` ×3 → `1 passing` (18.7 s / 18 s / 19.6 s), `BUILD SUCCESSFUL`; XML каждого прогона `tests="1" failures="0" errors="0"`. Четвёртый зелёный прогон — внутри полного `:app:test` |
| C2 — полный `:app:test` без отказов | подтверждён | `./gradlew :app:test` → exit 0, `BUILD SUCCESSFUL in 1m 43s`, `1348 passing, 12 pending`; агрегат по 155 XML-файлам: `tests=1360 skipped=12 failures=0 errors=0`, падающих файлов нет |
| C3 — сценарий и утверждения теста сохранены | подтверждён | `git diff HEAD -- app/src/test/java/ru/fgk/ws/app/sso/` — пусто: файл байт-в-байт равен версии в HEAD. В нём по-прежнему `THREADS = 6`, `new String(username)`, `assertEquals(1L, count)`, retry внутри теста нет |
| C4 — одиночный логин не деградировал | подтверждён | `./gradlew :sso-plagin:sso-plagin:test --rerun-tasks` → `BUILD SUCCESSFUL`, 6 тестов / 0 failures (`SsoSynchronizingOidcUserMapperTest` 5, `SsoTest` 1). Отсутствие повторного сохранения без конкуренции закрывает `singleLoginSavesOnce`: `verify(dataManager, times(1)).save(...)`, `verify(userRepository, times(1)).loadUserByUsername(...)` |
| C5 — изменения только в `sso-plagin` | подтверждён | `git status --porcelain -- app/src \| grep -i sso` — пусто. Все изменения в `app/src` относятся к `legal.warrantyrepair` и `nsi` (T01/T02/T07/T08) |
| C6 — при исчерпании попыток исключение пробрасывается | подтверждён | Код: `syncJmixUser` повторяет только при `isVersionConflict`, после `MAX_SYNC_ATTEMPTS` выполняет `throw lastConflict`. Тест `propagatesConflictWhenAttemptsExhausted` — `assertSame(failure, thrown)` и `times(MAX_SYNC_ATTEMPTS)` на `save`; `doesNotRetryUnrelatedFailure` — обычная ошибка уходит наружу с первой попытки |

## Выполненные проверки

Гейт 1 (статика): `./gradlew :app:compileJava spotlessCheckAll` → `BUILD SUCCESSFUL`
(21 task, все up-to-date относительно текущих исходников). Инспекция IDE
(`get_file_problems`) не выполнялась — MCP JetBrains в этой сессии не подключён;
замена — `compileJava` + `spotlessCheckAll`. Покрытие замены достаточно: новых
`*-view.xml` в таске нет, единственный новый XML (`changelog-test.xml`) реально
применяется Liquibase при зелёном `SsoTest`, то есть проверен исполнением, а не
только статически.

Гейт 2: полный `./gradlew :app:test` — exit 0, `1348 passing, 12 pending, 0 failing`.
Прогон не был `UP-TO-DATE`: отработали 155 XML-отчётов, файл
`TEST-…SsoOidcUserMapperConcurrencyIT.xml` перезаписан в 04:59:22 внутри этого прогона.

Гейт 3 не применим: views в таске не создаются.

C1, три прогона подряд с `--rerun-tasks` (без него Gradle отдаёт `UP-TO-DATE`):

```
########## RUN 1 ##########  1 passing (18.7s)   BUILD SUCCESSFUL in 51s
########## RUN 2 ##########  1 passing (18s)     BUILD SUCCESSFUL in 49s
########## RUN 3 ##########  1 passing (19.6s)   BUILD SUCCESSFUL in 52s
```

**Негативный контроль (проверка того, что зелёный даёт именно исправление, а не
окружение).** Временно откачен ровно один файл — `SsoSynchronizingOidcUserMapper.java`
(`git checkout HEAD -- <файл>`), остальное дерево не тронуто; прогон того же теста:

```
> Task :app:test FAILED
0 passing (18.9s) / 1 failing / 1 test completed, 1 failed
java.util.concurrent.ExecutionException: jakarta.persistence.OptimisticLockException:
  Exception [EclipseLink-5006] … The object [ru.fgk.sso.entity.User-01a094d6-… [managed]]
  cannot be updated because it has changed or been deleted since it was last read
```

Файл сразу восстановлен из копии в scratchpad, совпадение подтверждено по md5
(`ee57e0a7e1a9cba0685d1027ebe46da8`), `git status` по нему снова `M`. Отказ
воспроизводится точно тем стектрейсом, что описан в постановке, то есть тест
действительно проверяет заявленное поведение, а повтор + канонический замок —
именно то, что переводит его в зелёный.

Разбор кода (не только запуск): повтор навешан на `toJmixUser`, каждая попытка
идёт через `super.toJmixUser`, то есть заново читает пользователя через
`UserRepository.loadUserByUsername` — свежая версия, а не повторное сохранение
устаревшего экземпляра (это подтверждено и тестом
`retriesAndReloadsUserOnVersionConflict`: `times(3)` на `loadUserByUsername`).
`isVersionConflict` идёт по цепочке причин и защищён от самоссылающегося
`getCause()`. Замок — фиксированные 64 `ReentrantLock` по `Math.floorMod(hashCode, 64)`,
не `String.intern()` и не растущий реестр от внешнего ввода; снимается в `finally`.
`setSynchronizeRoleAssignments(true)` сохранён, `getApplicationUserClass` и
`populateUserAttributes` не изменены, код `io.jmix.oidc` не патчен.

## Ошибки и непроверенное

Подтверждённых ошибок нет.

Не проверено (не влияет на критерии):

- Инспекция IDE по изменённым файлам — нет подключения MCP JetBrains; заменена
  на `compileJava` + `spotlessCheckAll`, см. выше.
- Поведение при нескольких экземплярах приложения (второй уровень защиты —
  замок — работает только внутри JVM, между экземплярами спасает повтор).
  Это граница, явно принятая постановкой; отдельного стенда с двумя
  экземплярами не поднималось.
- Реальный логин через Keycloak не выполнялся: критерии таска этого не требуют,
  поведение одиночного логина закрыто `singleLoginSavesOnce` и зелёным `SsoTest`.

Замечание без статуса ошибки: `SsoTest` теперь поднимает контекст с исключённым
OAuth2-автоконфигом клиента и моками `JwtDecoder` / `ClientRegistrationRepository`.
Это сужает покрытие контекстного теста аддона, но не ослабляет ранее работавший
гейт — до T09 задача `:sso-plagin:sso-plagin:test` не запускалась вовсе (нет
launcher-а JUnit Platform на runtime-classpath при Gradle 9), что подтверждается
диффом `sso-plagin.gradle`. Изменение — чистое улучшение, отдельного исправления
не требует.

## Следующий шаг

T09 → `done`. Пункт 5 «Требуемого результата» плана закрыт: постоянного
исключения из гейта 2 у работы больше нет, `:app:test` держится строго зелёным
(1348 passing, 0 failing) на текущем рабочем дереве.

Следующий доступный таск — исполнитель, `task-execute` по
`specs/work/dr-warranty-repair-rename-ui/tasks/T02/task.md` (статус `todo`;
T03–T06 также `todo`, порядок и зависимости — по `plan.md`).
