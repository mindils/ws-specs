# F01 — Падение теста SsoOidcUserMapperConcurrencyIT в общем наборе :app:test

Статус: done
Таск: [task.md](../task.md)
Обнаружено: [checks/001.md](../checks/001.md)
Повторная проверка: выполнена — [checks/002.md](../checks/002.md) (pass)

## Замечания

### F01.1 — Падение concurrentLoginsOfTheSameUserDoNotFail при выполнении :app:test

Критерий: C5 (`./gradlew :app:test` — 0 failing)
Воспроизведение:
1. Запустить `./gradlew :app:test` или
   `./gradlew :app:test --tests "ru.fgk.ws.app.sso.SsoOidcUserMapperConcurrencyIT"`.
Ожидалось:
Все тесты набора `app` проходят успешно (0 failing).
Получено:
1 failing test из 1360 (1347 passed, 12 skipped, 1 failed):
```
ru.fgk.ws.app.sso.SsoOidcUserMapperConcurrencyIT > concurrentLoginsOfTheSameUserDoNotFail() FAILED
java.util.concurrent.ExecutionException: jakarta.persistence.OptimisticLockException: Exception [EclipseLink-5006]
The object [ru.fgk.sso.entity.User-... [managed]] cannot be updated because it has changed or been deleted since it was last read.
```
Доказательство:
Лог запуска `:app:test` и отчёт `app/build/reports/tests/test/index.html`.
Причина:
Тест `SsoOidcUserMapperConcurrencyIT` был добавлен в `app` (коммит
`dd71c094f`) в рамках T09 предыдущей работы `dr-warranty-repair-rename-ui` для
проверки устранения гонки синхронизации OIDC-пользователя. Реализация
исправления в модуле `sso-plagin` лежит в `stash@{1}`.

Решение по [Q02](../../../questions/Q02.md) (ответ пользователя 2026-09-14):
код OIDC отложен намеренно и из stash не возвращается, а тест снимается —
без исправления он краснит каждый прогон `:app:test` в этой работе.

## Как исправлять

1. Удалить файл
   `app/src/test/java/ru/fgk/ws/app/sso/SsoOidcUserMapperConcurrencyIT.java`
   (`git rm`). Больше в `app/src/**/sso` ничего не трогать: там остаётся
   только changelog `01-tbl/300-sso_user_app_ext.xml`.
2. Модуль `sso-plagin`, `docker/`, `.gitignore`, `application*.properties` и
   `system/worktree-*.sh` не трогать — зона пользователя.
3. Stash не изменять и не удалять: `stash@{0}`, `stash@{1}` — единственный
   источник отложенного кода OIDC. Возврат теста вместе с ним, когда
   пользователь вернётся к задаче:
   `git checkout dd71c094f -- app/src/test/java/ru/fgk/ws/app/sso/SsoOidcUserMapperConcurrencyIT.java`.

## Повторная проверка

1. `git stash list` — 8 записей, ни одна не изменена.
2. `./gradlew :app:test` — 0 failing.
3. Тесты `ru.fgk.ws.app.legal.techclaim.*`, `TechClaimDeadlineIT`,
   `TechClaimViewIT`, `WorkCalendarReferenceDataIT`, `NsiClaimTermEditRoleIT`
   по-прежнему выполняются и зелёные.

## Прогресс и продолжение

- [x] Разрешить [Q02](../../../questions/Q02.md) (решено 2026-09-14: из stash
      ничего не восстанавливаем).
- [x] Удалить отложенный тест `SsoOidcUserMapperConcurrencyIT` (сделано
      сессией `task-resolve` 2026-09-14: `git rm`, файл снят с индекса и из
      дерева, каталог `app/src/test/java/ru/fgk/ws/app/sso/` пуст).
- [x] Добиться 0 failing в `./gradlew :app:test`.
- [x] Передать итерацию 2 на проверку.
- [x] Повторная независимая проверка: отчёт [checks/002.md](../checks/002.md) —
      `pass`, замечание F01.1 устранено.

Ближайший шаг: таск завершён, переход к следующим таскам плана ([T02](../../tasks/T02/task.md),
[T03](../../tasks/T03/task.md)).
Препятствия: нет.
Результат исправления: [result.md](../result.md), итерация 2. Каталога
`app/src/test/java/ru/fgk/ws/app/sso/` в дереве нет, удаление файла стоит в
индексе. `./gradlew :app:test` — 1347 passing, 12 pending, 0 failing.
`git stash list` — прежние восемь записей, ни одна не изменена. Тесты
раздела и регрессии в этом же прогоне зелёные: `TechClaimDeadlineIT` 14,
`TechClaimViewIT` 3, `ClaimTermCalculatorTest` 6,
`WorkCalendarReferenceDataIT` 12, `NsiClaimTermEditRoleIT` 3 — без падений и
пропусков.
