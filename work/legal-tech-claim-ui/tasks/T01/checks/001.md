# Проверка 001 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-14 10:45
Состояние кода: ветка feature/caseone-table, незакоммиченные файлы переименования раздела в legal_tech_claim и восстановленной части stash@{1}
Окружение: Docker Compose (docker-rvk-db-1 PostgreSQL 16.11 на 127.0.0.1:5432), Gradle wrapper (services.gradle.org), OpenJDK 21.0.12, Linux x86_64; изоляция: единственный последовательный запуск тестов на локальной БД
Итог: fail — провален критерий C5: 1 failing test в наборе :app:test (SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail)

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | `grep -rniE "warranty_?repair\|warrantyrepair" app/src` выявил только разрешённые остатки: колонки `warranty_repair`, `warranty_repair_cost`, `warranty_*` виновника, enum `WarrantyRepairEnum` из модуля `dr`, вызовы в других модулях (`dr`, `rp`, `wagons`, `drcontract`) и текст в комментарии view. Ни одного старого имени таблицы, класса, пакета, Jmix-имени, константы БД или ключа i18n в коде раздела нет |
| C2 | Подтверждён | Запрос к `information_schema.columns` подтвердил наличие таблиц `legal_tech_claim` (49 колонок), `legal_tech_claim_deps` (76), `legal_tech_claim_du` (34), `legal_tech_claim_case_one` (16), `legal_tech_claim_deadlines` (37) и view `legal_v_tech_claim` (104). Запрос по `information_schema.tables` и `information_schema.views` на шаблон `%warranty_repair%` вернул 0 строк |
| C3 | Подтверждён | Запрос к `pg_indexes` подтвердил создание всех 6 целевых индексов: `idx_legal_tech_claim_wag_defect` (UNIQUE), `uq_legal_tech_claim_deps_claim` (UNIQUE частичный `WHERE deleted_date IS NULL`), `uq_legal_tech_claim_du_deps`, `uq_legal_tech_claim_case_one_deps`, `uq_legal_tech_claim_deadlines_deps`, `idx_legal_tech_claim_deadlines_revision` |
| C4 | Подтверждён | В `messages_ru.properties` зарегистрировано ровно 354 ключа с префиксом `ru.fgk.ws.app.legal.techclaim.entity/`. Скрипт проверки по AST/структуре классов сущностей и перечислений подтвердил 0 отсутствующих и 0 осиротевших ключей |
| C5 | Ошибка | Выполнение полного набора `./gradlew :app:test` завершилось с кодом 1: из 1360 тестов 1347 passed, 12 skipped, 1 failed (`SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail()`). Сами переименованные тесты (`ru.fgk.ws.app.legal.techclaim.*`, `TechClaimDeadlineIT`, `TechClaimViewIT` — 69 тестов) и регрессионные тесты модуля nsi (`WorkCalendarReferenceDataIT`, `NsiClaimTermEditRoleIT`) зелёные |
| C6 | Подтверждён | Поиск `dr_warranty_repair` и `WarrantyRepairDeadline` в файле `specs/work/legal-tech-claim-ui/input/calendar-behavior-differences.md` дал 0 совпадений; документ ссылается на новую схему `legal_tech_claim` |

## Выполненные проверки

1. **Компиляция и статический анализ:**
   `./gradlew :app:compileJava :app:compileTestJava spotlessCheckAll` —
   успешно, код 0.
2. **Критерий C1 (grep допустимых остатков):**
   Команда `grep -rniE "warranty_?repair|warrantyrepair" app/src | grep -viE ...`
   показала лишь вхождения в легаси-модулях `dr`, `rp`, `wagons`, `drcontract`,
   колонок `warranty_repair` / `warranty_repair_cost`, и `WarrantyRepairEnum`.
   Поиск файлов со старыми именами в `app/src` находит только `dr/entity/WarrantyRepairEnum.java`.
3. **Критерий C2 (структура БД):**
   `docker exec docker-rvk-db-1 psql -U root -d postgres -c "select table_name, count(*) from information_schema.columns where table_schema='main' and (table_name like 'legal_tech_claim%' or table_name='legal_v_tech_claim') group by 1 order by 1;"`
   - `legal_tech_claim`: 49
   - `legal_tech_claim_case_one`: 16
   - `legal_tech_claim_deadlines`: 37
   - `legal_tech_claim_deps`: 76
   - `legal_tech_claim_du`: 34
   - `legal_v_tech_claim`: 104
   Проверка на таблицы и view с именем `%warranty_repair%` вернула 0.
4. **Критерий C3 (индексы):**
   `docker exec docker-rvk-db-1 psql -U root -d postgres -c "select tablename, indexname, indexdef from pg_indexes where schemaname='main' and tablename like 'legal_tech_claim%' order by 1, 2;"`
   Все индексы, включая уникальные и частичный `WHERE (deleted_date IS NULL)`,
   на месте и соответствуют спецификации.
5. **Критерий C4 (i18n):**
   Проверка скриптом всех 6 классов сущностей и 8 перечислений против
   `messages_ru.properties` подтвердила точное соответствие 354 ключей
   (0 missing, 0 orphaned).
6. **Тесты раздела и регрессии:**
   `./gradlew :app:test --tests "ru.fgk.ws.app.legal.techclaim.*" --tests "ru.fgk.ws.app.it.TechClaimDeadlineIT" --tests "ru.fgk.ws.app.it.TechClaimViewIT" --tests "ru.fgk.ws.app.it.WorkCalendarReferenceDataIT" --tests "ru.fgk.ws.app.it.NsiClaimTermEditRoleIT"`
   Результат: 72 passing (25.5s), 0 failing.
7. **Полный набор тестов (критерий C5):**
   `./gradlew :app:test`
   Результат: BUILD FAILED, 1347 passing, 12 pending, 1 failing.
   Упавший тест: `ru.fgk.ws.app.sso.SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail()`.
   Изолированный перезапуск `./gradlew :app:test --tests "ru.fgk.ws.app.sso.SsoOidcUserMapperConcurrencyIT"`
   стабильно воспроизводит ошибку `OptimisticLockException`.
8. **Критерий C6 (документ отличий):**
   Проверка `grep_search` в `input/calendar-behavior-differences.md` по шаблонам
   `dr_warranty_repair` и `WarrantyRepairDeadline` вернула 0 результатов.

## Ошибки и непроверенное

- Ошибка критерия C5: в общем наборе `:app:test` падает тест конкурентной
  синхронизации OIDC-пользователя `SsoOidcUserMapperConcurrencyIT`. Падение
  вызвано тем, что код исправления T09 из аддона `sso-plagin` остался в
  `stash@{1}` и не был перенесён в рабочее дерево вместе с остальными
  файлами. Оформлено в [fixes/F01.md](../fixes/F01.md).
- Вопрос [Q02](../../questions/Q02.md), в котором исполнитель предположил,
  что отсутствие кода T09 не влияет на `:app:test`, фактически блокирует
  выполнение критерия C5 и требует решения пользователя.

## Повторная проверка

Не применимо (первая итерация проверки).

## Следующий шаг

Пользователь отвечает на вопрос [Q02](../../questions/Q02.md) о способе
восстановления остатка из `stash@{1}`. Затем исполнитель по [fixes/F01.md](../fixes/F01.md)
восстанавливает функционал конкурентной синхронизации в `sso-plagin`,
добивается 0 failing в `./gradlew :app:test` и передаёт таск на повторную
проверку.
