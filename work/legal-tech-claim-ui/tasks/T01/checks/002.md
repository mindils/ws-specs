# Проверка 002 — T01

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Исправление: [fixes/F01.md](../fixes/F01.md)
Итерация: 2
Дата: 2026-09-14 12:00
Состояние кода: ветка feature/caseone-table; снят тест SsoOidcUserMapperConcurrencyIT по исправлению F01; незакоммиченные файлы переименования раздела в legal_tech_claim
Окружение: Docker Compose (docker-rvk-db-1 PostgreSQL 16.11 на 127.0.0.1:5432), Gradle wrapper (services.gradle.org), OpenJDK 21.0.12, Linux x86_64; изоляция: единственный последовательный запуск тестов на локальной БД
Итог: pass — все критерии C1–C6 подтверждены; замечание F01 устранено (отложенный тест SsoOidcUserMapperConcurrencyIT удалён по решению Q02); полный набор :app:test завершился с кодом 0 (1347 passed, 12 pending, 0 failed); stash не затронут

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Повторный grep по `app/src` подтвердил отсутствие старых имён таблиц, классов, пакетов, констант БД или ключей i18n. Все оставшиеся совпадения относятся к разрешённым колонкам (`warranty_repair`, `warranty_repair_cost`), колонкам виновника, перечислению `WarrantyRepairEnum` из модуля `dr` и упоминаниям в легаси-модулях |
| C2 | Подтверждён | Запрос в БД `information_schema.columns` подтвердил наличие таблиц `legal_tech_claim` (49 колонок), `legal_tech_claim_deps` (76), `legal_tech_claim_du` (34), `legal_tech_claim_case_one` (16), `legal_tech_claim_deadlines` (37) и view `legal_v_tech_claim` (104). Запрос по `information_schema.tables` на шаблон `%warranty_repair%` в схеме `main` возвращает 0 |
| C3 | Подтверждён | Запрос в БД `pg_indexes` подтвердил наличие всех шести целевых индексов: `idx_legal_tech_claim_wag_defect` (UNIQUE), `uq_legal_tech_claim_deps_claim` (UNIQUE частичный `WHERE deleted_date IS NULL`), `uq_legal_tech_claim_du_deps`, `uq_legal_tech_claim_case_one_deps`, `uq_legal_tech_claim_deadlines_deps`, `idx_legal_tech_claim_deadlines_revision` |
| C4 | Подтверждён | В `messages_ru.properties` зарегистрировано ровно 354 ключа с префиксом `ru.fgk.ws.app.legal.techclaim.entity/`. Проверочный скрипт против классов сущностей и перечислений подтвердил 0 отсутствующих и 0 осиротевших ключей |
| C5 | Подтверждён | Выполнение `./gradlew :app:test` завершилось успешно: 1347 passing, 12 pending, 0 failing. Переименованные тесты раздела (`ru.fgk.ws.app.legal.techclaim.*`, `TechClaimDeadlineIT` — 14 тестов, `TechClaimViewIT` — 3 теста), а также регрессионные тесты (`WorkCalendarReferenceDataIT` — 12 тестов, `NsiClaimTermEditRoleIT` — 3 теста, `WorkCalendarSnapshotTest` — 10 тестов) выполнены и зелёные |
| C6 | Подтверждён | Поиск `dr_warranty_repair` и `WarrantyRepairDeadline` в `specs/work/legal-tech-claim-ui/input/calendar-behavior-differences.md` дал 0 совпадений; документ ссылается на новую схему `legal_tech_claim` |

## Выполненные проверки

1. **Проверка состояния репозитория и stash (F01):**
   - `git status -s`: файл `app/src/test/java/ru/fgk/ws/app/sso/SsoOidcUserMapperConcurrencyIT.java` удалён (staged `D`). Каталог `app/src/test/java/ru/fgk/ws/app/sso/` в файловой системе отсутствует.
   - `git stash list`: ровно 8 записей (от `stash@{0}` до `stash@{7}`), ни одна не изменена.
2. **Компиляция и статический анализ:**
   `./gradlew :app:compileJava :app:compileTestJava spotlessCheckAll` — BUILD SUCCESSFUL (10s).
3. **Критерий C1 (grep остатков):**
   `grep -rniE "warranty_?repair|warrantyrepair" app/src | grep -viE ...` — ни одного старого имени класса, таблицы, view, пакета, константы БД или ключа i18n раздела `legal`. Все совпадения принадлежат модулям `dr`, `rp`, `wagons`, `drcontract` либо разрешённым колонкам/полям.
4. **Критерий C2 (структура БД):**
   `docker exec docker-rvk-db-1 psql -U root -d postgres -c "select table_name, count(*) from information_schema.columns where table_schema='main' and (table_name like 'legal_tech_claim%' or table_name='legal_v_tech_claim') group by 1 order by 1;"`
   - `legal_tech_claim`: 49
   - `legal_tech_claim_case_one`: 16
   - `legal_tech_claim_deadlines`: 37
   - `legal_tech_claim_deps`: 76
   - `legal_tech_claim_du`: 34
   - `legal_v_tech_claim`: 104
   Таблицы/view `%warranty_repair%`: 0.
5. **Критерий C3 (индексы):**
   `docker exec docker-rvk-db-1 psql -U root -d postgres -c "select tablename, indexname, indexdef from pg_indexes where schemaname='main' and tablename like 'legal_tech_claim%' order by 1, 2;"`
   Все 6 целевых индексов соответствуют спецификации, включая уникальность и предикат частичного индекса.
6. **Критерий C4 (сверка ключей i18n):**
   Скрипт проверки на Python сопоставил ключи `ru.fgk.ws.app.legal.techclaim.entity/` в `messages_ru.properties` с полями 6 сущностей и 8 перечислений пакета `ru.fgk.ws.app.legal.techclaim.entity`. Итог: 354 ключа, missing: 0, orphaned: 0.
7. **Критерий C5 (полный набор тестов):**
   `./gradlew :app:test` — BUILD SUCCESSFUL (1m 55s). 1347 passing, 12 pending, 0 failing.
   По XML-отчётам подтверждено успешное выполнение:
   - `TechClaimDeadlineIT`: 14 passed
   - `TechClaimViewIT`: 3 passed
   - `ClaimTermCalculatorTest`: 40 passed (5 групп сценариев)
   - `WorkCalendarReferenceDataIT`: 12 passed
   - `NsiClaimTermEditRoleIT`: 3 passed
   - `WorkCalendarSnapshotTest`: 10 passed
   Тест `SsoOidcUserMapperConcurrencyIT` в прогоне отсутствует.
8. **Критерий C6 (документ отличий):**
   Поиск по `specs/work/legal-tech-claim-ui/input/calendar-behavior-differences.md` паттернов `dr_warranty_repair` и `WarrantyRepairDeadline` вернул 0 строк.

## Повторная проверка

- Ошибка критерия C5 из [checks/001.md](001.md) (падение `SsoOidcUserMapperConcurrencyIT.concurrentLoginsOfTheSameUserDoNotFail`):
  устранена в рамках замечания F01.1 удалением файла теста (`git rm`) в соответствии с решением пользователя по [Q02](../../questions/Q02.md).
- В повторном прогоне `./gradlew :app:test` нет ни одного упавшего теста (0 failing).
- Замечание [F01.1](../fixes/F01.md#f011--падение-concurrentloginsofthesameuserdonotfail-при-выполнении-apptest) подтверждено устранённым.

## Ошибки и непроверенное

Ошибок нет. Все критерии C1–C6 подтверждены текущими доказательствами.

## Следующий шаг

Таск [T01](../task.md) переведён в статус `done`, исправление [F01](../fixes/F01.md) — в статус `done`.
Следующий шаг по плану ([plan.md](../../plan.md)) — переход к волне 2: параллельная реализация тасков [T02](../../tasks/T02/task.md) (view `legal_v_tech_claim`) и [T03](../../tasks/T03/task.md) (роли и сервис `TechClaimService`).
