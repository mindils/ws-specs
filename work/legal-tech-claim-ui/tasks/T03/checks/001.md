# Проверка 001 — T03

Таск: [task.md](../task.md)
Результат реализации: [result.md](../result.md)
Итерация: 1
Дата: 2026-09-14
Состояние кода: commit 5bdd59347, рабочее дерево ветки feature/caseone-table (файлы T01, T02 и T03)
Окружение: Docker Compose (PostgreSQL 16.11 в docker-rvk-db-1 на localhost:5432, datasource application-test-local.properties), Gradle 9.5.1 daemon; изоляция: последовательный запуск тестов на локальной БД после проверок T01 и T02
Итог: pass — все критерии C1–C6 подтверждены; роли ДЭПС, ДЮ и администратора соответствуют матрице прав; TechClaimService.createClaim создаёт дополнительную претензию с парной строкой ДЮ и корректно переиспользует индекс при мягком удалении; регрессионные тесты и полный набор тестов :app:test пройдены без ошибок (1367 passing, 12 pending, 0 failing)

## Критерии и доказательства

| Критерий | Подтверждён / ошибка / не проверен | Доказательство или причина |
|---|---|---|
| C1 | Подтверждён | Роли `legal-tech-claim-deps`, `legal-tech-claim-du`, `legal-tech-claim-admin` регистрируются, контекст поднимается; политики соответствуют матрице прав (проверено в `TechClaimSecurityIT.depsRoleMatchesMatrix`, `duRoleMatchesMatrix`, `adminRoleMatchesMatrix` через `ResourceRoleRepository`; view/menu политики выставлены, лишние сущности исключены) |
| C2 | Подтверждён | Под ролью ДЭПС (`t03-claim-deps`) `LegalTechClaimDu` доступна только для чтения (`dataManager.save` отвергается с `AccessDeniedException`), изменения в `LegalTechClaimDeps` сохраняются успешно (`TechClaimSecurityIT.depsUserEditsOwnZoneOnly`) |
| C3 | Подтверждён | Под ролью ДЮ (`t03-claim-du`) `LegalTechClaimDeps` доступна только для чтения (`dataManager.save` изменения или создания новой записи отвергается с `AccessDeniedException`), изменения в `LegalTechClaimDu` сохраняются успешно (`TechClaimSecurityIT.duUserEditsOwnZoneOnly`) |
| C4 | Подтверждён | Обе роли имеют доступ на чтение к `VLegalTechClaim`, `LegalTechClaimDeadline`, `NsiWorkCalendarDay`, `NsiClaimTermNorm` и `NsiTermRevision`; попытки сохранения в `LegalTechClaimDeadline`, `NsiWorkCalendarDay`, `NsiClaimTermNorm` отвергаются с `AccessDeniedException` (`TechClaimSecurityIT.workRolesReadCalculationsAndReference`) |
| C5 | Подтверждён | `TechClaimService.createClaim` под ролью ДЭПС создаёт дополнительную претензию с `claim_index = max + 1`, создаёт парную пустую строку `LegalTechClaimDu` под системной аутентификацией с верным `deps_id`, не создаёт `LegalTechClaimCaseOne`, а слушатель автоматически создаёт `LegalTechClaimDeadline`; после мягкого удаления претензии индекс 2 переиспользуется; параллельное создание претензий даёт уникальные индексы (2 и 3); под ролью ДЮ вызов отвергается с `AccessDeniedException` без создания записей; распознавание конфликта индекса покрыто `TechClaimServiceTest` |
| C6 | Подтверждён | `./gradlew :app:test` — 1367 passing, 12 pending, 0 failing; `./gradlew spotlessCheckAll` — BUILD SUCCESSFUL; остаточных тестовых строк в БД нет |

## Выполненные проверки

1. **Компиляция и проверка форматирования:**
   - `./gradlew spotlessCheckAll` — BUILD SUCCESSFUL (1s).
   - `./gradlew :app:compileJava` — BUILD SUCCESSFUL (1s).
2. **Запуск интеграционных тестов безопасности T03 с принудительным перезапуском:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.it.TechClaimSecurityIT" --rerun-tasks`
   - Результат: 10 passing (24.2s), BUILD SUCCESSFUL.
   - Проверены: точное соответствие матрице прав политик ролей (`depsRoleMatchesMatrix`, `duRoleMatchesMatrix`, `adminRoleMatchesMatrix`), разграничение записи зон ДЭПС и ДЮ (`depsUserEditsOwnZoneOnly`, `duUserEditsOwnZoneOnly`), чтение и запрет записи в сроки, грид и НСИ (`workRolesReadCalculationsAndReference`), создание дополнительной претензии (`depsUserCreatesNextClaim`), повторное использование индекса после мягкого удаления (`claimIndexIsReusedAfterSoftDelete`), запрет создания претензии под ролью ДЮ (`duUserCannotCreateClaim`), корректное разведение индексов при конкурентном создании (`concurrentCreationGetsDistinctIndexes`).
3. **Запуск unit-тестов сервиса и связанных регрессионных тестов:**
   - `./gradlew :app:test --tests "ru.fgk.ws.app.legal.techclaim.service.TechClaimServiceTest" --tests "ru.fgk.ws.app.it.TechClaimDeadlineIT" --tests "ru.fgk.ws.app.it.NsiClaimTermEditRoleIT"`
   - Результат: 21 passing (24.1s), BUILD SUCCESSFUL:
     - `TechClaimServiceTest`: 4 passed (распознавание SQLState 23, имени constraint, игнорирование сторонних ошибок, безопасная обработка зацикленных причин).
     - `TechClaimDeadlineIT`: 14 passed (сервис пересчёта сроков работает без регрессий при сохранении претензий и зон ДЮ).
     - `NsiClaimTermEditRoleIT`: 3 passed (административная роль НСИ ведёт календарь и нормы).
4. **SQL-контроль чистоты БД после прогона тестов:**
   - Команда:
     ```bash
     docker exec docker-rvk-db-1 psql -U root -d postgres -c \
       "select (select count(*) from main.legal_tech_claim where wagnum = 90000003) as wagons, (select count(*) from main.sso_user where username like 't03-%') as users, (select count(*) from main.sec_role_assignment where username like 't03-%') as assignments;"
     ```
   - Результат: `wagons = 0`, `users = 0`, `assignments = 0` — тестовые сущности и пользователи полностью удалены.
5. **Полный прогон тестов приложения (C6):**
   - `./gradlew :app:test`
   - Результат: 1367 passing, 12 pending, 0 failing (BUILD SUCCESSFUL in 2m).

## Ошибки и непроверенное

Ошибок не обнаружено. Все критерии C1–C6 подтверждены.
Браузерная проверка экранов не проводилась (`render not browser-verified`), так как экраны `legal_TechClaim.list` и `legal_TechClaim.detail` реализуются в следующих тасках T04 и T05 (политики `@ViewPolicy` и `@MenuPolicy` зарегистрированы заранее согласно спецификации).

## Следующий шаг

Таск [T03](../task.md) переведён в статус `done`.
Следующий шаг по плану ([plan.md](../../plan.md)) — Волна 3: реализация тасков [T05](../../tasks/T05/task.md) (Detail view) и [T04](../../tasks/T04/task.md) (List view).
