# Отличия календарного расчёта: ВагТК → Jmix

Решение принято пользователем 2026-09-11: «Полный календарь», с отдельным
документом отличий для показа реализации.

Состояние реализации: календарь, нормы и календарная арифметика выполнены и
проверены в T07 — см. «Реализация календаря»; расчёт семи сроков, их хранение и
пересчёт выполнены в T08 — см. «Расчёт сроков» в конце документа. Результаты
старой MSSQL-процедуры на этих же данных не запускались: совпадение с ВагТК
проверено разбором кода первоисточника и тестами на искусственном календаре, а
не прогоном `pThp_PretTehSelect2`.

Переименование 2026-09-14 (T01 работы `legal-tech-claim-ui`): таблицы,
view, пакет, классы и Jmix-имена раздела получили базовое имя
`legal_tech_claim`; прежние имена раздела перечислены в таблице «Сводка»
файла [T01](../tasks/T01/task.md). Колонки, алгоритмы и нормы не менялись;
карта кода ниже приведена к новым именам.

## Источники и подтверждённые правила

- [fTHP_SrokDay.sql](fTHP_SrokDay.sql) — фактическая старая функция.
- [pThp_PretTehSelect2.sql](pThp_PretTehSelect2.sql) — условия назначения
  сроков и цвета, включая подготовку значений до блока `Srok_*`.
- [nvPretSrok_idoper_9-16.md](nvPretSrok_idoper_9-16.md) — нормы для
  `KodPret=1`, снятые с MSSQL 2026-09-11.
- [srok-calc-notes.md](srok-calc-notes.md) — навигация по SQL; при
  расхождении первичен полный SQL и явно согласованные здесь изменения.

| Этап | Старое поле | IdOper | Рабочих дней |
|---|---|---|---|
| Прикрепление комплекта документов | Srok_PassDoc | 16 | 2 |
| Передача в допретензионную работу | Srok_FilG | 9 | 12 |
| Предъявление допретензионного письма | Srok_DoPret | 10 | 10 |
| Передача в претензионную работу | Srok_PassPP | 11 | 19 |
| Предъявление претензии | Srok_Pret | 12 | 14 |
| Передача в судебную работу | Srok_PassSud | 13 | 4 |
| Подача иска | Srok_Sud | 14 | 14 |

## Согласованные отличия

| Аспект | ВагТК | Новая система |
|---|---|---|
| Выходные | Всегда суббота и воскресенье | Признак рабочего дня из полного календаря, включая рабочие субботы |
| Праздники | Однократно прибавляет число строк `nsHoliday` в первоначальном интервале | Проверяет каждую следующую дату; последовательные праздники пропускаются полностью |
| Старт в выходной | Сначала переносит старт на понедельник, затем прибавляет срок | Исходный день исключён; первый следующий рабочий день — день №1 |
| Источник календаря | `dbo.nsHoliday` плюс пятидневная арифметика | Собственная НСИ `main.nsi_work_calendar_day` (проверка `ws_store` выполнена, календаря там нет — см. «Реализация календаря») |
| Неполные данные | Нет проверки полноты календаря | Ошибка расчёта с указанием недостающего диапазона; нельзя показывать недостоверный срок |
| Вычисление | MSSQL-функция при выборке | Java-сервис сохраняет даты; db-view обеспечивает фильтрацию и актуальную просрочку |

Новый расчёт для положительного N: начать со следующей после исходной даты,
считать только рабочие дни до N включительно. Для N=0 вернуть исходную дату.
Отсутствие исходной даты означает отсутствие срока. Неизвестный норматив или
пробел календаря — ошибка данных, а не отсутствие срока.

Равенство всех результатов ВагТК и новой системы не является критерием
приёмки: отличия выше согласованы. Для обычной недели без праздников и
старта в рабочий день результаты должны совпадать.

## Примеры для демонстрации и тестов

Ниже **искусственные тестовые календари**, не утверждение о фактических
праздниках и переносах 2026 года. Дни недели у дат реальные.

| Исходные данные | Старый результат | Новый результат | Причина |
|---|---|---|---|
| Сб 12.09.2026 + 1 рабочий день; обычная пятидневка | Вт 15.09.2026 | Пн 14.09.2026 | Убрано предварительное перемещение старта на понедельник |
| Пн 14.09.2026 + 1 рабочий день; 15 и 16 сентября объявлены праздниками | Ср 16.09.2026 | Чт 17.09.2026 | Старый алгоритм добавляет праздник 15-го, но не проверяет новый конец 16-го |
| Пт 11.09.2026 + 1 рабочий день; суббота 12-го объявлена рабочей | Пн 14.09.2026 | Сб 12.09.2026 | Полный календарь учитывает рабочую субботу |
| Пн 14.09.2026 + 2 рабочих дня; праздников нет | Ср 16.09.2026 | Ср 16.09.2026 | Обычный отсчёт совпадает |

Первые три примера проверены при разборе задачи отдельным расчётом в памяти
на Python, воспроизводящим тело старой функции для заданных условий.
Это не проверка Java-реализации и не запуск функции на MSSQL.

## Что сохраняется

- Все семь норм и условия назначения из полной процедуры, кроме замены
  собственно календарной арифметики. Приоритеты `CASE` и `coalesce` —
  значимая часть правил; нельзя заменять `coalesce` выбором минимальной даты.
- `DoPret_Send + 14`, `Dat_Send + 14`, `Pret_Vozvrat_Dt + 14` — календарные
  дни; после них норма этапа прибавляется в рабочих днях.
- Эффективное `Send_PD` в исходном SELECT — `isnull(r.Send_PD, r.DatPret)`.
  Учесть этот fallback в расчёте и дате завершения этапа.
- Возврат для судебной передачи переопределяет срок только при
  `Pret_Vozvrat_Dt > Send_PD`; в SQL это отдельный UPDATE со своими
  условиями, а не ветка под ограничениями предыдущего UPDATE.
- Цвет: сравнить дату факта, иначе текущую дату, с сохранёнными порогами:
  `> +30 рабочих дней` — красный; иначе `> +10` — оранжевый; иначе
  `>= +1` — жёлтый; иначе подсветки нет. На датах ровно +10 и +30
  сохраняется предыдущая степень. Эти сравнения могут сменить цвет
  в выходной после порога: это не подсчёт числа рабочих дней просрочки.
- Завершённый этап не продолжает накапливать просрочку: сравнение с фактом.
  Цвета фиксируются в коде темы Jmix, не в НСИ и не в SQL как RGB-значения.

## Границы сопоставления

Автоматическая подмена `Dat_Pass` по реализации деталей/45 рабочим дням
в этой работе не переносится: используется дата комплекта новой модели.
Следовательно, сравнивать семь сроков нужно при одинаковых **эффективных**
исходных датах. Сравнение сырой строки ВагТК с обогащённым результатом
процедуры без подготовки дат не доказывает ошибку нового календаря.

Восьмой индикатор `Srok_FilOColor` (зависшие комплекты) и агрегатные
отчётные фильтры `@Col` не входят в согласованные семь сроков. В новой
работе обязательны фильтры по срокам и степеням просрочки в обычном гриде.

## Реализация календаря (T07, 2026-09-12)

### Проверка источника и выбранный путь

Проверено в БД rvk-ws (Docker Compose проекта, `localhost:5432`, схемы `main` и
`ws_store`) и в исходниках: производственного календаря нет ни в одной из схем.
Выполненные проверки перечислены в [результате T07](../tasks/T07/result.md).
Корпоративная `devx.main.vgk` из среды разработки не резолвится, SQL MCP не
подключён; отсутствие календаря в корпоративной `ws_store` подтверждено
пользователем 2026-09-12. Выбран путь собственной НСИ в схеме `main`;
проекция внешнего календаря не создаётся.

### Источник наполнения и покрытие

| Что | Значение |
|---|---|
| Источник | открытый производственный календарь РФ `xmlcalendar.ru` (сводка постановлений Правительства РФ о переносах), файлы `https://xmlcalendar.ru/data/ru/<год>/calendar.xml`, снято 2026-09-12 |
| Покрытие | 2024-01-01 … 2026-12-31, 1096 дней, сплошной диапазон без пропусков |
| Сверка | число рабочих дней 248 (2024), 247 (2025), 247 (2026) — совпадает с официальным календарём |
| Переносы в данных | рабочие субботы 27.04.2024, 02.11.2024, 28.12.2024, 01.11.2025; перенесённые выходные и праздники размечены отдельными видами дней |
| 2027 год | **не заполнен**: постановление о переносах на 2027 год на дату наполнения не опубликовано |

Пробел 2027 года оставлен намеренно и является рабочим поведением, а не
недоделкой: расчёт, вышедший за 2026-12-31, бросает `WorkCalendarGapException`
с недостающей датой и фактическим диапазоном, а не подставляет пятидневку.
Практическое следствие: сроки претензий, отсчёт которых (включая пороги
+30 рабочих дней) уходит в 2027 год, будут возвращать ошибку данных, пока
администратор не дозаполнит календарь. Диапазон 2024-2025 заполнен потому, что
перелив претензий из ВагТК даст исторические даты отцепок.

### Схема и код

| Артефакт | Где |
|---|---|
| Календарь | `main.nsi_work_calendar_day`, entity `ru.fgk.ws.app.nsi.entity.NsiWorkCalendarDay`, вид дня — `WorkCalendarDayKindEnum` |
| Нормы сроков | `main.nsi_claim_term_norm`, entity `NsiClaimTermNorm`, тип дней — `ClaimTermDayTypeEnum`; семь норм `KodPret=1` |
| Ревизии | `main.nsi_term_revision`, entity `NsiTermRevision`, состояние — `TermRevisionStatusEnum` |
| Календарная арифметика | `ru.fgk.ws.app.nsi.service.WorkCalendarSnapshot#addWorkingDays`, загрузка — `WorkCalendarService` |
| Нормы в расчёте | `ClaimTermNormService#loadNorms` / `#requireNorm` |
| Публикация | `TermReferenceRevisionService`, точка расширения `TermReferencePublicationHandler` |
| Тесты отличий | `app/src/test/java/ru/fgk/ws/app/nsi/service/WorkCalendarSnapshotTest.java` |
| Тесты наполнения и доступа | `app/src/test/java/ru/fgk/ws/app/it/WorkCalendarReferenceDataIT.java`, `NsiClaimTermEditRoleIT.java` |

Четыре примера из таблицы выше закреплены тестами
`WorkCalendarSnapshotTest`: старт в выходной, подряд идущие праздники, рабочая
суббота и обычная неделя. Там же проверены `N = 0`, отрицательное `N`, переход
через год и выход за границы наполненного календаря.

### Порядок публикации

1. Правка календаря или нормы через UI открывает (или дополняет) черновик
   ревизии — слушатель `TermReferenceDataChangeListener` работает до коммита,
   в той же транзакции, что и сама правка. С этого момента сохранённые сроки
   рассчитаны по прежней ревизии и актуальными не считаются.
2. Администратор нажимает «Опубликовать изменения» на экране ревизий.
   `TermReferenceRevisionService#publish` переводит черновик в `PUBLISHING`,
   вызывает все `TermReferencePublicationHandler` (пересчёт сроков добавляет
   T08) и только после их успеха ставит `PUBLISHED` с датой публикации.
3. Ошибка любого обработчика откатывает транзакцию публикации целиком:
   ревизия остаётся черновиком, прежние согласованные данные сохраняются,
   частично пересчитанный набор опубликованным не становится.
4. Начальное наполнение через Liquibase идёт мимо Jmix и событий не порождает,
   поэтому ревизия 1 сразу записана как `PUBLISHED`.
5. Тот же протокол распространяется на внешний источник календаря, если он
   появится: изменение снаружи регистрируется через `registerChange`, и до
   завершения пересчёта сроки прежней ревизии не выдаются за актуальные.

Ведение календаря и норм и публикацию даёт роль
`NsiClaimTermEditRole` (`nsi-claim-term-edit`). Чтение справочников рабочим
ролям ДЭПС и ДЮ добавляет T03; эту роль они не наследуют.

## Расчёт сроков (T08, 2026-09-12)

### Где что лежит

| Артефакт | Где |
|---|---|
| Чистый расчёт семи сроков и порогов | `ru.fgk.ws.app.legal.techclaim.service.ClaimTermCalculator` |
| Снимок исходных полей претензии | `ClaimTermSource` (новые имена полей, в javadoc — старые колонки) |
| Семь этапов и их коды операций | `ClaimTermStage` (`IdOper` 16, 9, 10, 11, 12, 13, 14) |
| Норма в виде, независимом от entity | `ClaimTermNormValue` |
| Срок и три порога одного этапа | `ClaimTermStageResult`, весь расчёт — `ClaimTermCalculation` |
| Сохранение, пересчёт, предварительный расчёт | `TechClaimDeadlineService` |
| Таблица расчёта | `main.legal_tech_claim_deadlines`, entity `LegalTechClaimDeadline`, changelog `01-tbl/324-legal_tech_claim_deadlines.xml` |
| Степень просрочки | `ClaimTermOverdueLevelEnum` (1/2/3); в базе не хранится |
| Пересчёт при изменении исходных данных | `TechClaimDeadlineRecalculationListener` |
| Пересчёт при публикации ревизии | `TechClaimDeadlinePublicationHandler` |
| Единая дата «сегодня» | `ClaimTermDateProvider` (`select current_date` базы) |
| Тесты формул и календарных отличий | `app/src/test/java/ru/fgk/ws/app/legal/techclaim/service/ClaimTermCalculatorTest.java` |
| Тесты записи, отката, массового пересчёта и публикации | `app/src/test/java/ru/fgk/ws/app/it/TechClaimDeadlineIT.java` |

Четыре примера из таблицы выше закреплены в T07 (`WorkCalendarSnapshotTest`);
в `ClaimTermCalculatorTest` те же отличия проверены уже на сроке этапа: рабочая
суббота сокращает срок прикрепления комплекта с 15.09 до 14.09.2026, подряд
идущие праздники 15 и 16 сентября сдвигают его с 16.09 на 18.09.2026.

### Что хранится и что вычисляется

Строка расчёта — одна на претензию (`deps_id` уникален) и содержит для каждого
из семи этапов четыре даты: срок и пороги +1, +10, +30 рабочих дней. Текущий
цвет и текущая степень просрочки НЕ хранятся: степень получается сравнением
даты фактического завершения этапа (иначе текущей даты БД) с порогами, поэтому
смена дня меняет её при следующем чтении, без ежедневной записи.

Неприменимый этап — вся группа из четырёх дат `NULL`. Успешный расчёт
существует и при семи неприменимых этапах; **отсутствие строки** означает, что
расчёта ещё не было. `revision_number` — номер ревизии НСИ, по которой считали.

Контракт для db-view T02: расчёт актуален, когда строка есть И
`revision_number` совпадает с номером последней опубликованной ревизии
(`main.nsi_term_revision`, статус `PUBLISHED`). Иначе претензия остаётся в
гриде, но помечается как требующая пересчёта. Открытый черновик ревизии —
глобальный признак «НСИ изменена, сроки ещё не пересчитаны»
(`TermReferenceRevisionService#hasUnpublishedChanges`).

Даты фактического завершения этапа для степени просрочки (порядок `coalesce`
первоисточника, заменять его выбором минимальной даты нельзя):

| Этап | Факт завершения |
|---|---|
| Прикрепление комплекта | `doc_transfer_date`, `Send_PD`, `pre_claim_transfer_date`, иначе дата БД |
| Передача в допретензионную работу | `Send_PD`, `pre_claim_transfer_date`, иначе дата БД |
| Предъявление допретензионного письма | `pre_claim_send_date`, иначе дата БД |
| Передача в претензионную работу | `Send_PD`, иначе дата БД |
| Предъявление претензии | `du.claim_send_date`, иначе дата БД |
| Передача в судебную работу | `du.to_law_date`, `du.lawsuit_date`, иначе дата БД |
| Подача иска | `du.lawsuit_date`, иначе дата БД |

Здесь `Send_PD` — эффективная дата передачи в претензионную работу:
`coalesce(deps.claim_transfer_date, du.claim_date)`.

### Когда выполняется пересчёт

1. **Сохранение исходных данных.** Слушатели `EntityChangedEvent` на
   `LegalTechClaimDeps`, `LegalTechClaimDu` и `LegalTechClaim`
   работают ДО коммита, в транзакции самого изменения. Ошибка расчёта
   (пробел календаря, отсутствующая норма) откатывает и исходное изменение:
   новые данные рядом со старым сроком не сохраняются. Изменение отцепки
   пересчитывает ВСЕ её претензии — договор аренды и договор планового ремонта
   входят в условия назначения сроков.
2. **Публикация ревизии календаря или норм.** `TermReferenceRevisionService#publish`
   вызывает `TechClaimDeadlinePublicationHandler`, который пересчитывает
   все претензии под номером ПУБЛИКУЕМОЙ ревизии, и только после успеха ревизия
   становится опубликованной. Ошибка откатывает публикацию целиком: ревизия
   остаётся черновиком, прежние расчёты сохраняются, смеси старых и новых нет.
3. **Массовый пересчёт вручную.** `TechClaimDeadlineService#recalculateAll()`
   — идемпотентен, повторный запуск даёт те же строки.

### Обязательный пересчёт после прямого импорта

Загрузка претензий SQL-ом мимо приложения (NiFi, перелив из ВагТК, ручные
`INSERT`/`UPDATE`) событий Jmix не порождает, поэтому сроки у таких претензий
не появляются и не обновляются. **Контракт интеграции:** любой такой импорт
обязан завершаться вызовом `TechClaimDeadlineService#recalculateAll()`.
До вызова импортированные претензии видны в гридах как требующие пересчёта —
это ожидаемое поведение, а не потеря данных. Проверено тестом
`directImportIsRecoveredByBulkRecalculation`.

### Граница календаря на практике

Календарь наполнен по 2026-12-31. Претензия, у которой срок или порог +30
рабочих дней уходит в 2027 год, **не сохраняется**: расчёт бросает
`WorkCalendarGapException`, и транзакция откатывается. Это согласованное
поведение («нельзя показывать недостоверный срок»), а не дефект. Лечится
дозаполнением календаря — changelog в `04-data` или административный экран
«Производственный календарь», — после чего выполняется публикация ревизии либо
`recalculateAll()`.

### Чем дополнить дальше

Дополнено T06 (см. «Демонстрация» ниже): результаты примеров на реальном
календаре, демонстрация фильтров, смены дня и публикации ревизии.

## Демонстрация (T06, 2026-09-14)

Раздел показывает, что даёт реализация на **реальном** производственном
календаре 2026 года из `main.nsi_work_calendar_day` (источник —
`xmlcalendar.ru`, 1096 дней с 2024-01-01 по 2026-12-31). Примеры из таблицы
«Примеры для демонстрации и тестов» построены на искусственных календарях,
поэтому здесь они пересчитаны без выдуманных праздников и рабочих суббот.

### Примеры таблицы на реальном календаре

В сентябре 2026 года нет ни праздников, ни перенесённых и ни рабочих суббот:
дни с 07.09 по 21.09 — обычная пятидневка. Поэтому из четырёх примеров
таблицы отличие сохраняет только первый.

| Исходные данные | ВагТК | Новая система | Совпадают |
|---|---|---|---|
| Сб 12.09.2026 + 1 рабочий день | Вт 15.09.2026 | Пн 14.09.2026 | нет |
| Пн 14.09.2026 + 1 рабочий день | Вт 15.09.2026 | Вт 15.09.2026 | да |
| Пт 11.09.2026 + 1 рабочий день | Пн 14.09.2026 | Пн 14.09.2026 | да |
| Пн 14.09.2026 + 2 рабочих дня | Ср 16.09.2026 | Ср 16.09.2026 | да |

Второй и третий примеры совпадают именно потому, что их отличие создавал
искусственный календарь: 15 и 16 сентября праздниками не объявлены, суббота
12 сентября рабочей не объявлена. Первый пример сохраняет отличие безусловно:
старт в выходной — правило алгоритма, а не свойство календаря.

Результаты новой системы получены запросом к наполненному календарю, то есть
той же арифметикой, что у `WorkCalendarSnapshot#addWorkingDays` (исходный день
исключён, каждый следующий день проверяется по календарю). Результаты ВагТК
выведены разбором тела `fTHP_SrokDay` при `@type = 2`; для сентября 2026 года
вывод не зависит от наполнения `dbo.nsHoliday`, потому что праздников в
интервалах примеров нет. Процедура на MSSQL не запускалась — сервер вне
корпоративной сети недоступен.

### Отличие на реальных праздниках 2026 года

Подряд идущие праздники в реальном календаре — новогодние каникулы. На них
отличие видно без искусственных данных:

| Исходные данные | ВагТК | Новая система | Причина |
|---|---|---|---|
| Ср 31.12.2025 + 1 рабочий день | Пт 02.01.2026 | Пн 12.01.2026 | Старый алгоритм однократно прибавляет праздники, попавшие в первоначальный интервал (там только 01.01), и новый конец 02.01 уже не проверяет; новый расчёт проходит все каникулы 01–08.01, перенос выходного 09.01 и выходные 10–11.01 |

Старый результат 02.01.2026 получается при любом наполнении `dbo.nsHoliday`,
в котором 1 января — праздник: в первоначальный интервал
`[31.12.2025, 01.01.2026]` другие дни каникул не попадают.

### Что показал сквозной сценарий

Проверено на локальной БД под ролями ДЭПС, ДЮ и администратора раздела;
выборка — 69 претензий, страница грида — 50 строк.

- **Семь сроков и степени согласованы.** У претензии, заведённой ДЭПС и
  дополненной ДЮ, значения вкладки «Сроки» совпали со строкой
  `legal_tech_claim_deadlines` и с колонками `legal_v_tech_claim`: срок
  прикрепления комплекта 17.07.2026 от даты комплекта 15.07.2026, письмо
  17.08.2026, передача в претензионную работу 30.09.2026, предъявление
  претензии 21.09.2026, передача в судебную 17.09.2026, подача иска
  02.10.2026; этап передачи в допретензионную работу — «Срок не назначен»
  (вагон не в аренде и договор на плановый ремонт есть).
- **Фильтры считает БД.** Степень 3 на первом и на седьмом этапе — по 16
  строк, степень 1 на первом этапе — 34, диапазон срока первого этапа
  11.06–21.06.2026 — 16, «Требуется пересчёт» да/нет — 33 и 36, «ВСЕГО к
  возмещению» ≥ 30 000 — 36, «Сумма претензии» ≥ 20 000 — 27. Все счётчики
  пагинации совпали с контрольными `count(*)` по view.
- **Смена дня меняет степень при следующем чтении.** Две претензии с порогами,
  сдвинутыми ровно на сутки, читаются одновременно: у первой степени 1, 2 и 3
  на трёх соседних этапах, у второй — пусто, 1 и 2. Завтрашнее чтение второй
  даст то же, что сегодняшнее чтение первой; часы при этом не переводились.
- **Завершённый этап перестаёт копить просрочку.** После публикации ревизии,
  сдвинувшей срок прикрепления комплекта с 17.07 на 20.07.2026, степень этого
  этапа сменилась с «Просрочка от 1 рабочего дня» на «В срок»: дата факта
  20.07.2026 сравнивается с новым порогом, а не с текущей датой.
- **Публикация ревизии пересчитывает сроки.** Правка одного дня календаря
  открыла черновик ревизии, публикация пересчитала все 69 претензий под новым
  номером ревизии, признак «Требуется пересчёт» снялся у всех, включая
  претензии, у которых строки расчёта не было вовсе.
- **Ошибка пересчёта не публикует ревизию.** Претензия с датой комплекта
  30.12.2026 (срок уходит за конец наполненного календаря) откатила публикацию
  целиком: ревизия осталась черновиком, все сохранённые расчёты остались на
  прежнем номере, смеси старых и новых не возникло.
